# Dual-Format SBOM Data-Placement Map (CycloneDX 1.6 ↔ SPDX 2.3 ↔ SPDX 3.0.1)

**Version**: v1 | **Canonical home**: this file (`docs/reference/sbom-format-mapping.md`). A mirror copy lives at `specs/010-spdx-output-support/contracts/sbom-format-mapping.md` for plan-review purposes; when the two disagree, *this file wins*.

This document is the contract that the SPDX 2.3 serializer (and the SPDX 3 stub) MUST honor. Per FR-013, FR-014, FR-015, every data element mikebom emits in CycloneDX has a row here naming its target location in each format and a one-line justification.

For the broader **component-tier model** (package-tier vs binary-tier vs file-tier, content-shape allowlist, full-mode override, behavior across `--file-inventory` modes), see `docs/reference/component-tiers.md`. That doc cites this catalog for per-annotation wire-shape details; this doc cites the tier model for behavior questions.

## Conventions

- **CycloneDX location**: JSON Pointer-style path into the CycloneDX 1.6 document (`/components/{i}/<field>` etc.).
- **SPDX 2.3 location**: Either a JSON path into the SPDX 2.3 document (`/packages/{i}/<field>`), an SPDX `Relationship` description, an SPDX `Annotation` with the named `field` envelope (per `mikebom-annotation.schema.json`), or `omitted — <reason>`.
- **SPDX 3.0.1 location**: Either a JSON-LD path into a 3.0.1 element graph (`{Element}/<property>`), an `Annotation` element pointer, an `ExternalRef` / `ExternalIdentifier` entry shape, or `omitted — <reason>` for entries mikebom intentionally does not emit.
- "**Annotation** ⟨field⟩" is shorthand for: emit one `SpdxAnnotation` whose `comment` is the JSON-encoded `MikebomAnnotationCommentV1` envelope with the named `field` value.

## Section A — Core identity (native fields in all three formats)

| # | mikebom data | CycloneDX 1.6 location | SPDX 2.3 location | SPDX 3.0.1 location | Justification |
|---|--------------|------------------------|-------------------|---------------------|---------------|
| A1 | PURL | `/components/{i}/purl` | `/packages/{i}/externalRefs[]` with `referenceCategory: "PACKAGE-MANAGER"`, `referenceType: "purl"`, `referenceLocator: <PURL>` | `software_Package/software_packageUrl` **plus** `software_Package/externalIdentifier[]` entry with `externalIdentifierType: "packageUrl"`, `identifier: <PURL>`. Mikebom emits both: the typed property is the canonical placement; the ExternalIdentifier entry is what the CDX ↔ SPDX 3 parity test looks for (downstream parsers commonly walk ExternalIdentifier lists). | All three formats have native, identical PURL semantics. SPDX 3 uses the dedicated `software_packageUrl` property **and** an `ExternalIdentifier[packageUrl]` entry; the entry's `externalIdentifierType` value is `"packageUrl"` (not `"purl"`) per the SPDX 3.0.1 `prop_ExternalIdentifier_externalIdentifierType` enum. |
| A2 | name | `/components/{i}/name` | `/packages/{i}/name` | `software_Package/name` (inherited from `Element_props`). | Native in all. |
| A3 | version | `/components/{i}/version` | `/packages/{i}/versionInfo` | `software_Package/software_packageVersion`. | Native in all. |
| A4 | supplier (org) | `/components/{i}/supplier/name` | `/packages/{i}/supplier` (`"Organization: <name>"` or `"NOASSERTION"`) | `software_Package/suppliedBy` (inherited from `Artifact_props`), value is an IRI reference to a separate `Organization` element in the same `@graph`. Organization element is deduplicated by name so one supplier that ships N packages produces one Organization + N `suppliedBy` references. | Native in all. SPDX 3 models supplier as an Artifact_props property pointing at a separate Agent element (not a Relationship edge — the `relationshipType` enum has no `suppliedBy` entry in 3.0.1). |
| A5 | author (person) | `/components/{i}/author` | `/packages/{i}/originator` (`"Person: <name>"` or `"NOASSERTION"`) | `omitted — mikebom's resolution layer does not currently surface an originator/author signal`. When the resolution layer grows an originator slot, this maps to `software_Package/originatedBy: [<Agent IRI>]` (inherited from `Artifact_props`, array-valued). The SPDX 2.3 path also omits `originator` today for the same reason. | Native in all once mikebom has the data. Both SPDX 2.3 and SPDX 3 paths omit pending resolution-layer support. |
| A6 | hashes | `/components/{i}/hashes[]` (each: `{alg, content}`) | `/packages/{i}/checksums[]` (each: `{algorithm, checksumValue}`) | `software_Package/verifiedUsing[]` of `Hash` elements with `algorithm` + `hashValue`. `algorithm` values are lowercase-no-hyphen (`sha256`, `sha1`, `md5`) per SPDX 3.0.1's `prop_Hash_algorithm` enum. | Native in all; mapping is 1-1. |
| A7 | license — declared | `/components/{i}/licenses[]` with `license.id` or `expression` | `/packages/{i}/licenseDeclared` — one of: canonical SPDX expression (all terms canonicalize), `"LicenseRef-<hash>"` (any term fails canonicalization, per the all-or-nothing canonicalization rule; matching entry in document-level `hasExtractedLicensingInfos[]` carries the raw expression as `extractedText`), `"NOASSERTION"` (no license data), or `"NONE"` (explicit no-license assertion, currently unused by mikebom) | `Relationship` element with `relationshipType: "hasDeclaredLicense"`, `from: <Package IRI>`, `to: [<LicenseExpression IRI>]`; the LicenseExpression is a separate `simplelicensing_LicenseExpression` element carrying `simplelicensing_licenseExpression: "<canonical SPDX expr>"`. LicenseExpression elements are deduped across packages. Non-canonicalizable expressions land via the same element with raw-string passthrough (`v3_licenses.rs::canonicalize_or_raw`). | Native in 1.6/2.3; SPDX 3 expresses licenses via a Relationship edge to a first-class LicenseExpression element. The `LicenseRef-<hash>` path for SPDX 2.3 preserves non-canonicalizable expressions with the same fidelity SPDX 3 has always had. |
| A8 | license — concluded | `/components/{i}/licenses[]` (concluded marker) | `/packages/{i}/licenseConcluded` — same shape as A7: canonical SPDX expression, `"LicenseRef-<hash>"`, `"NOASSERTION"`, or `"NONE"` | Same shape as A7 with `relationshipType: "hasConcludedLicense"`. **Omitted when concluded expression equals declared expression** (no redundant edge — mirrors the SPDX 2.3 path's defensive-omission rule). | Native in 1.6/2.3; SPDX 3 mirrors A7's shape. |
| A9 | external reference — homepage | `/components/{i}/externalReferences[]` with `type: "website"` | `/packages/{i}/externalRefs[]` with category `OTHER`, type `homepage` (or `homepage` field on Package if present in our data) | `software_Package/software_homePage` | Native in 1.6/2.3; 3.0.1 has dedicated `software_homePage`. |
| A10 | external reference — VCS | `/components/{i}/externalReferences[]` with `type: "vcs"` | `/packages/{i}/externalRefs[]` with category `OTHER`, type `vcs` | `software_Package/software_sourceInfo` | Native homes available in all. |
| A11 | external reference — deps.dev / registry | `/components/{i}/externalReferences[]` with `type: "distribution"` | `/packages/{i}/downloadLocation` (when distribution URL is canonical) and/or `externalRefs[]` | `software_Package/software_downloadLocation` | Use `downloadLocation` when authoritative; `NOASSERTION` otherwise. |
| A12 | CPE (one per fully-resolved candidate) | `/components/{i}/cpe` (primary single-valued) | `/packages/{i}/externalRefs[]` with `referenceCategory: "SECURITY"`, `referenceType: "cpe23Type"`, `referenceLocator: <CPE string>` | One `software_Package/externalIdentifier[]` entry per fully-resolved CPE with `externalIdentifierType: "cpe23"`, `identifier: <CPE string>`. Unresolved candidates stay in the `mikebom:cpe-candidates` Annotation per C19. | Native in all. SPDX 3's ExternalIdentifier list accepts multiple entries; the C19 split (native for resolved, Annotation for candidate set) matches the SPDX 2.3 pattern. |
| A13 | binary-reader component role (`Application` / `SharedLibrary` / `Object` / `Other`) | `/components/{i}/type` — `application` for Application, `library` for SharedLibrary, `file` for Object, `library` for Other (historic default, preserves consumer compat for unclassifiable binary-reader components) | `/packages/{i}/primaryPackagePurpose` — `APPLICATION` / `LIBRARY` / `FILE`; omitted for Other | `software_Package/software_primaryPurpose` — `application` / `library` / `file`; omitted for Other | Milestone 104 — derived from the source file's format header (Mach-O `MH_*` filetype, ELF `e_type` + `PT_INTERP` for PIE disambiguation, PE `IMAGE_FILE_HEADER.Characteristics`). Pre-milestone-104 every binary-reader component emitted CDX `type: "library"` regardless of role, mis-typing executables (issue surfaced 2026-05-24). All three formats now agree per the [binary-role cross-format contract](../../specs/104-binary-role-classification/contracts/binary-role-cross-format-mapping.md). Only affects binary-reader-emitted components (`scan_fs/binary/`); manifest- and lockfile-driven readers (npm / cargo / maven / pip / gem / deb / rpm / apk) keep their existing per-ecosystem `library` default. Per Constitution Principle V, all three native fields exist in their specs — no `mikebom:binary-role` annotation is introduced. |

## Section B — Graph structure (relationships)

| # | mikebom data | CycloneDX 1.6 location | SPDX 2.3 location | SPDX 3.0.1 location | Justification |
|---|--------------|------------------------|-------------------|---------------------|---------------|
| B1 | dependency edge (runtime) | `/dependencies[]/dependsOn[]` | `Relationship` with `relationshipType: "DEPENDS_ON"` | `Relationship` element with `relationshipType: "dependsOn"` | Native graph edges in all. |
| B2 | dependency edge (dev/build/test scope) | `/components/{i}/scope = "excluded"` (native CDX 1.6 enum value, set on the dep target) **plus** `mikebom:lifecycle-scope` property per C42 carrying the finer `development` / `build` / `test` distinction | **Default (`--spdx2-relationship-compat=full`):** native `Relationship` `relationshipType` of `DEV_DEPENDENCY_OF` / `BUILD_DEPENDENCY_OF` / `TEST_DEPENDENCY_OF` (one per scope value, reversed direction per spec) — the SPDX 2.3 spec's purpose-built field for the dev/build/test distinction. **`--spdx2-relationship-compat=basic`:** flat `DEPENDS_ON` edges in natural direction; scope information lives only on the target Package's `mikebom:lifecycle-scope` annotation per C42. **In both modes** the target Package also carries the `mikebom:lifecycle-scope` annotation so consumers walking `DEPENDS_ON` only — the Trivy / Syft convention — can still distinguish dev/build/test deps from deployed-runtime deps. **Both modes are spec-conformant but NOT equivalent**: `full` is the spec-native expression and the mikebom default per Constitution Principle X (Transparency); `basic` is a deliberate downshift for tool-compat reach. **This distinction is consumer-critical signal**: vulnerability scanners, license auditors, container deployment policies, and SBOM-diff tools all need to know which components ship to production vs which are dev-only or test-only. | Native `Relationship` `relationshipType: "dependsOn"` **plus** `scope` field on the relationship element carrying the SPDX 3 `LifecycleScopeType` enum value (`development` / `build` / `test`). Runtime-scoped edges omit `scope`. | Milestone 052 redesign: pre-052 used a custom `mikebom:dev-dependency` annotation in all three formats. Per Constitution Principle V (v1.4.0), every format has native fields for this signal — they're now used as the primary mechanism. CDX 1.6's 3-value `scope` enum (`required`/`optional`/`excluded`) cannot express the dev-vs-build-vs-test split, so the finer signal lives in `mikebom:lifecycle-scope` per C42 (Principle V finer-info carve-out). Issue #228 extended C42 to SPDX 2.3 as a parity-bridging annotation (Principle V parity-gap carve-out) so the dev/build/test distinction is recoverable from a `DEPENDS_ON`-walk under either relationship-compat mode. |
| B3 | nested containment (shade-jar parent → children) | `/components/{i}/components[]` (CDX nests inline) | Flat `Package`s + `Relationship` with `relationshipType: "CONTAINS"` from parent SPDXID to child SPDXID | Flat `Package`s + `Relationship` `relationshipType: "contains"` | SPDX has no nesting; containment is expressed by relationships. Edge case: nested components. |
| B4 | image / filesystem root | `/metadata/component` | `documentDescribes: [<root SPDXID>]` plus `Relationship` `DESCRIBES` from document to root | `SpdxDocument/rootElement` | All three formats name the root differently; mapping is mechanical. Synthesize a root if no natural one exists. |

## Section C — mikebom-specific data preserved via fallback

These rows have no native SPDX 2.3 home. Per clarification Q2 (Option A) and FR-016, they land in SPDX `annotations[]` with the named `MikebomAnnotationCommentV1.field` value. The annotation is attached to the document or to the relevant Package as indicated.

| # | mikebom property | CycloneDX 1.6 location | SPDX 2.3 location | SPDX 3.0.1 location | Justification |
|---|------------------|------------------------|-------------------|---------------------|---------------|
| C1 | `mikebom:source-type` | `/components/{i}/properties[name="mikebom:source-type"]` | Annotation `mikebom:source-type` on Package | Annotation `mikebom:source-type` on Package (SPDX 3 `Annotation` element carrying `MikebomAnnotationCommentV1`) | No SPDX 2.3 field; no SPDX 3.0.1 native property — annotation-envelope preserves losslessly across both formats per Q2 strict-match. |
| C2 | `mikebom:source-connection-ids` | property | Annotation `mikebom:source-connection-ids` on Package | Annotation `mikebom:source-connection-ids` on Package | No typed SPDX 3 home; a future SPDX 3 build/provenance profile may define one, at which point this row upgrades. |
| C3 | `mikebom:deps-dev-match` | property | Annotation `mikebom:deps-dev-match` on Package | Annotation `mikebom:deps-dev-match` on Package | No SPDX 2.3 / 3.0.1 native field. |
| C4 | `mikebom:evidence-kind` | property | Annotation `mikebom:evidence-kind` on Package | Annotation `mikebom:evidence-kind` on Package | SPDX 3 evidence profile is not yet stabilized in 3.0.1; annotation is the permanent home in 3.0.1 and may upgrade in a follow-up profile revision. **Value vocabulary** is a closed enum enforced at emission time in `mikebom-cli/src/generate/cyclonedx/builder.rs`. Current members: `deb-status-file`, `rpm-status-file`, `rpmdb-sqlite`, `apk-status-file`, `alpm-local-db`, `brew-install-receipt`, `brew-cask-metadata`, `dpkg-info-fallback`, `rpm-file`, `opkg-status-db` (m169 US2), `ipk-file` (m169 US1), `symbol-fingerprint`, `pacman-desc`. Reader-provenance labels distinguish installed-DB emissions (single-canonical-source-of-truth on a running rootfs — dedup wins) from archive-file emissions (per-artifact scan of an .rpm / .ipk / .deb sitting in a downloads directory — dedup loses when the installed-DB has the same PURL). See milestone 169 FR-016 for the ipk-file ↔ opkg-status-db dedup precedence. |
| C5 | `mikebom:sbom-tier` | property | Annotation `mikebom:sbom-tier` on Package | Annotation `mikebom:sbom-tier` on Package | No native field in either SPDX version. |
| ~~C6~~ | ~~`mikebom:dev-dependency`~~ | **REMOVED in milestone 052/part-2**. Pre-052 carried the dev-flag via this custom annotation across all three formats; per Constitution Principle V (v1.4.0) and milestone 052's Q1 → Option C, the signal now travels via native fields per B2 (CDX `scope` + new `mikebom:lifecycle-scope` per C42; SPDX 2.3 `DEV/BUILD/TEST_DEPENDENCY_OF`; SPDX 3 `lifecycleScope` parameter on `dependsOn`). | — | — | — |
| C7 | `mikebom:co-owned-by` | property | Annotation `mikebom:co-owned-by` on Package | Annotation `mikebom:co-owned-by` on Package | No native field. |
| C8 | `mikebom:shade-relocation` | property | Annotation `mikebom:shade-relocation` on Package | Annotation `mikebom:shade-relocation` on Package | Per research.md §R5: SPDX 3.0.1 has no typed property for "this artifact contains relocated symbols from another artifact"; `software_Package/contentBy` is a build-profile property whose semantics don't match. Annotation stays the home. |
| C9 | `mikebom:npm-role` | property | Annotation `mikebom:npm-role` on Package | Annotation `mikebom:npm-role` on Package | No native field. |
| C10 | `mikebom:binary-class` | property | Annotation `mikebom:binary-class` on Package | Annotation `mikebom:binary-class` on Package | No native field. |
| C11 | `mikebom:binary-stripped` | property | Annotation `mikebom:binary-stripped` on Package | Annotation `mikebom:binary-stripped` on Package | No native field. |
| C12 | `mikebom:linkage-kind` | property | Annotation `mikebom:linkage-kind` on Package | Annotation `mikebom:linkage-kind` on Package | No native field. |
| C13 | `mikebom:buildinfo-status` | property | Annotation `mikebom:buildinfo-status` on Package | Annotation `mikebom:buildinfo-status` on Package | No native field. |
| C14 | `mikebom:detected-go` | property | Annotation `mikebom:detected-go` on Package | Annotation `mikebom:detected-go` on Package | No native field. |
| C15 | `mikebom:binary-packed` | property | Annotation `mikebom:binary-packed` on Package | Annotation `mikebom:binary-packed` on Package | No native field. |
| C16 | `mikebom:confidence` | property | Annotation `mikebom:confidence` on Package | Annotation `mikebom:confidence` on Package | Per research.md §R5: SPDX 3 evidence profile (where confidence naturally lives) is not in 3.0.1 stable; annotation remains the home. Constitution Principle X transparency preserved. |
| C17 | `mikebom:raw-version` (RPM) | property | Annotation `mikebom:raw-version` on Package | Annotation `mikebom:raw-version` on Package | No native field. |
| C18 | `mikebom:source-files` | property carrying a **JSON-encoded array of rootfs-relative paths** (no leading `/`; no tempdir/rootfs prefix; cross-host deterministic). Pre-milestone-133 emission was a comma-separated string with absolute paths leaking the macOS tempdir; corrected in milestone 133 PR US2.1 per FR-012 defects A+B+C. | Annotation `mikebom:source-files` on Package, value is the SAME rootfs-relative JSON array (path normalization happens at source-population time in `scan_fs::mod.rs`, so CDX/SPDX 2.3/SPDX 3 read identical values — guaranteed by `holistic_parity` test under the C18 row's `SymmetricEqual` directionality). | Annotation `mikebom:source-files` on Package element with the same value. | Per research.md §R5: `software_Package/contentBy` exists in SPDX 3 but its shape ("the Element this Package's contents came from") doesn't match mikebom's list-of-source-file-paths signal; annotation preserves losslessly. **Milestone-133 US2.1 normalization (FR-012 defects A+B+C)**: the comma-string emission shape was a pre-existing serialization choice that broke machine parsing; the absolute-path emission leaked the scanner's tempdir into the SBOM (privacy + reproducibility issue). Source-side normalization in `scan_fs::sbom_path::normalize_sbom_path_relative` fixes both defects uniformly across all three formats. |
| C19 | `mikebom:cpe-candidates` | property | Annotation `mikebom:cpe-candidates` on Package (and `externalRefs[]` `SECURITY/cpe23Type` for any candidate that is fully resolved) | Split: **native** `ExternalIdentifier[cpe23]` entry per fully-resolved candidate (see A12) + `Annotation mikebom:cpe-candidates` carrying the full candidate list when more than one candidate exists | Multi-candidate set has no native home as a collection; resolved candidates are typed, residual set stays in annotation per Q2 strict-match. |
| C20 | `mikebom:requirement-range` | property | Annotation `mikebom:requirement-range` on Package | Annotation `mikebom:requirement-range` on Package | No native field. |
| C21 | `mikebom:generation-context` (document-level) | `/metadata/properties[name="mikebom:generation-context"]` | Document-level Annotation `mikebom:generation-context` | Document-level `Annotation` element on the SpdxDocument carrying `mikebom:generation-context` | Constitution Principle V requires "Generation Context"; annotation honors it in both SPDX versions. |
| C22 | `mikebom:os-release-missing-fields` (document-level) | metadata property | Document-level Annotation `mikebom:os-release-missing-fields` | Document-level `Annotation` element on the SpdxDocument carrying `mikebom:os-release-missing-fields` | No native field in either SPDX version. |
| C23 | `mikebom:trace-integrity-*` (document-level: ring-buffer-overflows, events-dropped, uprobe/kprobe-attach-failures) | metadata properties | Document-level Annotations `mikebom:trace-integrity-<subkey>` | Document-level `Annotation` elements on the SpdxDocument, one per subkey | Constitution Principle X transparency; preserved verbatim in both SPDX versions. |
| C24 | `mikebom:elf-build-id` | property | Annotation `mikebom:elf-build-id` on Package | Annotation `mikebom:elf-build-id` on Package | lowercase hex `NT_GNU_BUILD_ID` (typically 40 chars / 20-byte SHA-1). Linux universal binary identity used by `eu-unstrip`, `coredumpctl`, `debuginfod`, `*-dbgsym` packaging. No native field in either SPDX version. |
| C25 | `mikebom:elf-runpath` | property (JSON-encoded array) | Annotation `mikebom:elf-runpath` on Package (JSON array) | Annotation `mikebom:elf-runpath` on Package (JSON array) | `DT_RPATH` / `DT_RUNPATH` paths in declaration order, dedup'd. `$ORIGIN` etc. recorded raw (runtime-context-dependent). No native field. |
| C26 | `mikebom:elf-debuglink` | property (JSON-encoded object) | Annotation `mikebom:elf-debuglink` on Package (JSON object `{file, crc32}`) | Annotation `mikebom:elf-debuglink` on Package (JSON object `{file, crc32}`) | pointer to stripped-debug sibling file (`<name>.debug` + 8-hex CRC32). No native field. |
| C27 | `mikebom:go-vcs-revision` | property | Annotation `mikebom:go-vcs-revision` on Package | Annotation `mikebom:go-vcs-revision` on Package | commit SHA from Go `BuildInfo`'s `vcs.revision` key (set when `-buildvcs=true`, the Go default). Attached only to the main-module Go entry. No native field. |
| C28 | `mikebom:go-vcs-time` | property | Annotation `mikebom:go-vcs-time` on Package | Annotation `mikebom:go-vcs-time` on Package | RFC 3339 commit timestamp from `vcs.time`. Main-module only. No native field. |
| C29 | `mikebom:go-vcs-modified` | property | Annotation `mikebom:go-vcs-modified` on Package | Annotation `mikebom:go-vcs-modified` on Package | dirty-tree boolean from `vcs.modified` (recorded as the literal string `"true"` / `"false"` matching Go's wire format). Main-module only. No native field. |
| C30 | `mikebom:macho-uuid` | property | Annotation `mikebom:macho-uuid` on Package | Annotation `mikebom:macho-uuid` on Package | 16-byte `LC_UUID` hex-encoded lowercase (32 chars). The macOS analog of the ELF build-id; used by `dwarfdump`, crash-report symbolication, dSYM matching. Read from the FIRST slice on fat binaries. No native field in either SPDX version. |
| C31 | `mikebom:macho-rpath` | property (JSON-encoded array) | Annotation `mikebom:macho-rpath` on Package (JSON array) | Annotation `mikebom:macho-rpath` on Package (JSON array) | `LC_RPATH` paths in declaration order, dedup'd. `@executable_path` / `@loader_path` / `@rpath` recorded raw (runtime-context-dependent). No native field. |
| C32 | `mikebom:macho-min-os` | property | Annotation `mikebom:macho-min-os` on Package | Annotation `mikebom:macho-min-os` on Package | minimum OS shape `<platform>:<version>` (e.g. `macos:14.0`). Prefers `LC_BUILD_VERSION`; falls back to `LC_VERSION_MIN_MACOSX` / `LC_VERSION_MIN_IPHONEOS` / `LC_VERSION_MIN_TVOS` / `LC_VERSION_MIN_WATCHOS` when LC_BUILD_VERSION absent. No native field. |
| C33 | `mikebom:pe-pdb-id` | property | Annotation `mikebom:pe-pdb-id` on Package | Annotation `mikebom:pe-pdb-id` on Package | `<guid-hex-lowercase>:<age>` from the CodeView Type-2 record in `IMAGE_DIRECTORY_ENTRY_DEBUG`. The canonical PE binary identity used by Microsoft Symbol Server, Mozilla / Chromium symbol stores, WinDbg, drmingw. Linux analog is `mikebom:elf-build-id` (C24); macOS analog is `mikebom:macho-uuid` (C30). No native field in either SPDX version. |
| C34 | `mikebom:pe-machine` | property | Annotation `mikebom:pe-machine` on Package | Annotation `mikebom:pe-machine` on Package | lowercase string form of `IMAGE_FILE_HEADER.Machine` (`amd64`, `i386`, `arm64`, `armnt`, `ia64`, `riscv32`, `riscv64`, `unknown`). No native field. |
| C35 | `mikebom:pe-subsystem` | property | Annotation `mikebom:pe-subsystem` on Package | Annotation `mikebom:pe-subsystem` on Package | lowercase string form of `IMAGE_OPTIONAL_HEADER.Subsystem` (`console`, `windows-gui`, `efi-application`, `native`, `windows-boot-application`, etc.; `WINDOWS_CUI` → `console` per Microsoft toolchain idiom). No native field. |
| C36 | `mikebom:detected-cargo-auditable` | property (`"true"`/`"false"` literal) | Annotation `mikebom:detected-cargo-auditable` on Package (Bool literal) | Annotation `mikebom:detected-cargo-auditable` on Package (Bool literal) | cross-link flag set on the file-level binary component when the binary carries a parsed cargo-auditable `.dep-v0` manifest. The Rust analog of the long-shipped `detected_go = true` typed flag. Lets consumers find the per-crate `pkg:cargo/<name>@<version>` components emitted by `entry.rs::cargo_auditable_packages_to_entries` without scanning every component. No native field. |
| C37 | `mikebom:macho-codesign-identifier` | property | Annotation `mikebom:macho-codesign-identifier` on Package | Annotation `mikebom:macho-codesign-identifier` on Package | codesign identifier from the `LC_CODE_SIGNATURE` SuperBlob's `CodeDirectory.identOffset`. Typically the bundle ID (`com.apple.ls`) for app-signed binaries or the basename for ad-hoc-signed ones. Universal across Apple-signed binaries; the answer to "which binary is this codesign payload for". No native field in either SPDX version. |
| C38 | `mikebom:macho-codesign-flags` | property (JSON-encoded array) | Annotation `mikebom:macho-codesign-flags` on Package (JSON array) | Annotation `mikebom:macho-codesign-flags` on Package (JSON array) | decoded names from the `CodeDirectory.flags` u32 bitfield (`adhoc`, `hardened-runtime`, `library-validation`, etc.; unrecognized bits emit as `unknown-0x<hex>`). Sorted alphabetically for determinism. No native field. |
| C39 | `mikebom:macho-codesign-team-id` | property | Annotation `mikebom:macho-codesign-team-id` on Package | Annotation `mikebom:macho-codesign-team-id` on Package | 10-char Apple Team ID from `CodeDirectory.teamOffset` (CD version ≥ 0x20200). Present for developer-signed binaries; absent for ad-hoc-signed and Apple-system-binary signatures (whose `TeamIdentifier=not set`). No native field. |
| C40 | `mikebom:component-role` | property | Annotation `mikebom:component-role` on Package | Annotation `mikebom:component-role` on Package | Filesystem-position-classified component role. Open-enum string; current values: `build-tool` (component lives under build-tool installation paths — Maven `/usr/share/maven/lib/`, Gradle `/usr/share/gradle/lib/`, sbt `/opt/sbt/`), `language-runtime` (component lives under language-runtime system-installed paths — JDK `/usr/lib/jvm/*/lib/`, system Node `/usr/lib/node_modules/`, system Python `/usr/lib/python*/[site,dist]-packages/`); milestones 053 (Go) + 064 (cargo) + 066 (npm) + 068 (pip) + 069 (gem) + 070 (maven) add `main-module` (the workspace root component representing the project itself); milestone 106 (uv + Bun workspaces) adds `workspace-root` (a synthetic top-level component sitting above the workspace's `main-module` members — see `specs/106-ecosystem-coverage-expansion/contracts/workspace-emission.md` for the emission model). Three-state semantics: absence of the annotation does NOT mean "definitely application code" — it means the heuristic didn't classify the component. Drives downstream consumer filtering (vulnerability scanners, license auditors, conformance suites) without removing any component from the SBOM. **For the `main-module` value specifically (milestones 053 + 064 + 066 + 068 + 069 + 070)**: the C40 annotation is a SUPPLEMENTARY signal layered on top of each format's standards-native "BOM subject" construct per Constitution Principle V — CDX uses `metadata.component.type: "application"` (when N=1 main-module) or `components[]` siblings under a synthetic super-root (when N>1; cargo + npm workspace cases, maven multi-module reactor builds), SPDX 2.3 uses `primaryPackagePurpose: "APPLICATION"` plus `documentDescribes` (multi-target for workspace cases), SPDX 3.0.1 uses `software_primaryPurpose: "application"`. Consumers should prefer the native field; C40 exists for backwards-compat and cross-format consistency. **For the `workspace-root` value specifically (milestone 106)**: synthetic component with `pkg:generic/<workspace-name>` PURL, emitted by the uv (`uv.lock`) and Bun (`bun.lock`) readers when the root manifest declares a workspace section. Sits ABOVE the workspace's main-module members with `dependsOn` edges to each member; preserves intra-workspace dependency edges between members per the source manifest. Per-ecosystem main-module coverage matrix: Go ✅ (053), cargo ✅ (064), npm ✅ (066), pip ✅ (068, PEP 621 `[project]` only — Poetry-only `[tool.poetry]` deferred), gem ✅ (069, top-level `*.gemspec` only — application-style `Gemfile`-only projects skipped per FR-002), maven ✅ (070, top-level `pom.xml` + multi-module reactor builds with parent-POM inheritance + property substitution), uv ✅ (106), Bun ✅ (106). **Issue #104 fully closed by milestone 070.** |
| C41 | `mikebom:not-linked` | property (Bool literal `true`) | Annotation `mikebom:not-linked` on Package (Bool literal) | Annotation `mikebom:not-linked` on Package (Bool literal) | Set on Go source-tier components (from `go.sum`) when a Go binary is also present in the rootfs AND the binary's `runtime/debug.BuildInfo` does NOT confirm the component as linked. Means "this module was in the lockfile but the linker did not embed it in the compiled binary in this rootfs" — typical reasons: build-tag alternatives the linker DCE'd (e.g., `bytedance/sonic` when gin's fast-path isn't compiled in), test scaffolding (`davecgh/go-spew`, `kr/pretty`), older-yaml shims pulled by transitive go.mod requires. Two-state semantics: present = `true` (proven not-linked); absent = either "in BuildInfo" OR "no binary present to compare against" (consumers MUST check whether a binary was scanned to disambiguate). Driven by milestone 050 G3 redesign (was a silent drop in alpha.9 and earlier; now lossless tag). Composes with `mikebom:lifecycle-scope` (C42): a module can be both test-only (C42) and not-linked (C41). No native field. |
| C42 | `mikebom:lifecycle-scope` | property (open-enum string: `development` / `build` / `test`) | Annotation `mikebom:lifecycle-scope` on Package (issue #228 — emitted in BOTH relationship-compat modes). Composes with the native typed relationship variants (`DEV/BUILD/TEST_DEPENDENCY_OF`) under `--spdx2-relationship-compat=full` (default); under `--spdx2-relationship-compat=basic` it is the SOLE carrier of the scope distinction since edges collapse to plain `DEPENDS_ON`. | Annotation NOT emitted on SPDX 3 — SPDX 3 expresses the same signal natively via the `LifecycleScopeType` parameter on `dependsOn` per B2. | **Native-field audit per Constitution Principle V (v1.4.0)**: CDX 1.6 native `scope` enum (`required`/`optional`/`excluded`) cannot express the dev-vs-build-vs-test split — only inclusion-in-deployment. SPDX 2.3's native typed dependency variants (`DEV/BUILD/TEST_DEPENDENCY_OF` per B2) carry scope on the edge in `full` compat mode, but only `full` mode — and walking a `DEPENDS_ON`-only view (the Trivy / Syft convention deployed across most existing SBOM consumers) cannot see those edges. The `mikebom:lifecycle-scope` Package annotation bridges the parity gap so that whichever relationship-compat mode is in effect, the dev/build/test distinction is recoverable from the Package itself. **Why this matters to consumers**: knowing that a component is dev-only, build-only, or test-only — vs. a deployed-runtime dependency — is the difference between an actionable CVE on a shipped artifact and one that doesn't affect production. Tooling that can't distinguish these will over-report risk against test deps (`testify`, `junit`, `criterion`, `mocha`) and under-report against deployed runtime deps. SPDX 3.0.1 expresses scope natively via `LifecycleScopeType`, so the annotation is omitted there to avoid redundancy. Three-state semantics: present = `development`/`build`/`test`; absent = Runtime or unclassified. Composes with native CDX `scope: "excluded"` on the same components (C42 carries the finer distinction; CDX `scope` is the coarse signal). Driven by milestone 052/part-2's native-emission redesign + issue #228's SPDX 2.3 parity-bridge extension. |
| ~~C44~~ | ~~`mikebom:graph-completeness` (Go-scoped)~~ | **REMOVED in milestone 170** (closes #500-follow-up + #516). Pre-m170 this row carried a Go-scoped graph-completeness signal at annotation key `mikebom:graph-completeness` — the same key milestone 158's C104 uses for universal reachability. The collision produced duplicate emissions with undefined consumer behavior. Post-m170: (a) the universal graph-completeness signal is now the sole owner of `mikebom:graph-completeness`, documented at row C4 (with C104 as its parity-extractor entry); (b) the Go-specific transitive-edge-resolution signal has its modern home at row C110 (`mikebom:go-transitive-coverage`) with a richer reason-code vocabulary. Follow-up investigation on whether the pre-m170 C44 semantic is fully reconstructible from remaining signals is tracked at [issue #516](https://github.com/kusari-oss/mikebom/issues/516). | — | — | — |
| C45 | `mikebom:orphan-reason` | property (open-enum string: `stale-go-sum-entry` / `dead-lockfile-entry` / `hoisted-unused` / `unresolved-indirect-require` / `flat-attached-fallback`) | Annotation `mikebom:orphan-reason` on Package | Annotation `mikebom:orphan-reason` on Package | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native equivalent. The `mikebom:orphan-reason` annotation is the finer-info carve-out per Principle V (mirrors C40/C41's posture). Three-state semantics: present on orphan components (BFS-unreachable from `metadata.component.purl` following `dependsOn` edges — per milestone-167 clarification 2026-07-06) with the classification of WHY the orphan exists; absent on non-orphan components. Vocabulary lands in two milestones: milestone 061 introduced `unresolved-indirect-require` + `flat-attached-fallback` (Go-only, emitted at Go-reader-time in `golang/legacy.rs:2091,2118`); milestone 167 extended the vocabulary to 5 codes total by adding `stale-go-sum-entry` (Go multi-version case: orphan + same-name reachable sibling exists), `dead-lockfile-entry` (npm multi-version case, analogous), and `hoisted-unused` (npm no-sibling case) — all three emitted at emit-time in `generate/orphan_reason.rs`. FR-005 priority order (most-specific to least-specific) determines which code fires when a component matches multiple criteria: `stale-go-sum-entry` (Go multi-version) → `dead-lockfile-entry` (npm multi-version) → `hoisted-unused` (npm no-sibling) → `unresolved-indirect-require` (Go no-sibling, m061 preserved). `flat-attached-fallback` sits above the priority order — never overwritten by the m167 emit-time classifier (backward-compat guard). Emission scope is restricted to `pkg:golang/*` and `pkg:npm/*` ecosystems (FR-001); other ecosystems continue to carry no orphan-reason. Composes with C44 (doc-level completeness) — C44 aggregates ("the SBOM is partial because of these classes"); C45 localizes ("this specific component is an orphan because of this class"). |
| C46 | `mikebom:source-document-binding` | per-component `properties[]` entry — JSON-encoded string carrying the full `SourceDocumentBinding` payload (`{algo, hash?, source_doc_id, strength, reason?}`) per `contracts/source-document-binding-annotation.md` C-3 CDX 1.6 | per-Package `annotations[].comment` envelope (`MikebomAnnotationCommentV1`) — `value` is the real JSON object form of the same payload | per-Package `Annotation.statement` envelope — same shape as SPDX 2.3 | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native per-component "this binary's source SBOM identity + binding hash" carrier in any of the three formats. The standards-native cross-document REFERENCE (CDX `externalReferences[type:bom]`, SPDX 2.3 `externalDocumentRefs` + `BUILT_FROM` relationship, SPDX 3 `import` + `Relationship[built_from]`) is emitted as a SIBLING per FR-004 — but the per-component binding hash + strength label have no native equivalent at the per-component level today. Per Principle V's named pattern: native-first (cross-document refs), mikebom-annotation supplementary (per-component hash + strength), justification clause documented in `docs/reference/cross-tier-binding.md` (FR-010). Only emitted on `mikebom:sbom-tier: build` or `deployed` components — source-tier SBOMs do NOT carry this annotation (they ARE the binding target, not the bound entity). Driven by milestone 072 for cross-tier verification (closes the user's ask "verify the binary running in the image is the binary the source SBOM describes"). |
| C47 | `mikebom:identifiers` | document-level `metadata.properties[]` entry — JSON-encoded string carrying an array of `{scheme, value, source_label?}` objects (sorted lex by `(scheme, value)`) per `contracts/identifiers-annotation.md` C-2 CDX 1.6 | document-level `annotations[]` entry wrapped in the `MikebomAnnotationCommentV1` envelope — `value` is the real JSON array form of the same payload | document-level `Element.externalIdentifier[]` carries every identifier (built-in + user-defined) natively per SPDX 3's open-typed multi-identifier model; the `mikebom:identifiers` annotation is NOT used on SPDX 3 | **Native-field audit per Constitution Principle V (v1.4.0)**: built-in identifier schemes (`repo:`, `git:`, `image:`, `attestation:`) ride standards-native carriers — CDX `metadata.component.externalReferences[]` with per-scheme `type` mapping (`vcs` / `distribution` / `attestation`), SPDX 2.3 dual-carrier (main-module `Package.externalRefs[PERSISTENT-ID]` + redundant `creationInfo.creators` text line), SPDX 3 `Element.externalIdentifier[]`. The `mikebom:identifiers` annotation is the finer-info carve-out per Principle V's documented-exception path: it carries operator-defined opaque schemes (`acme_corp_id:`, `internal_ticket:`, etc.) for which no standards-native carrier accepts arbitrary opaque namespaces. SPDX 3's open-typed `Element.externalIdentifier[]` model handles BOTH built-in and user-defined identifiers natively, so the annotation is omitted on the SPDX 3 side. Driven by milestone 073 for cross-tier identifier handshake (closes the user's ask "stable identifier on emitted SBOMs so milestone 074's `--bind-to-source <id>` can resolve"). Justification clause: `docs/reference/identifiers.md` (FR-010). |
| C48 | `mikebom:resolver-step` | property (open-enum string: `go-sum-fallback` for now, may extend to other ladder steps in future milestones) | Annotation `mikebom:resolver-step` on Package | Annotation `mikebom:resolver-step` on Package | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native equivalent for "which transitive-resolution step claimed this component" in any of the three formats. CDX 1.6's `Component.evidence.identity[].methods[].technique` is the closest analog (enum: `manifest-analysis` / `binary-analysis` / etc.), but its values are coarse fingerprinting techniques, not resolver-step labels. The `mikebom:resolver-step` annotation is the finer-info carve-out per Principle V — operators making vulnerability-impact-radius decisions need to distinguish high-fidelity discovery (steps 1–3 of milestone-055's Go-resolver ladder: `go mod graph` / `$GOMODCACHE` / proxy fetch — full per-transitive parent-child topology) from lower-fidelity step-5 (milestone 091's `go.sum` flat fallback — root → transitive only, no inter-transitive structure). Two-state semantics for now: present with value `go-sum-fallback` on Go components reached via step 5 (offline + cache-empty CI configuration); absent on components reached via steps 1–3 (the higher-fidelity paths). Driven by milestone 091 (closes #174). Per Constitution Principle X (Transparency): step 5 is a known-lower-fidelity discovery path (loses inter-transitive topology), so consumers MUST be able to identify those edges. |
| C49 | `mikebom:elf-compiler-stamps` | per-file-level-component `properties[]` entry — JSON-encoded array of unique NUL-delimited compiler stamps from the ELF `.comment` section, within-binary deduped, per-entry capped at 4 KiB, total capped at 64 KiB with `"... (truncated)"` overflow marker | Annotation `mikebom:elf-compiler-stamps` on Package | Annotation `mikebom:elf-compiler-stamps` on Package | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native per-binary compiler/linker version field in CDX 1.6 / SPDX 2.3 / SPDX 3. CDX 1.6 has `component.publisher` (free-form supplier — describes who publishes the component, not how it was built); SPDX 2.3 has `Package.originator` / `Package.builtDate` (publisher + release date, not toolchain); SPDX 3's `Build` profile is far broader (build-instruction metadata) and doesn't carry per-binary toolchain identifiers. The `mikebom:elf-compiler-stamps` annotation is the build-tier-provenance carve-out — same posture as milestone-023's `mikebom:elf-build-id` / `mikebom:elf-runpath` / `mikebom:elf-debuglink`. Driven by milestone 098 to answer "who built this binary?" — complements milestone-096/097 identification-tier signals. |
| C50 | `mikebom:macho-build-version` | per-file-level-component `properties[]` entry — JSON-encoded object `{"platform", "min_os", "sdk"}` carrying the parsed Mach-O `LC_BUILD_VERSION` record. Unknown platform IDs stringify as `unknown-<numeric-id>` | Annotation `mikebom:macho-build-version` on Package | Annotation `mikebom:macho-build-version` on Package | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native field for per-binary Mach-O build-platform + SDK version metadata. SPDX 2.3 has no Mach-O-specific carriers at all; CDX 1.6 has `metadata.component.platform` (single-string label, not the structured `(platform, min_os, sdk)` triple). The `mikebom:macho-build-version` annotation is the build-tier-provenance carve-out — coexists with milestone-024's flat `mikebom:macho-min-os` property for backward compatibility. Driven by milestone 098. |
| C51 | `mikebom:macho-build-tools` | per-file-level-component `properties[]` entry — JSON-encoded array of `{"tool", "version"}` records from the Mach-O `LC_BUILD_VERSION` tools list, declaration order preserved | Annotation `mikebom:macho-build-tools` on Package | Annotation `mikebom:macho-build-tools` on Package | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native field for per-binary Mach-O linker + compiler tool versions. The milestone-098 annotation is the build-tier-provenance carve-out — typical values are `[{"tool": "clang", "version": "1500.0.40"}, {"tool": "ld", "version": "1015.0"}]`. Driven by milestone 098. |
| C52 | `mikebom:pe-linker-version` | per-file-level-component `properties[]` entry — `<major>.<minor>` decimal string from `IMAGE_OPTIONAL_HEADER.MajorLinkerVersion`/`MinorLinkerVersion`. Always-emit on parseable PEs (zero-zero → `"0.0"` informative for packers/obfuscators) | Annotation `mikebom:pe-linker-version` on Package | Annotation `mikebom:pe-linker-version` on Package | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native field for per-binary PE linker version. Closest is CDX 1.6's `component.publisher` (free-form, not a structured linker-version field). The `mikebom:pe-linker-version` annotation is the build-tier-provenance carve-out — always-emit per milestone-098 FR-003 (matches milestone-096 Q2 convention for `mikebom:binary-packed`). Driven by milestone 098. |
| C53 | `mikebom:download-url` | per-component `properties[]` entry — declared upstream archive/source URL (Bazel `http_archive.urls[0]` / `http_archive.url`, CMake `FetchContent_Declare(URL ...)` / `ExternalProject_Add(URL ...)`) | Annotation `mikebom:download-url` on Package | Annotation `mikebom:download-url` on Package | **Native-field audit per Constitution Principle V (v1.4.0)**: native homes exist in all three formats — CDX 1.6 `component.externalReferences[].type = "distribution"`, SPDX 2.3 `Package.downloadLocation`, SPDX 3 `software_Package/downloadLocation`. Current milestone-103 emission lands as an annotation alongside the PURL (the PURL already carries the upstream identity; this property captures the literal archive URL the build system pinned). **Open Principle-V migration candidate**: a follow-up milestone should switch CDX emission to `externalReferences[].type = "distribution"` and SPDX to `downloadLocation` — listed here so the gap is visible. |
| C54 | `mikebom:bazel-archive-name` | per-component `properties[]` entry — original `http_archive.name = "..."` label from the Bazel WORKSPACE rule, emitted only when it differs from the sanitized component `name` | Annotation `mikebom:bazel-archive-name` on Package | Annotation `mikebom:bazel-archive-name` on Package | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native equivalent in any of the three formats. CDX 1.6 has `component.group` (org/namespace, not a build-system-local target label), SPDX 2.3/3 have no Bazel-aware fields. The `mikebom:bazel-archive-name` annotation preserves the build-system-local label so reverse-lookup from SBOM to WORKSPACE rule is lossless when sanitization re-maps the name (e.g., `name = "com_google_googletest"` → sanitized `googletest`). Driven by milestone 103. |
| C55 | `mikebom:source-mechanism` | per-component `properties[]` entry — closed-enum string identifying which C/C++ reader and rule emitted this component. Values (15 total after milestone 155): alpha.41 seven — `cmake-fetchcontent-git`, `cmake-fetchcontent-url`, `cmake-externalproject`, `cmake-vendored`, `bazel-http-archive`, `vcpkg-manifest`, `conan-recipe`; milestone-105 six — `cpm-cmake`, `zephyr-west`, `idf-component`, `idf-component-local`, `vcpkg-classic`, `git-submodule`; milestone-155 two — `cmake-find-package`, `cmake-pkg-check-modules`. | Annotation `mikebom:source-mechanism` on Package | Annotation `mikebom:source-mechanism` on Package | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native equivalent for "which build-system mechanism emitted this component" in any of the three formats. The closest signal is the PURL ecosystem prefix (`pkg:vcpkg/...` vs `pkg:conan/...` vs `pkg:generic/...` vs `pkg:github/...`), but the PURL prefix conflates multiple mechanisms — `pkg:generic/foo` covers `cmake-fetchcontent-url`, `cmake-externalproject`, AND `cmake-vendored`. The `mikebom:source-mechanism` annotation is the finer-info carve-out per Principle V: operators trying to answer "where did this library come from in my build?" get an explicit, greppable, single-string answer rather than reverse-engineering PURL + per-reader annotations (`mikebom:vendored`, `mikebom:download-url`, `mikebom:bazel-archive-name`). Closed enum so consumers can build deterministic policy (vs free-text drift). Always-emit on every C/C++-reader-emitted component. |
| C56 | `mikebom:also-detected-via` | **Hybrid emission per milestone-105 research R1**: CDX 1.6 emits the signal natively in `/components/{i}/evidence/identity[0]/methods[]` — each detection record (winner first, losers follow) carries `{technique, confidence, mikebom-source-mechanism}`. The C56 parity extractor reads `methods[*].mikebom-source-mechanism` skipping the first entry. CDX components MAY omit a redundant `mikebom:also-detected-via` property entirely. | Annotation `mikebom:also-detected-via` on Package; value is a JSON-array string of the losing readers' source-mechanism values, sorted lexicographically by `SourceMechanism` enum Ord (deterministic per SC-010). | Same as SPDX 2.3 — annotation `mikebom:also-detected-via` on Package, JSON-array string payload. | **Native-field audit per Constitution Principle V (v1.4.0)**: CDX 1.6 has `evidence.identity[].methods[]` as a natively-aware multi-method shape — adequate for CDX. SPDX 2.3 has **no** native equivalent (no per-relationship-evidence model; closest is `Annotation` which is what we use). SPDX 3.0.1's `EvidenceMaterial` carries observation provenance but cannot natively express "this component's PURL was independently confirmed by another reader's source-mechanism" without lossy encoding. Per Principle V's last paragraph ("parity-bridging annotation permitted ONLY when one format has the native field but another doesn't"), the SPDX-side `mikebom:also-detected-via` annotation IS justified — it bridges the SPDX-side gap so the parity-extractor produces matching BTreeSets across all three formats. Milestone 105 (FR-015). |
| C57 | `mikebom:build-reference` | per-component `properties[]` entry — closed enum `declared-and-used` / `declared-only`. Emitted by the `git-submodule` reader (FR-008a) only. | Annotation `mikebom:build-reference` on Package. | Annotation `mikebom:build-reference` on Package. | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native equivalent for "is this submodule actually referenced by the parent project's `find_package` calls?" in any of the three formats. CDX 1.6's `component.scope` covers runtime/optional/excluded but not "declared-but-unreferenced source-tier submodule". SPDX 2.3/3 have no equivalent. The closed-enum annotation lets downstream vuln scanners filter un-referenced submodules with a single `jq` query. Milestone 105 (FR-008a). **Implementation note (research R3)**: this annotation was originally spec'd as a reuse of `mikebom:linkage-kind`, but that annotation is used by the binary readers with a closed enum of `dynamic`/`static`/`mixed` and a CDX builder debug-assert that would have been violated — `mikebom:build-reference` was introduced as a NEW annotation instead to preserve clean semantic separation between binary-tier link mode and source-tier build-graph reference. |
| C58 | `mikebom:fingerprint-corpus-sha` | per-component `properties[]` entry — string. Value: 12-hex prefix of the corpus commit SHA that produced the match (matches `git rev-parse --short` default), OR the literal `bundled` when the in-source bundled fallback produced the match. Emitted ONLY on components whose `mikebom:evidence-kind == "symbol-fingerprint"` AND only when the operator opted in via `--fingerprints-corpus` (or `MIKEBOM_FINGERPRINTS_CORPUS=1`). When non-opt-in (the default), the annotation is omitted entirely so byte-identity with pre-108 SBOMs is preserved (SC-003). Co-emitted with `mikebom:also-detected-via` (already C56) when the FR-013 multi-record collision fires (e.g., LibreSSL + OpenSSL share `SSL_*` symbols, both emit, each lists the other under `also-detected-via`). | Annotation `mikebom:fingerprint-corpus-sha` on Package. | Annotation `mikebom:fingerprint-corpus-sha` on Package (graph-element Annotation targeting the component, `statement.value` carries the string). | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native equivalent for "which fingerprint-corpus revision identified this component" in any of the three formats. The closest signal is CDX 1.6's `component.evidence.identity[].source.url` (a free-form URL for the evidence source), but URL-pointing-to-corpus-tarball is a heavier carrier than the 12-hex SHA, AND consumers would need to fetch the URL to recover the corpus revision rather than pasting 12 chars into `git log`. SPDX 2.3/3 have no per-component evidence-source URL field at all. The 12-hex annotation is the smallest collision-resistant signal that's still copy-pasteable into a browser address bar; the `bundled` sentinel surfaces the opt-in-but-fell-back case (cache empty + `--offline` OR network failure). Closed value space (12-hex regex OR literal `bundled`) so consumers can build deterministic policy. Driven by milestone 108 (FR-005). See `docs/reference/identifiers.md` §11 for the 4-step consumer lookup recipe + the FR-013 collision triage example. |
| C59 | `mikebom:fingerprint-confidence` | per-component `properties[]` entry — string. Value: numeric fused-confidence score formatted as `"X.XX"` (e.g., `"0.70"` / `"0.85"` / `"0.99"`). Emitted ONLY on components whose `mikebom:evidence-kind == "symbol-fingerprint"` AND only when the operator opted in via `--fingerprints-corpus` — same gating as C58 (`mikebom:fingerprint-corpus-sha`); the two co-emit. When non-opt-in (the default), the annotation is omitted entirely so byte-identity with pre-110 SBOMs is preserved. For v1 records (the milestone-108 corpus shape, which is what currently ships), the value is always `"0.70"` — the design-doc §7 "threshold-met exported symbols" baseline mapped to v1 records per the 2026-06-03 /speckit-clarify Q3 mapping. For v2 records (milestone 110 Phase 4+), the value is the matcher's fused output via the "max + bump" fusion rule across all matching indicators. The matcher suppresses emission entirely when fused confidence falls below 0.70 (the design-doc `medium` floor) per the 2026-06-03 Q1 clarification, so the value space at emission time is `[0.70, 0.99]`. | Annotation `mikebom:fingerprint-confidence` on Package. | Annotation `mikebom:fingerprint-confidence` on Package (graph-element Annotation targeting the component, `statement.value` carries the string). | **Native-field audit per Constitution Principle V (v1.4.0)**: CDX 1.6 has `component.evidence.identity[].confidence` as a native numeric (0–1 float) carrier for per-component identification confidence — adequate for CDX. The milestone-110 emission path (Phase 4) will populate this native field IN ADDITION TO the `mikebom:fingerprint-confidence` property for CDX so existing parity-extractor tooling reads either side. SPDX 2.3 has **no** native per-component confidence field; SPDX 3.0.1's evidence-profile typed-confidence model is not stable in 3.0.1 (sketched only). Per Principle V's last paragraph ("parity-bridging annotation permitted ONLY when one format has the native field but another doesn't"), the SPDX-side `mikebom:fingerprint-confidence` annotation IS justified — it bridges the SPDX-side gap so the parity-extractor produces matching BTreeSets across all three formats. Distinct from the existing C16 `mikebom:confidence` enum-string carrier (value=`"heuristic"`, fixed) so no value-space collision: C16 carries the qualitative evidence-strength label (a small closed enum); C59 carries the quantitative numeric score from the new milestone-110 fusion algorithm. The MVP-slice (milestone 110 Phase 3) emits the property carrier only; Phase 4 will additionally populate CDX-native `evidence.identity[].confidence` for the multi-indicator matcher's output. Driven by milestone 110 (FR-017). |
| C60 | `mikebom:build-inclusion` | property (open-enum string: `unknown` / `not-needed`). For `unknown` (milestone 112 US1): property only — NO native `scope` field is set (CDX consumers default an absent `scope` to `required`; emitting `excluded` would falsely assert NotNeeded, so the deliberate omission IS the signal per FR-002). For `not-needed` (milestone 112 US2): native `scope: "excluded"` is set UNCONDITIONALLY (independent of `--include-dev` / `--exclude-scope` filtering — it asserts a build-graph fact, not a view preference) alongside the property, plus the companion `mikebom:build-inclusion-derivation` property naming the evidence source. | Annotation `mikebom:build-inclusion` on Package (parity bridge). | Annotation `mikebom:build-inclusion` on Package (parity bridge; graph-element Annotation targeting the component). | **Native-field audit per Constitution Principle V (v1.4.0)**: CDX 1.6's native `scope` enum (`required`/`optional`/`excluded`) can express "excluded from the build" but NOT "participation unknown" — there is no fourth enum value, and absence already means `required` by spec default. SPDX 2.3 has **no native excluded-scope field at all**: `primaryPackagePurpose` is a type taxonomy, and the typed relationship variants (`DEV/BUILD/TEST_DEPENDENCY_OF` per B2) assert a *positive* scope, which would be false for a module the build does not need. SPDX 3.0.1's `LifecycleScopeType` enum on `dependsOn` (`build`/`design`/`development`/`runtime`/`test`/`other`) likewise has no `excluded` or `unknown` value. Per Principle V's parity-bridge carve-out, the annotation carries the signal in both SPDX formats and the finer `unknown`-vs-`not-needed` distinction in CDX. **Why this matters to consumers**: Go modules discovered via the lower-fidelity fallback paths (C48 `mikebom:resolver-step = go-sum-fallback`, C45 `mikebom:orphan-reason = flat-attached-fallback`) appear in `go.sum` but may never be linked into any build — vulnerability scanners that treat them as deployed-runtime deps over-report risk. Three-state semantics: `unknown` = mikebom could not establish build participation (toolchain-free heuristic, always on); `not-needed` = the Go toolchain's `go mod why -m` proved the main module does not need it (default-on when a `go` binary is present, opt-out via `--no-go-mod-why`); absent = production participation confirmed or assumed (pre-112 semantics, byte-identical emission). Mutually exclusive with C42 test-scope tagging (test-scoped modules keep their finer C42 signal). BuildInfo-confirmed components (binary present, no C41 `mikebom:not-linked`) are exempt per FR-010. Driven by milestone 112 (issue: 20/87 golang components in kusari-cli scans sat outside cyclonedx-gomod's build list with no consumer-visible scope). |
| C61 | `mikebom:build-inclusion-derivation` | property (open-enum string; value this milestone: `go-mod-why`). REQUIRED companion of `mikebom:build-inclusion = not-needed` (C60) — names the evidence source that established the verdict. NOT emitted alongside `unknown` (the toolchain-free heuristic is the documented default semantics of `unknown` itself; a discriminator would add no information). | Annotation `mikebom:build-inclusion-derivation` on Package. | Annotation `mikebom:build-inclusion-derivation` on Package (graph-element Annotation targeting the component). | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native equivalent for "which tool/analysis established this component's build-participation verdict" in any of the three formats. CDX 1.6's `evidence.identity[].methods[].technique` covers component *identity* evidence, not build-graph participation evidence. SPDX 2.3 has no per-package provenance field. SPDX 3.0.1's evidence/provenance profile cannot express "this exclusion verdict came from `go mod why -m`" without lossy free-text. The annotation is the provenance discriminator per Constitution Principle X (Transparency), mirroring C48 `mikebom:resolver-step` (milestone 091) and C62 `mikebom:lifecycle-scope-derivation`. **Why this matters to consumers**: `not-needed` triggers consumer-visible filtering (vuln scanners may deprioritize the component) — operators auditing a surprising exclusion need to know it came from the Go toolchain's own module-graph proof (`go mod why -m` answered "main module does not need module X") rather than a mikebom heuristic, and can reproduce it by running the same command. Open enum so future evidence sources (e.g., other ecosystems' toolchains) extend without schema churn. Driven by milestone 112 (FR-006). |
| C62 | `mikebom:lifecycle-scope-derivation` | property (open-enum string: `test-only-closure` / `go-mod-why`). Companion of `mikebom:lifecycle-scope = test` (C42) emitted ONLY when the test scope was derived by graph analysis rather than the direct per-module `_test.go` import-walk signal: `test-only-closure` = the module is transitively reachable only from test-only roots (test-closure propagation fix, 2026-06); `go-mod-why` = the Go toolchain's `go mod why -m` dependency chain ends in a `.test` package (milestone 112 US2). Absent on test-scoped components = direct import-walk signal (pre-existing semantics, byte-identical emission). | Annotation `mikebom:lifecycle-scope-derivation` on Package. | Annotation `mikebom:lifecycle-scope-derivation` on Package (graph-element Annotation targeting the component). NOTE: unlike C42 itself (omitted on SPDX 3 because `LifecycleScopeType` natively carries the scope), the *derivation* has no native carrier, so the annotation IS emitted on SPDX 3. | **Native-field audit per Constitution Principle V (v1.4.0)**: every format can carry the test *scope* (CDX `scope: "excluded"` + C42 property; SPDX 2.3 `TEST_DEPENDENCY_OF`; SPDX 3 `LifecycleScopeType` = `test`) but NO format has a field for *how* that scope was determined. The annotation is the provenance discriminator per Constitution Principle X, mirroring C48 `mikebom:resolver-step` and C61 `mikebom:build-inclusion-derivation`. **Why this matters to consumers**: graph-derived test scope is weaker evidence than a direct `_test.go` import — an operator disputing a test-only classification (e.g., a module they believe ships to production) needs to know whether to audit mikebom's import walk, the test-only closure propagation, or re-run `go mod why -m` themselves. Open enum so future derivation mechanisms extend without schema churn. Introduced by the test-closure propagation fix (value `test-only-closure`); extended by milestone 112 (value `go-mod-why`, FR-007). |
| C63 | `mikebom:exclude-path` | envelope-level — `metadata.properties[]` entry — string. Value: comma-joined list of every user-supplied exclusion entry that was in effect for the scan, in source order (CLI `--exclude-path` flags first, then `MIKEBOM_EXCLUDE_PATH` env-var entries split on the platform path-list separator). Literal entries are forward-slash-normalized; pattern entries appear verbatim. Emitted ONLY when at least one entry was in effect; absent (along with byte-identical pre-feature output) when no exclusions were supplied. | envelope-level — `creationInfo.annotations[]` entry — annotator=`Tool: mikebom-<version>`, annotationType=`OTHER`, comment=`mikebom:exclude-path=<comma-joined>`. Emission gating identical to CDX. | envelope-level — `Annotation` element on the `SpdxDocument` — annotationType=`other`, subject=`SpdxDocument.spdxId`, statement=`mikebom:exclude-path=<comma-joined>`. Emission gating identical to CDX. | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native field expressing "the operator excluded these paths during scan" exists in CDX 1.6, SPDX 2.3, or SPDX 3.0.1. CDX 1.6 has `metadata.lifecycles` (build phase, not path filter) and `metadata.tools.services` (tool name, not parameters). SPDX 2.3 has `creationInfo.creatorComment` (free-form opaque text, structurally inadequate). SPDX 3 `Annotation` is the format's general structured-extension surface. The `mikebom:exclude-path` annotation is the finer-info carve-out per Principle V — it carries scan-time operator-supplied filter parameters the standards do not model. **Per Constitution Principle X (Transparency)**: when operator-supplied exclusions narrow component completeness, consumers MUST be able to detect the narrowing from the SBOM itself (without access to the original scan invocation). This annotation provides that signal. Driven by milestone 113 (FR-014 / SC-007). Closes issue #334. |
| C64 | `mikebom:produces-binaries` | per-component `properties[]` entry — JSON-encoded string carrying an array of canonical binary names (lowercase ASCII, extensionless, deduped, lex-sorted). Emitted ONLY on main-module components (those carrying `mikebom:component-role = "main-module"` per C40); transitive-dependency components MUST NOT carry this property. Absent when the ecosystem manifest declares no binaries (library-only crates / packages) — absence is correct, not a gap. | Annotation `mikebom:produces-binaries` on Package (`MikebomAnnotationCommentV1` envelope, value = JSON array). | Annotation `mikebom:produces-binaries` on Package (graph-element Annotation targeting the component, same JSON-array shape as SPDX 2.3). | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native field expressing "this package produces these executable names" exists in CDX 1.6, SPDX 2.3, or SPDX 3.0.1. CDX 1.6's `externalReferences[type=executable]` carries URLs (homepage / distribution / VCS endpoints), not output-binary names — semantically wrong shape. CDX `evidence.identity[field=name]` is name-DISCOVERY confidence, not output-name CAPABILITY. SPDX 2.3 `Package.packageFileName` is the canonical filename of the SOURCE artifact, not the produced binaries. SPDX 3.0.1 `ExternalIdentifier[]` is for CPE/advisory references. SPDX `Annotation` is the documented extensibility point for novel semantics — same path C1–C45 already follow. The `mikebom:produces-binaries` annotation is the finer-info carve-out per Principle V. **Per Constitution Principle X (Transparency)**: when consumed by the cross-tier `--bind-to-source` flow (milestone 072), the annotation enables automatic aliasing of image-tier `pkg:generic/<name>` binaries back to the source-tier ecosystem PURL — the operator's flagship component binds without an explicit `--pkg-alias` flag. Per-ecosystem extraction: Cargo (`[[bin]]` + `src/main.rs` default + `src/bin/*.rs` implicit per spec Q1 clarification — emitted on EACH workspace member's main-module component), npm (`bin` field both string and object forms), pip (`[project.scripts]` + `[project.gui-scripts]`, setup.cfg `console_scripts`/`gui_scripts` fallback), gem (`executables` array), maven (shade-/jar-plugin `<finalName>`, extensionless per spec Q2 clarification — the binder's `.jar`-suffix tolerance handles the image-side `pkg:generic/baz.jar` translation), Go (filesystem walk for `package main` directories). The binder records alias provenance via the milestone-111 `SourceDocumentBinding` envelope's new `alias_source` field (`operator-supplied` vs `automatic-from-produces-binaries`) so auditors can distinguish the two paths. Driven by milestone 116 (Option B of issue #225, extending milestone 111's Option A `--pkg-alias` flag). |
| C65 | `mikebom:source-tier = "declared"` | per-component `properties[]` entry — string. NEW permitted value on the existing per-component source-tier key (joins `installed` / `analyzed` / `source` from pre-119). Emitted on every supplement-introduced SOLO component (PURL not in scanner output) and on the `services[]` entry side property. NOT emitted on collisions — those keep the scanner's pre-existing tier value, since the supplement provided enrichment, not the original observation. | Annotation `mikebom:source-tier` on Package, `MikebomAnnotationCommentV1` envelope, value=`declared`. | Annotation `mikebom:source-tier` on Package (graph-element Annotation targeting the component, same envelope shape as SPDX 2.3). | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native field expressing "this component was operator-declared via a supplement file, not scanner-observed from on-disk evidence" exists in CDX 1.6, SPDX 2.3, or SPDX 3.0.1. CDX 1.6's `evidence.identity[].methods[]` records identification methods, not source-tier provenance. SPDX 2.3 `Package.primaryPackagePurpose` is a category taxonomy, not a source-tier marker. SPDX 3.0.1's evidence-profile model would express this via `software_packagedBy`-style relationships in a future profile, but the 3.0.1 stable schema doesn't have a stable field for it. **Per Constitution Principle X (Transparency)**: consumers reading the emitted SBOM MUST be able to assign different trust weights to scanner-observed vs developer-declared components; the value-set extension provides that signal. This is a value-set extension (not a new annotation key), preserving backwards compatibility for consumers reading source-tier today. Driven by milestone 119 (FR-011 / SC-004). |
| C66 | `mikebom:supplement-cdx` | envelope-level — `metadata.properties[]` entry — string. Value: `<path>@sha256:<hex>` where `<path>` is the VERBATIM string the operator passed to `--supplement-cdx` (Decision 6 — not absolutized) and `<hex>` is the lowercase 64-char SHA-256 over the supplement file's raw bytes. Emitted ONLY when `--supplement-cdx <PATH>` was supplied; absent (along with byte-identical pre-feature output) when no supplement was in effect. Per Decision 3 — emitted EVEN WHEN the supplement file is empty (zero components/services/dependencies) so consumers can see that a supplement was supplied regardless of whether it had any effect. | envelope-level — `creationInfo.annotations[]` entry — annotator=`Tool: mikebom-<version>`, annotationType=`OTHER`, comment=`mikebom:supplement-cdx=<path>@sha256:<hex>`. Emission gating identical to CDX. | envelope-level — `Annotation` element on the `SpdxDocument` — annotationType=`other`, subject=`SpdxDocument.spdxId`, statement=`mikebom:supplement-cdx=<path>@sha256:<hex>`. Emission gating identical to CDX. | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native field expressing "this SBOM merged in an operator-supplied supplement file with hash X" exists in CDX 1.6, SPDX 2.3, or SPDX 3.0.1. CDX `metadata.tools.components[]` lists tools that contributed to the SBOM but not external supplement files (semantically wrong category — the supplement is input data, not a tool). SPDX 2.3 `creationInfo.creatorComment` is free-form prose, structurally inadequate. SPDX 3.0.1's typed-tool model doesn't cover operator-supplied input files. **Per Constitution Principle X (Transparency)**: operators reading the emitted SBOM MUST be able to detect that a supplement was used (the merge changes which components appear and how conflicts resolved); the annotation provides this signal in a machine-parseable form, with SHA-256 making tampering detectable at audit time. Pattern parallels C63 `mikebom:exclude-path` (milestone 113 — operator-supplied scan parameter emitted at envelope level). Driven by milestone 119 (FR-012 / SC-004). Closes issue #326. |
| C67 | `mikebom:assertion-conflict` | per-component `properties[]` entry — JSON-encoded string carrying an array of conflict-record objects. Repeatable conflicts on one component accumulate into the same array property (storage shape: BTreeMap value is a `serde_json::Value::Array`). Each record object: `{field, scanner_value, supplement_value, winner, justification}`. `field` ∈ honored CDX field names (`licenses`, `hashes`, `cpe`, `version`, `supplier`, `copyright`, `name`, `description`, `externalReferences`, …). `winner` ∈ `{"scanner", "supplement"}`. `justification` ∈ the minimal 2-value enum per spec clarification Q3: `{"bytes-evident-detection-preserved", "developer-metadata-override"}`. Emitted ONLY on components where a supplement-declared value contradicted a scanner-discovered value on a honored field; absent on all other components. | Annotation `mikebom:assertion-conflict` on Package (`MikebomAnnotationCommentV1` envelope, value = JSON-encoded array). | Annotation `mikebom:assertion-conflict` on Package (graph-element Annotation targeting the component, same JSON-array shape as SPDX 2.3). | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native field expressing "scanner observed X but operator declared Y; here's who won and why" exists in CDX 1.6, SPDX 2.3, or SPDX 3.0.1. CDX `evidence.identity` carries identification confidence + method, not conflict-resolution audit trails. SPDX 2.3 `Annotation` is free-form prose, not structured records. SPDX 3.0.1's evidence-profile model would express this via a future `conflictResolution` profile that doesn't exist in 3.0.1 stable. **Per Constitution Principle X (Transparency)**: every conflict-resolution decision MUST be auditable; the structured machine-parseable record lets consumers reconstruct "what did the operator declare; what did the scanner find; where did they conflict" without out-of-band metadata. The hard/soft partition (scanner wins on bytes-derived facts per FR-006; developer wins on metadata per FR-007) is the SOURCE of `winner` AND `justification` — the two values are mechanically derived from the field per data-model invariant 1, so consumers can re-validate the partition by re-running the logic. Pattern parallels C64 `mikebom:produces-binaries` (milestone 116 — JSON-encoded array as `properties[]` entry value). Driven by milestone 119 (FR-008 / FR-009 / SC-003). |
| C68 | `mikebom:kmp-source-set` | per-component `properties[]` entry — JSON-encoded string carrying an array of Kotlin Multiplatform source-set names (lex-sorted, deduped). Each source-set name reflects the originating `kotlin { sourceSets { <name> { dependencies { ... } } } }` block in `build.gradle.kts`. Common values: `commonMain`, `commonTest`, `jvmMain`, `androidMain`, `iosMain`, `iosX64Main`, `iosArm64Main`, `macosMain`, `linuxX64Main`, etc. The reader does NOT validate names against a hard list — operators may declare custom source-sets via `sourceSets.create("myCustomSet")` and the annotation reflects whatever name they used. Emitted ONLY on components discovered from a `kotlin { sourceSets { ... } }` block; absent on non-KMP components AND on components from top-level `dependencies { ... }` blocks (Android default; no KMP). | Annotation `mikebom:kmp-source-set` on Package (`MikebomAnnotationCommentV1` envelope, value = JSON-encoded array). | Annotation `mikebom:kmp-source-set` on Package (graph-element Annotation targeting the component, same JSON-array shape as SPDX 2.3). | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native field expressing "this dependency was declared in a specific Kotlin Multiplatform source-set" exists in CDX 1.6, SPDX 2.3, or SPDX 3.0.1. CDX 1.6's `evidence.identity[].methods[]` carries identification methods, not source-set provenance. SPDX 2.3's `Package.primaryPackagePurpose` is a category taxonomy, not a build-target provenance marker. SPDX 3.0.1's evidence-profile model would express this via a future `kotlinMultiplatformSourceSet` profile that doesn't exist in 3.0.1 stable. **Per Constitution Principle X (Transparency)**: consumers filtering an SBOM to a single target (Android-only, iOS-only) MUST know which source-set declared each dep; the annotation provides this signal in a machine-parseable form. Per the FR-006 timing contract, the kotlin_dsl reader emits one `PackageDbEntry` per `(dep × source-set)` tuple pre-dedup, each carrying the SAME merged source-set array; the milestone-105 dedup pipeline collapses them into ONE canonical component post-emission, preserving the merged annotation. Pattern parallels C64 `mikebom:produces-binaries` (milestone 116) and C67 `mikebom:assertion-conflict` (milestone 119) in storage shape (JSON-encoded array as `properties[]` value). Driven by milestone 122 (FR-006). |
| C69 | `mikebom:root-selection-heuristic` | envelope-level — `metadata.properties[]` entry — JSON-encoded string carrying a `{"heuristic": <name>, "confidence": <float>}` object inside the canonical `mikebom-annotation/v1` envelope. `<name>` ∈ `{"repo-root-main-module" (0.95), "ecosystem-priority" (0.70), "longest-common-prefix" (0.80), "maven-scan-target-coord" (0.60), "synthetic-placeholder" (0.30)}`. Emitted ONLY when the new milestone-127 ladder fired below the count==1 fast path AND the auto-pick fell through past at least one detected main-module (i.e., `losers.is_empty() == false`). Suppressed on: (a) count==1 fast path (preserves byte-identity per SC-003 on the 33 alpha.48 goldens), (b) operator override (`--root-name` etc. — the milestone-077 override audit channel is the right surface), (c) zero-main-module scans (no loss to signal). | envelope-level — `creationInfo.annotations[]` entry — annotator=`Tool: mikebom-<version>`, annotationType=`OTHER`, comment carries the same envelope shape as CDX. Emission gating identical. | envelope-level — `Annotation` element on the `SpdxDocument` — annotationType=`other`, subject=`SpdxDocument.spdxId`, statement carries the same envelope shape. Emission gating identical. | **Native-field audit per Constitution Principle V (v1.4.0)**: NO native field exists in CDX 1.6, SPDX 2.3, or SPDX 3.0.1 for surfacing the document-scope "which heuristic elected the BOM subject" signal. CDX 1.6's `evidence.identity[].confidence` is per-component-scope, not document-scope, and carries only a confidence float with no heuristic name. SPDX 2.3's `documentDescribes` and SPDX 3.0.1's `rootElement` are pure ref arrays — the spec is silent on how implementations chose them. Per Principle V's parity-bridging clause, the `mikebom:root-selection-heuristic` annotation is justified — it carries a signal no standard format models. **Per Constitution Principle X (Transparency)**: downstream consumers keying software identity on the BOM subject (vulnerability matching, license attribution, supply-chain identity tracking) MUST be able to assess how confident the auto-pick is and which heuristic produced it; the structured `(heuristic, confidence)` pair lets them programmatically gate on the auto-pick's quality. Modeled on CDX's existing component-scoped `evidence.identity.confidence` channel (numeric float in `[0.0, 1.0]`). Companion to the milestone-127 `tracing::warn!` log emission at scan-end (gated on the same `losers.is_empty() == false` condition) which surfaces the loser PURLs to operators. Driven by milestone 127 (FR-006). Closes issues #366 + #367. |
| C70 | `mikebom:srcrev` | per-component `properties[]` entry — string (full hex SHA the recipe's `SRCREV` field declared; lowercased). Emitted on Yocto recipe components per FR-003 when the recipe declares `SRCREV` alongside a git `SRC_URI`. Pair with C71 (`mikebom:src-uri`) for the full upstream-source provenance trail. | Annotation `mikebom:srcrev` on Package (envelope shape). | Annotation `mikebom:srcrev` on Package (graph-element Annotation). | **Principle V audit**: NO native CDX/SPDX field carries "commit SHA pinning the vcs ref" (CDX `externalReferences[].comment` is free-form prose, structurally inadequate). Parity-bridge justified. Driven by milestone 128 FR-003. |
| C71 | `mikebom:src-uri` | per-component `properties[]` entry — JSON-encoded array of every `SRC_URI` entry verbatim (git/file/http URIs preserved with their BitBake qualifiers). Emitted on Yocto recipe components per FR-002 when the recipe declares `SRC_URI`. Pair with C70 (`mikebom:srcrev`) for full provenance. | Annotation `mikebom:src-uri` on Package. | Annotation `mikebom:src-uri` on Package. | **Principle V audit**: CDX `externalReferences[type=vcs/distribution]` carries individual upstream URLs but NOT the recipe-declared entry set as a whole (compliance audit trail). Parity-bridge justified. Driven by milestone 128 FR-002. |
| C72 | `mikebom:srcrev-by-machine` | per-component `properties[]` entry — JSON-encoded object keyed by Yocto machine arch (e.g., `qemuarm`, `qemuarm64`). Emitted on Yocto recipe components that declare `SRCREV_<machine>` overrides per FR-003. | Annotation `mikebom:srcrev-by-machine` on Package. | Annotation `mikebom:srcrev-by-machine` on Package. | **Principle V audit**: No native field for per-machine commit pins. Parity-bridge. Milestone 128 FR-003. |
| C73 | `mikebom:yocto-layer` | per-component `properties[]` entry — string carrying the owning layer's `BBFILE_COLLECTIONS` value (e.g., `meta-balena-rust`). Emitted on EVERY Yocto recipe component AND the synthesized layer-root component per FR-006 + FR-007 (the nearest-ancestor heuristic). | Annotation `mikebom:yocto-layer` on Package. | Annotation `mikebom:yocto-layer` on Package. | **Principle V audit**: No native field models Yocto layer attribution. CDX `component.group` is namespace, not layer-source. Parity-bridge justified. Milestone 128 FR-006. |
| C74 | `mikebom:yocto-layer-version` | per-component `properties[]` entry — string carrying the layer's `LAYERVERSION_<collection>` value (e.g., `"1"`). Absent when the layer.conf omits LAYERVERSION; that case emits `mikebom:yocto-layer-version-missing: true` as a transparency annotation. | Annotation `mikebom:yocto-layer-version` on Package. | Annotation `mikebom:yocto-layer-version` on Package. | **Principle V audit**: No native field. Parity-bridge. Milestone 128 FR-006. |
| C75 | `mikebom:yocto-layer-series` | per-component `properties[]` entry — JSON-encoded array of Yocto release series the layer compatible with (e.g., `["scarthgap"]`). From `LAYERSERIES_COMPAT_<collection>`. | Annotation `mikebom:yocto-layer-series` on Package. | Annotation `mikebom:yocto-layer-series` on Package. | **Principle V audit**: No native field. Parity-bridge. Milestone 128 FR-006. |
| C76 | `mikebom:bbappend-applied` | per-component `properties[]` entry — JSON-encoded array of `.bbappend` file paths (lex-sorted, deduped, workspace-relative) that modify the recipe per FR-008. Emitted ONLY when ≥1 append matches; absent otherwise. | Annotation `mikebom:bbappend-applied` on Package. | Annotation `mikebom:bbappend-applied` on Package. | **Principle V audit**: BitBake-specific append-customization tracking has no native equivalent. Parity-bridge. Milestone 128 FR-008. |
| C77 | `mikebom:depends-unresolved` | per-component `properties[]` entry — JSON-encoded array of `DEPENDS` recipe names that did NOT resolve to a recipe in the scan. Surfaces closure gaps per FR-009 + Constitution Principle X transparency. | Annotation on Package. | Annotation on Package. | **Principle V audit**: SPDX/CDX express CONFIRMED relationships; no native field surfaces "we knew about a dep but couldn't resolve it." Parity-bridge. Milestone 128 FR-009. |
| C78 | `mikebom:rdepends-unresolved` | per-component `properties[]` entry — JSON-encoded array of RDEPENDS package names that did NOT resolve. Same shape as C77 but for runtime deps. | Annotation on Package. | Annotation on Package. | **Principle V audit**: Same as C77 for runtime deps. Parity-bridge. Milestone 128 FR-009. |
| C79 | `mikebom:yocto-unexpanded-vars` | per-component `properties[]` entry — JSON-encoded array of `${VAR}` references the reader could NOT resolve (only `${PN}` and `${PV}` are resolved per FR-005). Emitted when ≥1 identity-field references an unexpandable var; transparency signal per Constitution X. | Annotation on Package. | Annotation on Package. | **Principle V audit**: BitBake-specific approximation transparency; no native field. Parity-bridge. Milestone 128 FR-005. |
| C80 | `mikebom:yocto-license-closed` | per-component `properties[]` entry — boolean `true` when the recipe's `LICENSE` field is exactly `"CLOSED"` (BitBake's proprietary marker). Emitted alongside `NOASSERTION` license expression so consumers can distinguish "no license declared" from "explicitly proprietary." | Annotation on Package. | Annotation on Package. | **Principle V audit**: CDX/SPDX have `NOASSERTION` but no discriminator for proprietary-by-declaration. Parity-bridge. Milestone 128 FR-012. |
| C81 | `mikebom:yocto-description` | per-component `properties[]` entry — string carrying the recipe's `DESCRIPTION` field when it differs from `SUMMARY`. The `SUMMARY` field maps to the native `component.description` slot per FR-010; this annotation preserves the longer-form description only when it adds information. | Annotation on Package. | Annotation on Package. | **Principle V audit**: SPDX `Package.description` exists but emission gating differs; the annotation makes the gating mechanically auditable. Parity-bridge for the gating signal. Milestone 128 FR-010. |
| C82 | `mikebom:src-uri-local-only` | per-component `properties[]` entry — boolean `true` when ALL `SRC_URI` entries are `file://` (recipe builds entirely from in-tree files; no external upstream). Per FR-002 AC#4. | Annotation on Package. | Annotation on Package. | **Principle V audit**: No native field for "no external upstream." Parity-bridge. Milestone 128 FR-002. |
| C83 | `mikebom:yocto-class-extend` | per-component `properties[]` entry — JSON-encoded array carrying BBCLASSEXTEND flavors (e.g., `["native", "nativesdk"]`). | Annotation on Package. | Annotation on Package. | **Principle V audit**: BBCLASSEXTEND flavor signal has no native equivalent. Parity-bridge. Milestone 128. |
| C84 | `mikebom:yocto-overrides-merged` | per-component `properties[]` entry — boolean `true` when ≥1 override-syntax merge (`FIELD:append:<override>` etc.) fired during parsing. Transparency signal for the FR-016 union-merge approximation. | Annotation on Package. | Annotation on Package. | **Principle V audit**: No native field for "we approximated this." Parity-bridge per Principle X. Milestone 128 FR-016. |
| C85 | `mikebom:yocto-recipe-name` | per-component `properties[]` entry — string carrying the recipe's filename-derived name. Emitted ONLY when FR-002a's host-typed PURL path fires (`pkg:github/<owner>/<repo>@<srcrev>`), in which case the component's PURL identifies the upstream artifact and this annotation preserves the recipe-identity provenance. | Annotation on Package. | Annotation on Package. | **Principle V audit**: When PURL points at upstream (e.g., `pkg:github/<o>/<r>`), the recipe-identity info has no native carrier. Parity-bridge. Milestone 128 FR-002a. |
| C86 | `mikebom:yocto-recipe-version` | per-component `properties[]` entry — string carrying the recipe's filename-derived version. Emitted ONLY alongside C85 when FR-002a's host-typed PURL fires. | Annotation on Package. | Annotation on Package. | **Principle V audit**: Same rationale as C85. Parity-bridge. Milestone 128 FR-002a. |
| C87 | `mikebom:assembly-version-informational-stripped` | per-component `properties[]` entry — string carrying the existing `mikebom:assembly-version-informational` value with the SemVer §10 build-metadata suffix (everything from the FIRST `+`) removed. Companion to the verbatim Informational annotation; NOT a replacement. Emitted ONLY when the source value contains `+` (FR-009 — no semantic content otherwise) AND the stripped prefix passes the milestone-131 `is_plausible_version_string` sanity filter (FR-010 — silent skip on failure). Surfaces the syft-parity canonical form so consumers comparing mikebom against syft's `pkg:nuget` versions can key on either representation without post-processing. | Annotation on Package, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:assembly-version-informational-stripped","value":"<stripped>"}`. | Annotation on Package, same envelope shape. | **Principle V audit**: CDX 1.6 `components[].version`, SPDX 2.3 `packages[].versionInfo`, and SPDX 3 `software:version` are single canonical-version slots — no native field expresses an "alternate canonical representation" alongside a primary version. Parity-bridge for syft-comparators. Milestone 132 US2 FR-008.1. |
| C88 | `mikebom:layer-digest` | per-component `properties[]` entry — string carrying `sha256:<hex>` (the SHA-256 of the OCI layer blob's compressed bytes — matches trivy's `LayerDigest` semantic, NOT the uncompressed `DiffID`). Emitted ONLY for image scans (`--image`); non-image scans omit. When the same path is written by multiple layers, the LAST layer in the `manifest.json` `Layers[]` array wins (OCI overlay semantics — later layers shadow earlier writes; consumers asking "which layer introduced this content?" want the latest writer because earlier writes are no longer at-rest). The path lookup uses the component's `evidence.source_file_paths[0]` (rootfs-relative after PR US2.1 normalization) against the path → layer-digest map built by `scan_fs::docker_image::extract`. | Annotation on Package, same `sha256:<hex>` value carried via `MikebomAnnotationCommentV1` envelope. | Annotation on Package element, same value. | **Principle V audit**: no CDX / SPDX 2.3 / SPDX 3 construct for "OCI layer digest containing this component's source path". CDX `evidence.occurrences[].location` carries the path but no layer-digest sub-property. Parity-bridge that closes the trivy-feature-parity gap for image diff / forensics / "which layer added this content" queries. Milestone 133 US2.2 FR-013. |
| C91 | `mikebom:component-tier` | per-component `properties[]` entry — string carrying `"file"` for orphan-walker-emitted file-tier components. Marker for downstream consumers: file-tier components have no PURL (FR-009) and identify content via SHA-256 + path list rather than registry coordinates. Emitted ONLY when `--file-inventory=orphan` (default since milestone 133 US1.C) or `--file-inventory=full` runs and the file survives the FR-005 content-shape allowlist + FR-011 hybrid dedupe. | Annotation on Package, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:component-tier","value":"file"}`. The Package itself emits with `filesAnalyzed: false` and no `externalRefs[purl]`. | Annotation on `software_File` element, same envelope shape. The element type swap (`software_File` vs `software_Package`) is the primary tier signal; the annotation is the cross-format-symmetric carrier. | **Principle V audit**: CDX `component.type = "file"` IS the native tier signal in CDX 1.6 (parity-bridge unnecessary on the CDX side beyond the type field itself — the annotation rides through extra_annotations for symmetric emission across all three formats so consumers reading the SPDX paths see the same marker). SPDX 2.3 has no native "file-tier component" enum value on Package — `filesAnalyzed: false` is the closest signal but doesn't distinguish file-tier from "fast-path OS package no-deep-hash" components. Parity-bridge. Milestone 133 US1.C FR-002. |
| C92 | `mikebom:file-paths` | per-component `properties[]` entry — string carrying a JSON-encoded array of every rootfs-relative path (sorted lex-ascending per FR-007, no leading `/`) where the component's SHA-256 content was observed. Capped at 100 entries per FR-007; when the cap fires the companion `mikebom:file-paths-truncated = "true"` annotation also emits. Emitted ONLY on file-tier components (those carrying C91 `mikebom:component-tier = "file"`). | Annotation on Package, JSON-encoded array string via the standard envelope. | Annotation on `software_File`, same shape. | **Principle V audit**: CDX `evidence.occurrences[].location` exists for path coverage on regular components but file-tier components don't carry `evidence.occurrences[]` (the SHA-256 + the file-paths annotation IS the identity; there's no manifest-vs-deployed distinction to express). SPDX 2.3 / SPDX 3 file-element shapes likewise don't natively carry "every path where this hash was observed". Parity-bridge. Milestone 133 US1.C FR-007. |
| C93 | `mikebom:file-inventory-skipped-oversize` | document-scope `metadata.properties[]` entry — decimal string carrying the count of files the file-tier walker skipped because their size exceeded `--file-inventory-size-limit` (default 100 MB per FR-010). Emitted ONLY when the walker ran (`--file-inventory=orphan` or `=full`) AND the counter is non-zero. | Document-scope annotation (`bom.annotations[]`) via `MikebomAnnotationCommentV1` envelope. | Document-scope `Annotation` element with `subject` pointing at the `SpdxDocument` element. | **Principle V audit**: no CDX / SPDX 2.3 / SPDX 3 native field for "count of files the file-tier walker skipped due to size". Principle X transparency: operators get a single document-level number proving the walker ran and what it skipped. Parity-bridge. Milestone 133 US3 FR-010. |
| C94 | `mikebom:file-inventory-skipped-special-files` | document-scope counter — decimal string of files the walker skipped because they were special files (devices, sockets, FIFOs, char/block specials). Emitted gating same as C93. | Document-scope annotation, same envelope. | Document-scope Annotation element. | **Principle V audit**: no native field for "count of special files skipped during file-tier walk". Principle X transparency. Parity-bridge. Milestone 133 US3. |
| C95 | `mikebom:file-inventory-unreadable` | document-scope counter — decimal string of files the walker couldn't open or read (permissions, missing, mid-flight delete). Emitted gating same as C93. | Document-scope annotation. | Document-scope Annotation. | **Principle V audit**: no native field. Principle X transparency — flags I/O issues that prevented complete inventory. Parity-bridge. Milestone 133 US3. |
| C96 | `mikebom:file-paths-truncated` | per-component `properties[]` entry — string `"true"` carrying the flag that the component's `mikebom:file-paths` array hit the 100-entry FR-007 cap and some paths were dropped from the wire shape. Emitted ONLY on file-tier components when the truncation fired. | Annotation on Package. | Annotation on `software_File`. | **Principle V audit**: native fields don't carry "this list-valued field was truncated at emission". Companion to C92 — keeps the cap behavior auditable. Parity-bridge. Milestone 133 US3 FR-007. |
| C97 | `mikebom:file-inventory-mode` | document-scope `metadata.properties[]` entry — string carrying `"full"` ONLY when the operator opted into `--file-inventory=full` (the explicit FR-011 hybrid-dedupe bypass). The default `orphan` mode and the byte-identity-preserving `off` mode do NOT emit this marker. Per Constitution Strict Boundary §5 (added in 1.5.0), full-mode SBOMs MUST carry this marker so consumers can detect at parse time that the file-tier set may duplicate package/binary tier coverage. | Document-scope annotation via `MikebomAnnotationCommentV1` envelope. | Document-scope `Annotation` element with `subject` pointing at the `SpdxDocument` element. | **Principle V audit**: no CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field for "consumer-visible override marker indicating that file-tier emission bypassed dedupe". Required by Strict Boundary §5 as the auditable signal of the override. Parity-bridge. Milestone 133 US4 FR-019 (a). |
| C98 | `mikebom:license-concluded-source` | per-component `properties[]` entry — string identifying WHERE the component's `licenseConcluded` value came from. Initial value (this PR): `"operator-asserted"` — set when the operator passed `--conclude-licenses`, formally asserting they have reviewed the declared licenses and accept them as the analyst-verified conclusion. Future enrichment work may add `"clearly-defined"`, `"deps-dev"`, etc. for parallel provenance. Emitted ONLY on components whose `licenseConcluded` was populated by this mechanism; pre-existing `NOASSERTION` conclusions stay unannotated. | Annotation on Package, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:license-concluded-source","value":"operator-asserted"}`. | Annotation on Package, same envelope shape. | **Principle V audit**: CDX 1.6 `component.licenses[].acknowledgement` enum is `"declared"` / `"concluded"` — it identifies which TYPE of license the entry expresses, not the SOURCE of the conclusion. SPDX 2.3 `licenseConcluded` is a single scalar with no provenance carrier. SPDX 3.0.1 `hasConcludedLicense` Relationship has no native provenance field. The annotation lets downstream consumers distinguish operator-asserted conclusions (which carry a human-review claim per the `--conclude-licenses` flag's operator-assertion contract) from external-enrichment-derived conclusions (ClearlyDefined, deps.dev). Parity-bridge. Issue #363. |
| C99 | `mikebom:duplicate-purl-divergent` | per-component `properties[]` entry on the deduped root component for every detected divergent same-PURL collision — string carrying the JSON-encoded `DivergenceRecord` envelope `{v, purl, reason, paths[], dep_sets_by_path?, hashes_by_path?}` per `specs/134-divergent-purl-detection/contracts/per-component-property.md`. `reason` is one of `deps-differ` / `hashes-differ` / `both`. Emitted ONLY on components whose Cargo.toml manifests collide on `(name, version)` AND have divergent declared dep sets (always checked) or divergent deep-hashes (under `--deep-hash` only). Cargo-only this milestone; detection logic is structured for ecosystem-agnostic reuse. | Annotation on Package, `MikebomAnnotationCommentV1` envelope wrapping the `DivergenceRecord` as the `value`. | Annotation on `software_Package` element, same envelope shape. | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "same identity, divergent content" semantics. CDX `evidence.occurrences[]` describes WHERE a component was found (presence-only) but not that the occurrences disagree on content. SPDX `VARIANT_OF` / `COPY_OF` relationships are between two DIFFERENT PURLs — semantic mismatch with within-PURL divergence. `sourceInfo` is unstructured prose that fails SC-005's machine-readability requirement. See `specs/134-divergent-purl-detection/research.md` R1 for the full audit. Parity-bridge. Issue #125. |
| C100 | `mikebom:purl-collisions-detected` | document-scope `metadata.properties[]` entry — string carrying the JSON-encoded `CollisionsSummary` envelope `{v, collisions[]}` aggregating every `DivergenceRecord` detected in the scan. Sort order of `collisions[]`: lex by `record.purl`. Emitted ONLY when at least one divergent collision was detected; absent on clean scans (FR-009 byte-identity invariant). Per `specs/134-divergent-purl-detection/contracts/document-scope-annotation.md`, the document-scope summary is strictly redundant with the per-component C99 properties — consumers reading either surface get the same set of records. | Document-scope annotation (`bom.annotations[]`) via `MikebomAnnotationCommentV1` envelope. | Document-scope `Annotation` element with `subject` pointing at the `SpdxDocument` element, same envelope shape. | **KEEP-NO-NATIVE**. Same justification as C99 — no native field expresses scan-wide aggregation of same-PURL divergent collisions. The document-scope surface answers "what's every collision in the scan?" via one `jq` query without walking every component. See `specs/134-divergent-purl-detection/research.md` R1. Parity-bridge. Issue #125. |
| C101 | `mikebom:peer-edge-targets` | per-component `properties[]` entry on the source component that owns ≥1 peer-driven DEPENDS_ON edge — string carrying the JSON-encoded array of PURL strings naming the peer-driven targets. Sort order: alphabetical (lex-ascending) per the milestone-145 `mikebom:file-paths` `BTreeSet` collection precedent. Emitted ONLY when the npm `parse_package_lock` reader (milestone 147) actually emits ≥1 peer-driven edge — components whose `peerDependencies` were unmet (FR-002) or whose peers were also declared in a regular section (FR-003 — regular wins) get NO annotation. Operators wanting to reconstruct the pre-147 install-only edge view can subtract this set from each component's `dependsOn`. | Annotation on Package, `MikebomAnnotationCommentV1` envelope wrapping the PURL-array as the `value` (native JSON array shape per milestone-145 envelope-shape fix). | Annotation on `software_Package` element, same envelope shape (native array). | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "this DEPENDS_ON edge was driven by a peer-declaration rather than a regular `dependencies`/`devDependencies`/`optionalDependencies` declaration." CDX 1.6 `dependencies[].dependsOn[]` is `Array<bom-ref-string>` with NO per-element metadata slot — there's no carrier to tag individual edges with "this one is peer-driven." SPDX 2.3 typed-relationship enum is `{DEPENDS_ON, DEV_DEPENDENCY_OF, BUILD_DEPENDENCY_OF, TEST_DEPENDENCY_OF, RUNTIME_DEPENDENCY_OF}` with NO `PEER_DEPENDENCY_OF` variant. SPDX 3 `LifecycleScopedRelationship.scope` enum is `{development, build, test, runtime, design, other}` with NO `peer` value. Parity-bridge that preserves the install-vs-functional discriminator for downstream consumers (vulnerability scanners, license tooling) that want to filter the dep graph by edge kind. Milestone 147 (closes Trivy-comparison orphan gap on the looker-frontend npm lockfile: 5 orphans → 0). |
| C102 | `mikebom:demoted-from-main-module` | per-component `properties[]` entry on a `library`-typed component in `components[]` — boolean-string `"true"` marking the entry as the manifest-derived main-module that was preserved (rather than dropped per the milestone-077 clean-replacement default) when an operator passed `--root-name` / `--root-version` / `--root-purl` together with the new `--preserve-manifest-main-module` opt-in flag. The demoted entry retains its original ecosystem-derived PURL + name + version + license + hashes; the only differences vs the pre-override main-module are (a) `component.type` changes from `application` → `library`, (b) this annotation is added, (c) the entry appears in `components[]` instead of at `metadata.component`. Per US1 clarification Option A (recorded 2026-06-29), the demoted entry has NO outbound `dependsOn` edges in the wire output — its dep-graph topology is re-anchored on the operator-override root via milestone-084 logic. | Annotation on Package, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:demoted-from-main-module","value":"true"}`. | Annotation element. **Subject-routing divergence vs CDX + SPDX 2.3**: the SPDX 3 emitter's `package_iri_by_purl` aliasing at `v3_document.rs:318-324` rewrites the demoted entry's PURL → synth-root IRI to serve milestone-084 relationship re-anchoring (US1 Option A). The demote annotation rides with the alias and ends up emitted with `subject = synth_root_iri` (e.g., the operator-override `widget-svc` element) rather than `subject = demoted_entry_iri`. The annotation VALUE remains byte-identical across all three formats per FR-009; only the SPDX 3 SUBJECT differs. Consumers querying by annotation `field` key (rather than by subject IRI) find the annotation regardless. A future milestone could split `package_iri_by_purl` into separate annotation-routing vs relationship-routing maps to align subject behavior; deferred per the milestone-149 scope. | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "this library was demoted from main-module by an override flag." Six rejected alternatives audited at spec-time: (1) CDX `component.type` enum — `library` describes the role but not the demote provenance (a library that was always a library and a library demoted from main-module are observationally identical on `type` alone); (2) CDX `component.scope` enum — lifecycle-scope axis, orthogonal to demote-provenance; (3) SPDX 2.3 `Package.primaryPackagePurpose` enum — same shape as CDX `component.type`, describes role not provenance; (4) SPDX 2.3 `Relationship[relationshipType]` enum — `VARIANT_OF` is closest but its semantic is about the SAME COMPONENT with variations (architecture, language), not about a role demote; (5) SPDX 3 `software_softwarePurpose` enum — same as SPDX 2.3 `primaryPackagePurpose`; (6) SPDX 3 `LifecycleScopedRelationship.scope` enum — lifecycle-scope axis, orthogonal. Parity-bridge that lets consumers identify demoted entries vs natural library deps without parsing the override context. Milestone 149 (closes issue #151). |
| C103 | `mikebom:cmake-find-package-name` | per-component `properties[]` entry — string preserving the original casing of the package name as it appeared in the CMake source's `find_package(<Name> ...)` call. Emitted ONLY when the original casing differs from the lowercased PURL name segment (e.g., `find_package(OpenSSL 1.1.0)` → PURL `pkg:generic/openssl@1.1.0` + property value `"OpenSSL"`; `find_package(zlib 1.2.11)` → PURL `pkg:generic/zlib@1.2.11` + NO annotation). Emitted only by the `cmake-find-package` mechanism; NOT emitted for `cmake-pkg-check-modules` (pkg-config module names are conventionally already lowercase-with-hyphens). | Annotation `mikebom:cmake-find-package-name` on Package, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:cmake-find-package-name","value":"OpenSSL"}`. | Annotation element with the same envelope; subject routing follows the standard SPDX 3 package-IRI pattern. | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field carries "original casing of a name we normalized." Rejected alternatives: (1) CDX `component.name` — mikebom's `component.name` IS the lowercased normalized form (matches PURL name segment for consistency); using it for original casing would break PURL/name correspondence; (2) CDX `component.group` — namespace/organization axis, orthogonal to casing; (3) SPDX 2.3 `Package.name` — same shape as CDX; (4) SPDX 2.3 `PackageComment` — free-text, but consumers would parse a natural-language string vs an explicit annotation; (5) SPDX 3 `Package.name` — same shape as SPDX 2.3; (6) SPDX 3 `NamespaceMap.namespace` — package-import prefix, orthogonal. Parity-bridge letting compliance auditors pattern-match against the exact CMake declaration form. Conditional emission (only when casing differs) keeps SBOMs of all-lowercase-CMake-projects uncluttered. Milestone 155 (FR-008). |
| C104 | `mikebom:graph-completeness` | document-scope property/annotation — three-value string `complete` \| `partial` \| `unknown` per FR-006. Always emitted per FR-003 (universal presence). `complete` iff BFS from the multi-root seed set reaches 100% of emitted components AND no gap class fired. `partial` iff a gap was detected AND classified into a documented reason code (see C105). `unknown` in all other cases (caution-first fallback per Q1 clarification 2026-07-03 — prefer `unknown` over guessing). Milestone 158 (closes #492). | Document-level `Annotation` on `SPDXRef-DOCUMENT`, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:graph-completeness","value":"complete"}`. | Document-scope `Annotation` element on the SpdxDocument root IRI; same envelope shape. | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "how confident is mikebom that the emitted dependency graph is complete." Rejected alternatives: (1) CDX `metadata.compositions[]` with `aggregate: incomplete` — the CDX composition model tracks per-component completeness of the composition itself (`complete`, `incomplete`, `incomplete_first_party_only`, `unknown`), not overall graph-reachability from the declared root; using it would conflate two orthogonal completeness axes; (2) SPDX 2.3 `Package.filesAnalyzed` — file-level completeness, orthogonal to dep-graph completeness; (3) SPDX 3 `LifecycleScopedRelationship.scope` — lifecycle-scope axis, orthogonal. Standards-native precedence per Constitution Principle V explicitly acknowledged: FR-010 of milestone-158 spec codifies the migration path if either standard adopts an official graph-completeness enum. |
| C105 | `mikebom:graph-completeness-reason` | document-scope property/annotation — human-readable reason string present iff C104 value != `complete` AND the gap could be classified into the closed 8-code vocabulary. Grammar per spec.md contracts/graph-completeness-vocabulary.md: `<code>: <detail>[; <code>: <detail>]*`. Codes: `workspace-peer-detection-degraded`, `root-selection-ambiguous`, `root-selection-failed`, `edge-resolution-degraded`, `go-transitive-coverage-degraded`, `go-workspace-mode-anomaly`, `orphaned-components-detected`, `multi-ecosystem-partial-root`. Multiple codes joined by `; ` (semicolon + space). Milestone 158. | Document-level `Annotation` on `SPDXRef-DOCUMENT`, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:graph-completeness-reason","value":"orphaned-components-detected: 3 component(s) not reachable from root"}`. | Document-scope `Annotation` element on the SpdxDocument root IRI; same envelope shape. | **KEEP-NO-NATIVE**. Companion to C104; same rejection audit applies. |
| C106 | `mikebom:pnpm-alias` | per-component property/annotation — bare string value equal to the LOCAL-NAME as it appeared in the pnpm-lock.yaml `dependencies:`/`peerDependencies:`/`optionalDependencies:` sub-mapping when the local-name aliases to a different real package (e.g. `react-helmet-async: '@slorber/react-helmet-async@1.3.0(...)'` produces `mikebom:pnpm-alias = "react-helmet-async"` on the emitted `pkg:npm/%40slorber/react-helmet-async@1.3.0` component). Local-name value byte-identical to lockfile source (no URL-encoding, no case normalization). Multi-alias case (same canonical component reached via 2+ distinct local-names) emits a `Value::Array` of local-names (FR-012). Milestone 159 (closes #493). | Annotation on Package, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:pnpm-alias","value":"react-helmet-async"}`. | Annotation element with same envelope. | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "this package identity was reached via a lockfile-declared alias." Rejected alternatives: (1) CDX `component.description` — free-text, opaque to parsers; (2) CDX `component.properties[]` with a different property key — this IS such a property; (3) SPDX 2.3 `Package.homepage` / `Package.summary` — semantic mismatch; (4) SPDX 3 `Element.description` — free-text. Standards-native precedence per Constitution Principle V explicitly acknowledged: FR-013 of milestone-159 spec codifies migration if either standard adopts a package-alias enum. |
| C107 | `mikebom:yarn-alias` | per-component property/annotation — bare string value equal to the LOCAL-NAME as it appeared in the yarn.lock v1 key spec (e.g. `"@cosmograph/cosmos@^1.1.1", "@cosmograph/cosmos@npm:@cosmos.gl/graph":` produces `mikebom:yarn-alias = "@cosmograph/cosmos"` on the emitted `pkg:npm/%40cosmos.gl/graph@2.6.4` component). Byte-identical to lockfile source. Multi-alias case emits `Value::Array` per FR-012. Milestone 159. | Annotation on Package with the same envelope. | Annotation element with same envelope. | **KEEP-NO-NATIVE**. Companion to C106; same rejection audit applies. |
| C108 | `mikebom:go-transitive-source` | per-component property/annotation — bare string value naming which of the 5-step ladder resolved the module's transitive requires: `go-mod-graph` (step 1), `module-cache` (step 2), `proxy-fetch` (step 3), `go-sum-fallback` (step 5, milestone 091), or `unresolved` (step 6 — no source produced edges). Universal-emit per Q2: every `pkg:golang/...` component EXCEPT the synthetic `pkg:golang/stdlib@vX.Y.Z` carries this. Milestone 160 (closes #494). | Annotation on Package, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:go-transitive-source","value":"proxy-fetch"}`. | Annotation element with same envelope. | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "which resolution mechanism produced this component's transitive edges." Rejected alternatives: (1) CDX `component.evidence.identity[]` — evidence-model tracks identity confidence, not resolution-source provenance; (2) SPDX 2.3 `Package.originator` — supplier metadata, semantic mismatch; (3) SPDX 3 `Element.originatedBy` — same rejection. Standards-native precedence per Constitution Principle V explicitly acknowledged: FR-008 of milestone-160 spec codifies migration if either standard adopts a resolution-provenance enum. |
| C109 | `mikebom:go-transitive-unresolved-reason` | per-component property/annotation — bare kebab-case string naming why a step-6 (`unresolved`) module got there. Present iff C108 == `unresolved`. Closed 7-code vocab per FR-003: `proxy-fetch-timeout`, `proxy-fetch-not-found`, `proxy-fetch-forbidden`, `proxy-off-in-chain`, `goprivate-matched`, `module-cache-miss`, `unknown-error`. Milestone 160 (closes #494). | Annotation on Package with same envelope: `{"type":"mikebom:go-transitive-unresolved-reason","value":"proxy-fetch-not-found"}`. | Annotation element with same envelope. | **KEEP-NO-NATIVE**. Companion to C108; same rejection audit applies. |
| C110 | `mikebom:go-transitive-coverage` | document-scope property/annotation — three-value string `complete` \| `partial` \| `unknown` per FR-004. Distinct from C104 (which reports whether we built a top-level graph at all): C110 reports what fraction of Go modules had per-module TRANSITIVE requires resolved via the milestone-055/091 ladder. Q1 reason-code-driven: `unknown` iff a "we can't measure" reason applies (offline, `off` in GOPROXY, `go mod graph` degraded); `partial` iff we ran but ≥1 module ended `unresolved`; `complete` iff every module resolved via steps 1–4. Emitted iff SBOM has ≥1 Go component. Milestone 160 (closes #494). | Document-level `Annotation` on `SPDXRef-DOCUMENT`, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:go-transitive-coverage","value":"partial"}`. | Document-scope `Annotation` element on the SpdxDocument root IRI; same envelope shape. | **KEEP-NO-NATIVE**. C104's rejection audit re-applies: no CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "what fraction of a specific ecosystem's modules had transitive requires resolved." R1 in milestone-160 research locks the distinction from C104. Standards-native precedence per Constitution Principle V explicitly acknowledged: FR-008 of milestone-160 spec codifies migration if either standard adopts an ecosystem-coverage enum. |
| C111 | `mikebom:go-transitive-coverage-reason` | document-scope property/annotation — human-readable reason string present iff C110 value != `complete`. Grammar per contracts/annotations.md C111: `<code>: <detail>[; <code>: <detail>]*`. Closed-but-extensible 5-code vocab per Q4: `proxy-fetch-degraded`, `offline-mode`, `goproxy-off-in-chain`, `go-mod-graph-degraded`, `module-cache-empty-and-no-proxy`. Milestone 160. | Document-level `Annotation` on `SPDXRef-DOCUMENT`, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:go-transitive-coverage-reason","value":"offline-mode: transitive edges from proxy fetches unavailable"}`. | Document-scope `Annotation` element on the SpdxDocument root IRI; same envelope shape. | **KEEP-NO-NATIVE**. Companion to C110; same rejection audit applies. |
| C112 | `mikebom:go-workspace-mode` | document-scope property/annotation — three-value string with grammar `detected: <N> use-modules` \| `absent` \| `malformed: <reason>` per FR-004. Distinct from C104/C110 per research.md R1: C104 reports whether we built a top-level graph; C110 reports Go transitive-edge coverage; C112 reports whether the scanned Go repo used `go.work` workspace mode and how many `use`d modules were discovered. Emitted iff a `go.work` file is present at the scanned root AND `GOWORK=off` is NOT set (byte-identity guard per SC-003). Q2: empty-use case emits `detected: 0 use-modules` (not `malformed:`) — legitimate scaffolding scenario. Malformed reason vocab is closed-but-extensible 6-code: `missing-use-close-paren`, `invalid-use-path`, `duplicate-use-path`, `unknown-directive`, `invalid-replace-syntax`, `io-error: <detail>`. Milestone 161 (closes #495). | Document-level `Annotation` on `SPDXRef-DOCUMENT`, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:go-workspace-mode","value":"detected: 47 use-modules"}`. | Document-scope `Annotation` element on the SpdxDocument root IRI; same envelope shape. | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "did this scan use Go workspace mode." Rejected alternatives: (1) CDX `metadata.tools[].externalReferences[]` — tool-scope, not scan-scope; (2) SPDX 2.3 `creationInfo.comment` — free-text, opaque to parsers; (3) SPDX 3 `Element.description` — same free-text mismatch. Standards-native precedence per Constitution Principle V explicitly acknowledged: FR-009 of milestone-161 spec codifies migration if either standard adopts a workspace-mode enum. |
| C113 | `mikebom:synthetic-built-in` | per-component property/annotation — bare string value naming the language runtime whose toolchain provides this gem as built-in. Closed 1-value vocab in scope for milestone 162: `"ruby"`. Emitted iff mikebom synthesized this component as a fill for a `Gemfile.lock` GEM/specs dep-target that references a Ruby toolchain-provided built-in gem (per the FR-001 allowlist union across Ruby 3.2/3.3/3.4). Per Q1: synthetic components carry versionless PURL (`pkg:gem/<name>` — no `@version` segment) so consumer vulnerability scanners doing exact-version CVE lookups don't false-positive. Milestone 162 (closes #496). | Annotation on Package, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:synthetic-built-in","value":"ruby"}`. | Annotation element on the Package IRI; same envelope. | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "this component was synthesized from a dropped edge because target is toolchain-provided." Rejected alternatives: (1) CDX `component.evidence` — evidence-model tracks confidence, not synthetic-provenance; (2) CDX `component.description` — free-text, opaque to parsers; (3) SPDX 2.3 `Package.filesAnalyzed = false` — orthogonal (file-level analysis flag). Standards-native precedence per Constitution Principle V: FR-009 codifies migration if either standard adopts a synthetic-component enum. |
| C114 | `mikebom:built-in-requirement` | per-component property/annotation — preserves the original Gemfile.lock version-constraint clause (e.g., `">= 1.2.0"`, `"~> 1.0, >= 1.0.2"`) on synthetic built-in components per FR-005. Emitted iff (a) the component is a synthetic Ruby built-in gem AND (b) at least one source spec's dep-declaration had a non-empty requirement clause. Value shape per R4: single-source case emits a bare string; multi-source case (same built-in gem referenced by multiple sources with different constraints) emits a JSON-string-encoded array of sorted, deduplicated constraint strings. Milestone 162 (closes #496). | Annotation on Package with same envelope. Multi-source: `comment = "mikebom:built-in-requirement=[\">= 1.2.0\", \">= 2.0.0\"]"`. | Annotation element with same envelope shape. | **KEEP-NO-NATIVE**. Companion to C113; same rejection audit applies. |
| C115 | `mikebom:unresolved-declared-dep` | per-component property/annotation — names dep(s) declared in a workspace-peer's `package.json` (in `dependencies:` or `devDependencies:`) that mikebom couldn't cross-resolve against the union of Tier A lockfile entries in the scan. Emitted iff (a) the component is a workspace-peer main-module (per milestone 066's main-module logic on a `package.json` inside a workspace peer directory) AND (b) at least one declared dep was `Unresolved` per the FR-004 classifier. Value shape: single unresolved dep → bare string of the dep name; multiple → JSON-string-encoded array of sorted+deduplicated dep names. Milestone 163 (closes #498). | Annotation on Package, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:unresolved-declared-dep","value":"@some/removed-package"}`. Multi: `comment = "mikebom:unresolved-declared-dep=[\"@a/pkg\",\"@b/pkg\"]"`. | Annotation element on the Package IRI; same envelope shape. | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "this component's manifest declared a dep the SBOM producer couldn't resolve." Rejected alternatives: (1) CDX `component.evidence` — evidence-model tracks confidence, not per-manifest-decl unresolved reasons; (2) SPDX `Package.externalRef` — designed for resolved external URIs, not unresolved names. Companion to Q1+Q2 unified disposition: the edge is SUPPRESSED and the source-side annotation is the auditor's signal. Standards-native precedence per Constitution Principle V: milestone-163 FR-007 codifies migration if either standard adopts a declared-but-unresolved-dep vocabulary. |
| C116 | `mikebom:dep-alternative-alternates` | per-source-component property/annotation — records the second-and-later members of a Debian/opkg `Depends: pkg-a \| pkg-b \| pkg-c` alternative list. Emitted iff the source component's Depends field contains at least one alternative-list entry. Q2 semantic per milestone 169: mikebom takes the FIRST alternative as the resolved dep edge (`pkg-a`), then records the fallback set (`["pkg-b", "pkg-c"]`) here so the SBOM preserves the operator's original expressiveness. Value shape: JSON-object keyed by the first-alt name, value is a JSON-array of fallback names. Multiple alt-lists in one Depends field co-exist under distinct keys: `{"pkg-a": ["pkg-b", "pkg-c"], "libfoo": ["libbar"]}`. Emitted by BOTH the m169 `ipk_file` (US1 archive-file) and `opkg` (US2 installed-DB) readers via the shared `control_file::parse_depends_field_with_alternatives` helper — value is byte-identical for the same Depends string across both readers. Consumers doing SCA/vulnerability-triage should scan this annotation when a first-alt CVE fires: an alternate may be installed instead (Debian-family runtime resolution is package-manager-driven and not visible from static SBOM inspection alone). Milestone 169 (closes #500 Q2 clarification). | Annotation on Package, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:dep-alternative-alternates","value":"{\"pkg-a\": [\"pkg-b\", \"pkg-c\"]}"}`. | Annotation element on the Package IRI; same envelope shape. | **KEEP-NO-NATIVE**. No CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 native field expresses "these are unchosen alternatives for a dep edge." Rejected alternatives: (1) CDX `dependencies[]` — flat dep-graph model, no first-vs-alt distinction; (2) SPDX `Relationship { type: DEPENDS_ON }` — same flat model, no ordinality; (3) emitting multiple parallel DEPENDS_ON edges (one per alternative) — would inflate the graph with edges the target rootfs will never all resolve simultaneously (an alt-list installs exactly ONE of the members). The source-side per-package annotation is the smallest lossless carrier. Standards-native precedence per Constitution Principle V: milestone-169 FR-004 codifies migration if either standard adopts an alt-list vocabulary. |
| C117 | `mikebom:go-transitive-fallback-count` | document-scope property/annotation — stringified non-negative integer counting Go modules whose FINAL resolution step was m091's `GoSumFallback` (step 5 of the 5-step ladder). Emitted iff the scan included ≥1 Go component AND the Go transitive resolver ran (identical presence gate to C110 `mikebom:go-transitive-coverage`). Per Q1 clarification (spec.md m172): value `"0"` MUST be emitted explicitly on healthy scans — consumers distinguish "no Go in scan" (annotation absent) from "Go was scanned cleanly" (annotation `"0"`) from "Go was scanned with degradation" (annotation `"N > 0"`). Value shape regex: `^0$\|^[1-9][0-9]*$` (no leading zeros, no sign, no compact form). Companion to milestone-160's `mikebom:go-transitive-coverage` (C110) and per-component `mikebom:go-transitive-source` (C108) — this doc-scope aggregate lets consumers threshold on "N modules degraded" without walking every component's annotations. **SC-005 invariant**: `[.metadata.properties[] \| select(.name == "mikebom:go-transitive-fallback-count") \| .value \| tonumber] == [.components[]?.properties[]? \| select(.name == "mikebom:go-transitive-source" and .value == "go-sum-fallback")] \| length` (structurally guaranteed since both derive from the same `ResolutionStep` values). Milestone 172. | Document-level `Annotation` on `SPDXRef-DOCUMENT`, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:go-transitive-fallback-count","value":"11"}`. | Document-scope `Annotation` element on the SpdxDocument root IRI; same envelope shape. | **KEEP-NO-NATIVE**. CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 have no native "count of transitive resolutions that degraded to flat fallback" field. Rejected alternatives: (1) CDX `component.evidence.identity[].confidence` — per-component evidence-model, not a doc-scope aggregate; requires walking every Go component instead of one metadata property. (2) SPDX `Package.filesAnalyzed` — unrelated flag (file-analysis flag on a Package, not a resolution-quality doc-scope count). (3) SPDX 3 `CreationInfo.comment` free-text — loses machine-parseability; the numeric aggregate is what CI thresholding needs. Standards-native precedence per Constitution Principle V: milestone-172 FR-004 codifies migration if either standard adopts a resolution-quality vocabulary. |
| C118 | `mikebom:go-cache-warming-mode` | document-scope property/annotation — closed-enum string with three values: `"off"` (default; no warming performed, C117 signal preserved as the operator's diagnostic), `"per-workspace"` (operator opted in via `--warm-go-cache=per-workspace`; the m173 warmer ran `go mod download` in every discovered Go workspace BEFORE the m055/m091 transitive resolver), `"offline-inhibited"` (operator requested `per-workspace` AND `--offline`; warming skipped per FR-003, annotation still emitted for auditability). Emission gate identical to C110/C117: annotation present iff the scan has ≥1 Go component. Value regex: `^(off\|per-workspace\|offline-inhibited)$`. Companion to C117 — C117 tells consumers HOW MANY modules degraded; C118 tells them WHICH mode was in effect during the scan. The tuple `(C118, C117)` is fully self-describing: `(off, 0)` = healthy scan without warming; `(off, N>0)` = degraded scan without warming (FR-004 advisory log fires); `(per-workspace, 0)` = warming succeeded, C117 is the ground truth; `(per-workspace, N>0)` = warming ran but N modules still fell to step-5 (partial GOPROXY reach); `(offline-inhibited, ...)` = warming would have run but --offline suppressed it. Milestone 173. | Document-level `Annotation` on `SPDXRef-DOCUMENT`, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:go-cache-warming-mode","value":"per-workspace"}`. | Document-scope `Annotation` element on the SpdxDocument root IRI; same envelope shape. | **KEEP-NO-NATIVE**. CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 have no native "which tool-side pre-resolver cache-warming mode was active during scan" field. Rejected alternatives: (1) CDX `metadata.tools[].externalReferences[]` — tool-metadata scope, not scan-context; can't distinguish per-scan mode. (2) SPDX 2.3 `creationInfo.comment` — free-text, opaque to consumers grepping for specific enum values. (3) SPDX 3 `CreationInfo.comment` — same free-text limitation. Standards-native precedence per Constitution Principle V: milestone-173 codifies migration if either standard adopts a resolver-preparation-mode vocabulary. |
| C119 | `mikebom:go-cache-warming-failed` | document-scope property/annotation — JSON-encoded array of `{reason, workspace}` records naming every Go workspace where cache warming (`go mod download`) failed. Field order intentional: `reason` first, `workspace` second (matches serde's struct-declaration emission order → alphabetical JSON keys for byte-identity across regenerations). Records sorted alphabetically by `workspace`. `reason` is a closed 6-value enum: `go-binary-absent` (host lacks `go` on PATH), `spawn-failed` (`Command::new("go")` failed; permission-denied or executable-format error), `timeout` (per-workspace timeout of 60s exceeded), `subcommand-failed` (`go mod download` exited non-zero — typically unreachable modules), `parse-error` (subprocess exited zero but output was un-interpretable; rare), `budget-exhausted` (overall wall-clock budget of 300s consumed before this workspace could be attempted). Emitted iff C118 is present AND at least one workspace failed — clean scans do NOT emit an empty-array C119 (byte-identity gate per FR-007). Companion to C118 — when C119 is present, C118 tells consumers what MODE the operator requested and C119 tells them WHICH workspaces broke. Milestone 173. | Document-level `Annotation` on `SPDXRef-DOCUMENT`, `MikebomAnnotationCommentV1` envelope: `{"type":"mikebom:go-cache-warming-failed","value":"[{\"reason\":\"timeout\",\"workspace\":\"cmd/foo\"},{\"reason\":\"subcommand-failed\",\"workspace\":\"cmd/bar\"}]"}`. | Document-scope `Annotation` element on the SpdxDocument root IRI; same envelope shape. | **KEEP-NO-NATIVE**. CDX 1.6 / SPDX 2.3 / SPDX 3.0.1 have no native "per-workspace resolver-preparation failure record" field. Rejected alternatives: (1) CDX `component.evidence.identity[].confidence` — per-component evidence-model can't express doc-scope aggregation of tool-side preparation failures. (2) SPDX `Package.filesAnalyzed = false` — per-Package flag with unrelated semantics (file-analysis, not warming failure). (3) SPDX 3 `CreationInfo.comment` free-text — loses machine-parseability; the array-of-records form is what CI dashboards + jq recipes need. Standards-native precedence per Constitution Principle V: milestone-173 codifies migration if either standard adopts a resolver-preparation-failure vocabulary. |
| C120 | `mikebom:workspace-member` | per-component property/annotation — JSON-encoded array of workspace root-relative paths (forward-slash separator on all platforms per FR-010) that the component belongs to. Records alphabetically sorted, deduplicated. Single-workspace components emit a 1-element array (not a bare string) for consumer-parsing uniformity (matches the C99 / C63 / C119 array-in-string precedent). Emitted for every component whose `evidence.source_file_paths` yields at least one derivable workspace root via `scan_fs::workspace_root::derive_workspace_root` (handles both root-relative filesystem paths AND `path+file://<abs>` URI forms). File-tier (m133) and any other unattributable components DO NOT get the annotation — absence is the wire-visible signal for "no workspace attribution" (per FR-002). Value regex: `^\[(\"[^\"]+\"(,\"[^\"]+\")*)?\]$`. Consumers filter with `select((.properties[]? \| select(.name == "mikebom:workspace-member") \| .value \| fromjson) \| contains(["<workspace>"]))` for per-workspace CVE triage — the P1 motivating use case from the langflow (10-workspace monorepo) audit. Milestone 176 (closes discoverability gap). | Annotation on Package, `MikebomAnnotationCommentV1` envelope: `{"schema":"mikebom-annotation/v1","field":"mikebom:workspace-member","value":"[\"src/frontend\",\"src/lfx\"]"}`. | Annotation element on the Package IRI; same envelope shape. | **KEEP-NO-NATIVE**. Standards-native audit per Constitution Principle V. Rejected alternatives: (1) CDX `component.group` — "The grouping name or identifier. This will often be a shortened, single name of the company or project that produced the component" per CDX 1.6 spec. Semantically the component's *authoring organization/project* (e.g., `com.fasterxml.jackson.core` for `jackson-core`), NOT the scan target's workspace boundary. Different concept. (2) SPDX 2.3 `Package.sourceInfo` — free-text, not machine-parseable at scale. (3) SPDX 3 `Element.namespace` — identity-URI scoping, not workspace-boundary attribution. (4) CDX `component.evidence.identity[].techniques[].value` — per-technique confidence hint, not workspace membership. Standards-native precedence per Constitution Principle V: milestone-176 FR-011 codifies migration if either standard adopts a scan-target workspace-boundary vocabulary. |
| C121 | `mikebom:workspaces-detected` | document-scope property/annotation — JSON-encoded array of all workspace root-relative paths detected in the scan, alphabetically sorted. Value equals the sorted-deduplicated UNION of every per-component C120 `mikebom:workspace-member` value across the emitted SBOM (FR-012 cross-annotation invariant — structurally guaranteed by construction: both derive from the same in-process workspace enumeration via the shared `generate::workspace_detected::compute` helper). Emitted iff the union is non-empty (FR-003: absent when zero workspaces detected — matches the C119 no-empty-array precedent). Single-workspace scans emit a 1-element array. Value regex identical to C120. Enables the SC-007 use case: enumerate every workspace in one jq call without walking `components[]`. Milestone 176. | Document-level `Annotation` on `SPDXRef-DOCUMENT`, `MikebomAnnotationCommentV1` envelope: `{"schema":"mikebom-annotation/v1","field":"mikebom:workspaces-detected","value":"[\".\",\"src/frontend\",\"src/lfx\"]"}`. | Document-scope `Annotation` element on the SpdxDocument root IRI; same envelope shape. | **KEEP-NO-NATIVE**. Same audit outcome as C120 — CDX / SPDX 2.3 / SPDX 3.0.1 have no native document-scope workspace-enumeration field. Rejected alternatives: (1) CDX `metadata.component.components[]` (nested composition) — structural nesting, not a flat enumeration; requires walking a tree instead of one property. Also loses "workspaces that exist" vs "workspaces that produce components" distinction. (2) SPDX `SpdxDocument.hasFile[]` — file-level enumeration, wrong grain. (3) SPDX 3 `SpdxDocument.rootElement[]` — root-element pointer scope, not workspace boundaries. Standards-native precedence per Constitution Principle V: milestone-176 FR-011 codifies migration if either standard adopts a document-scope workspace-enumeration vocabulary. |

## Section D — Evidence (CycloneDX 1.6 has native model; SPDX 2.3 does not)

| # | mikebom data | CycloneDX 1.6 location | SPDX 2.3 location | SPDX 3.0.1 location | Justification |
|---|--------------|------------------------|-------------------|---------------------|---------------|
| D1 | evidence — identity (technique + confidence) | `/components/{i}/evidence/identity[0]` | Annotation `evidence.identity` on Package, payload preserves `technique` + `confidence` | Annotation `evidence.identity` on Package, same payload shape | SPDX 2.3 has no evidence model; SPDX 3.0.1 has a typed evidence model sketched only in the evidence profile (not in 3.0.1 stable) — annotation preserves losslessly in both formats. |
| D2 | evidence — occurrences | `/components/{i}/evidence/occurrences[]` (location + SHA-256 + legacy MD5) | Annotation `evidence.occurrences` on Package, payload is the original CDX array | Annotation `evidence.occurrences` on Package, same payload shape | Same rationale as D1; deb deep-hash output relies on this. |

## Section E — Compositions / completeness markers

| # | mikebom data | CycloneDX 1.6 location | SPDX 2.3 location | SPDX 3.0.1 location | Justification |
|---|--------------|------------------------|-------------------|---------------------|---------------|
| E1 | ecosystem completeness claim | `/compositions[]` with `aggregate` + `assemblies/dependencies` | Document-level Annotation `compositions`, payload is the original CDX array | Document-level `Annotation` element on the SpdxDocument carrying `compositions`, payload shape `{complete_ecosystems: [...]}` | SPDX 2.3 has no `compositions` analogue; SPDX 3's `profileConformance` is a closely-related typed concept but doesn't carry the ecosystem-completeness claim shape — annotation preserves the CDX fidelity. |

## Section F — VEX (CycloneDX 1.6 has native section; SPDX 2.3 does not)

| # | mikebom data | CycloneDX 1.6 location | SPDX 2.3 location | SPDX 3.0.1 location | Justification |
|---|--------------|------------------------|-------------------|---------------------|---------------|
| F1 | vulnerabilities (VEX statements) | `/vulnerabilities[]` | **Sidecar OpenVEX 0.2.0 file** at `mikebom.openvex.json` (next to the SPDX file). Referenced from the SPDX document via `externalDocumentRefs[]` (`externalDocumentId: "DocumentRef-OpenVEX"`, `spdxDocument: <relative path>`, `checksum: SHA-256 of sidecar bytes`). When no VEX statements exist, no sidecar file is created. | **Same sidecar file** at `mikebom.openvex.json`. Cross-referenced from the SpdxDocument element via `externalRef[]` with `externalRefType: "vulnerabilityExploitabilityAssessment"` (the SPDX-3-vocabulary-canonical VEX type from `prop_ExternalRef_externalRefType`), `contentType: "application/openvex+json"`, `locator: [<relative path>]`, `comment: "OpenVEX 0.2.0 sidecar produced by mikebom"`. When no VEX statements exist, no sidecar file is created and no externalRef entry is emitted. Per spec clarification Q1 / FR-014. | Per clarification Q2 (Option A): annotations would balloon for vulnerable images; OpenVEX is the purpose-built spec; CSAF VEX is overkill. SPDX 3 lacks a SHA-256 slot on ExternalRef so content-integrity is the consumer's responsibility (matches SPDX 3 vocabulary intent). |

## Section G — Document envelope

| # | mikebom data | CycloneDX 1.6 location | SPDX 2.3 location | SPDX 3.0.1 location | Justification |
|---|--------------|------------------------|-------------------|---------------------|---------------|
| G1 | tool name + version | `/metadata/tools/components[name="mikebom"]` | `creationInfo/creators[]: "Tool: mikebom-<version>"` | `CreationInfo/createdUsing` of `Tool` element with `name`/`packageVersion` | Native in all. |
| G2 | created timestamp | `/metadata/timestamp` | `creationInfo/created` | `CreationInfo/created` | Native in all. Sourced from a single `OutputConfig.created` to keep all formats in one invocation in lockstep. |
| G3 | data license | n/a | `dataLicense: "CC0-1.0"` (SPDX-mandated) | `dataLicense` field on document | SPDX-required. |
| G4 | document namespace | n/a (CDX uses `serialNumber: "urn:uuid:..."` — random per emission) | `documentNamespace` derived per R8 (deterministic) | `SpdxDocument/spdxId` URI (deterministic) | CDX `serialNumber` remains random for backward-compat reasons; SPDX namespaces are deterministic per FR-020. |

## Section H — Structural differences between formats

Rows here document format-level representational differences that don't fit cleanly in per-row mappings. They're NOT bugs; they're spec-level differences that consumers comparing mikebom outputs across formats need to be aware of.

| # | Topic | CycloneDX 1.6 | SPDX 2.3 | SPDX 3.0.1 | Justification |
|---|-------|---------------|----------|------------|---------------|
| H1 | Nested vs. flat component representation | **Nested** via `component.components[]` (CDX 1.6 §6.2.10) — e.g. Maven shade-plugin fat-jars emit vendored coords nested under the fat-jar top-level component. Mikebom's `cyclonedx/builder.rs::build_components` filters `parent_purl.is_none()` for top-level emission and folds children via `effective_parent`. | **Flat** `packages[]` (SPDX 2.3 §7 — spec forbids nesting). Parent-child containment expressed via `Relationship[relationshipType: "CONTAINS"]` edges. Every `ResolvedComponent` becomes a top-level Package regardless of `parent_purl`. | **Flat** `@graph` `software_Package` elements. Same pattern as SPDX 2.3: parent-child via `Relationship[relationshipType: "contains"]`. | Consequence: CDX's `cdx.components.length` counts *top-level only*; SPDX 2.3's `spdx23.packages.length` counts *everything*. Comparing the two raw counts will drift by the count of nested children. The [`component_count_parity.rs`](../../mikebom-cli/tests/component_count_parity.rs) test locks the invariant: `cdx.flattened_count == spdx23.packages.length - synthetic_root_count == spdx3.software_Package.count - synthetic_root_count`. The user-reported "22-component drift" on the external `polyglot-builder-image` fixture came from comparing top-level CDX to flat SPDX — not a bug, a format-level difference. See `specs/012-sbom-quality-fixes/research.md` §R2 for the full investigation trace. |

## Section I — Native-field audit records (Constitution Principle V)

Constitution Principle V (v1.4.0) requires every milestone that introduces operator-supplied or detector-derived data to first audit each SBOM format for an existing native landing slot, and to emit a `mikebom:` annotation only when no native field exists. The rows below are the durable audit-trail entries — proof that the "look for native fields first" check ran and the outcome was recorded for future milestone reviewers.

| Milestone | Subject | CDX 1.6 audit | SPDX 2.3 audit | SPDX 3 audit | Outcome |
|-----------|---------|---------------|----------------|--------------|---------|
| 080 | Operator-supplied SBOM metadata (`--creator`, `--annotator` + `--annotation-comment`, `--metadata-comment`, `--scan-target-name`, `--metadata-file`) | **Audited** `bom.annotations[]` (CDX 1.6 §4.21, schema `definitions.annotation`) for `--metadata-comment` and `--annotator`/`--annotation-comment` landing slots; confirmed required fields `subjects, annotator, timestamp, text` are present and the `annotator` `oneOf` accepts `organization` / `individual` / `component` discriminator (matches `Creator.kind`). Confirmed `metadata.tools.components[]` accepts new application-typed entries for `--creator "Tool:"`. Confirmed `metadata.manufacturer` (single-valued) accepts the first `--creator "Organization:"`. Confirmed `metadata.authors[]` accepts `--creator "Person:"`. | **Audited** `creationInfo.creators[]`, `creationInfo.comment`, `annotations[]` (annotator + annotationDate + annotationType + comment), and document-level `name` field per SPDX 2.3 §6 + §8. All flag families have direct native homes; no fallback needed. | **Audited** `CreationInfo.createdBy[]` / `createdUsing[]` for `--creator`, new `Annotation` elements for `--metadata-comment` + `--annotator`/`--annotation-comment` (subject = SpdxDocument IRI; `annotationType: "other"`), `SpdxDocument.name` for `--scan-target-name`. The SPDX 3 `Tool` / `Organization` / `Person` element types directly model `CreatorKind`. | **Native CDX 1.6 support confirmed; no `mikebom:` parity bridge introduced.** All five flag families land at standards-native slots in all three formats. The audit closed with zero new annotation namespaces. The integration test `cdx_native_annotations_emit_correctly` (mikebom-cli/tests/sbom_user_metadata.rs) gates this at CI time by asserting no `mikebom:invocation-comment` / `mikebom:annotation` / `mikebom:metadata-comment` / etc. fallback properties leak into emitted CDX 1.6 SBOMs. |
| 081 | Document-level SBOM-type signaling (CISA SBOM Types: Design / Source / Build / Analyzed / Deployed / Runtime). | **Audited** `metadata.lifecycles[].phase` (CDX 1.6 schema, `phase` enum: `design`/`pre-build`/`build`/`post-build`/`operations`/`discovery`/`decommission`). Wired natively since milestone 047 via `lifecycle_phases::aggregate_phases` + `tier_to_phase`. **Clean — no code change needed.** | **Audited** SPDX 2.3 spec for a document-level SBOM-type enum; **none exists**. The closest is `Document.creationInfo.creators[]` (signals "what tool produced this SBOM" not "what type of SBOM this is") and per-Package `relationship` types (per-component, not document-level). **Principle V escape clause applies** — `creationInfo.comment` continues to carry the milestone 047 aggregated phase set as free-text. Documented as such; no native-field gap. | **Audited** SPDX 3.0.1 schema (`mikebom-cli/tests/fixtures/schemas/spdx-3.0.1.json`): `software_Sbom_props.software_sbomType[]` enumerates exactly the 6 CISA SBOM Types (`design`/`source`/`build`/`analyzed`/`deployed`/`runtime`). **Native field gap** — pre-081 mikebom emitted only the `comment` aggregation on `SpdxDocument`; the native enum was not wired. | **CDX 1.6 + SPDX 2.3 unchanged. SPDX 3 native field promotion**: emit a new `software_Sbom` element in `@graph` alongside the existing `SpdxDocument` element (the schema's `unevaluatedProperties: false` constraint blocks adding `software_sbomType` to a `SpdxDocument`-typed element AND blocks `dataLicense`/`import`/`namespaceMap` on a `software_Sbom`-typed element, so both elements coexist with their respective property sets). The new `--sbom-type <type>` flag (US3) drives a single-element override across all three formats while preserving per-component `mikebom:sbom-tier` annotations. The `runtime` SBOM-type tier auto-detection (US4) is **deferred** to a separate GitHub issue per research §3 — mikebom's eBPF observes builds, not runtime; the flag accepts `runtime` as a vocab value for operator self-assertion. See `docs/reference/sbom-types.md` for the operator-facing reference. |
| 141 | Erlang/OTP `*.app.src` `applications:` / `included_applications:` / `optional_applications:` keyword-family discrimination — distinguishing which OTP application descriptor keyword sourced each main-module dep-edge target (required hard-dep vs included embedded-sub-app vs optional OTP-26+ soft-dep). | **Audited** `component.scope` (CDX 1.6 enum: `required`/`optional`/`excluded`). REJECTED as the carrier — its semantic is "is this dep required for the application to function" (build/runtime lifecycle-scope axis), NOT "which OTP runtime-startup-behavior keyword sourced this edge." Adopting `scope` would conflate the OTP supervisor-startup discrimination with the dev/test/runtime lifecycle-scope axis the milestone-052 bridge already drives. A future operator may legitimately have an `optional_applications:` dep that is ALSO production-lifecycle-scope (the `telemetry` integration example) — the two axes must coexist. | **Audited** SPDX 2.3 `Relationship[relationshipType]` enum (`DEV_DEPENDENCY_OF`, `BUILD_DEPENDENCY_OF`, `TEST_DEPENDENCY_OF`, etc.) — these are lifecycle-scope discriminators, NOT OTP-keyword-family discriminators. REJECTED for the same reason as CDX `scope`. | **Audited** SPDX 3 `LifecycleScopeType` (vocab: `build`, `runtime`, `test`, `development`, `design`, `other`) — same lifecycle-scope axis. REJECTED for the same reason. | **`mikebom:erlang-app-dep-kind` parity-bridge annotation introduced** (values: `"required"` / `"included"` / `"optional"`). Documented under research §R3 + §R6 of `specs/141-erlang-rebar-reader/research.md`. Precedence on multi-source name collision: required > included > optional (most-binding wins). Supplemented by `mikebom:otp-stdlib = "true"` annotation per Q1 on entries matching the hardcoded Ericsson-distributed OTP runtime allowlist (informational marker — does NOT gate emission per Q1's over-emission contract). Supplemented by `mikebom:vcs-declared-ref` annotation on Git-source pinned-deps to preserve the operator's declared-ref form (`ref` / `tag` / `branch`) alongside the resolved SHA. None of the three annotations have a standards-native carrier; the parity-bridge is durable until the SBOM standards evolve a runtime-startup-behavior vocabulary. |
| 142 | Scala/SBT `%%`-declared dep PURL emission requires a Scala-version suffix (`_2.13` / `_3`) to construct a valid Maven Central coordinate. When the source build.sbt's `scalaVersion := "..."` is absent, the reader applies a Q1 inference cascade — explicit setting → `project/build.properties` embedded `scala.version=` → default `_2.13`. Operator-debugging requires visibility into WHICH cascade rung produced the suffix (was the `_2.13` explicit, inferred, or defaulted?). | **Audited** `component.scope` (CDX 1.6 enum: `required`/`optional`/`excluded`) and `component.evidence.identity.confidence` — REJECTED. `scope` is lifecycle-scope (orthogonal). `evidence.identity.confidence` would conflate PURL-derivation-heuristic with identity-resolution-confidence; not a fit. No native CDX field carries "the artifactId suffix was inferred via this specific cascade rung." | **Audited** SPDX 2.3 `Annotation` shape and `Package.externalRefs[].comment` — REJECTED. `Annotation.comment` is general free-text without a "heuristic-derived-attribute" axis; flowing the cascade-rung discrimination through there would defeat the structured-property goal of Principle V. | **Audited** SPDX 3 `Annotation` (general comment) and `LifecycleScopeType` (lifecycle-scope axis) — REJECTED for the same reasons. | **`mikebom:scala-version-source` parity-bridge annotation introduced** (values: `"build-sbt-explicit"` / `"build-properties-embedded"` / `"default-fallback"`). Emitted on every `%%`-declared design-tier dep AND on every main-module component (the latter via the Q1 cascade + F6 remediation contract — operators must distinguish explicit-scalaVersion main-modules from default-fallback ones; without propagation through `metadata.rs`'s curated allowlist the annotation would be stripped on the promoted `metadata.component` subject). Documented under research §R3 of `specs/142-scala-sbt-reader/research.md`. Pure-Java `%`-declared deps do NOT carry the annotation (no Scala suffix applies). The parity-bridge is durable until SBOM standards evolve a "PURL field provenance" vocabulary. |
| 143 | Haskell ecosystem TWO parity-bridges: (a) GHC boot-library discriminator (`mikebom:ghc-stdlib`) — distinguishes the ~22 packages that ship with the Glasgow Haskell Compiler (`base`, `text`, `bytestring`, `containers`, etc.) from user-pulled Hackage packages so downstream tools can filter "user code only" views; their versions are coupled to the compiler release rather than to user-declared deps; (b) Stackage snapshot identifier (`mikebom:stackage-resolver`) — preserves the human-friendly resolver name (`lts-22.0` / `nightly-2024-01-15` / `ghc-9.6.4`) for the snapshot-placeholder component, so downstream tools can correlate the curated-bundle without parsing the PURL. | **(a) ghc-stdlib audit**: `component.scope` (lifecycle-scope axis, orthogonal — REJECTED). `component.evidence.identity.confidence` (PURL-resolution confidence, orthogonal — REJECTED). No CDX field carries "is this an ecosystem stdlib member." **(b) stackage-resolver audit**: `component.group` (vendor/grouping — REJECTED, Stackage isn't the vendor; the resolver is a curation identifier, not a vendor name). `component.evidence.distribution.url` (distribution URL — REJECTED, the resolver isn't a URL). | **(a) ghc-stdlib audit**: `Package.primaryPackagePurpose` (FILE/INSTALL/OPERATING-SYSTEM/etc. — `OPERATING-SYSTEM` is closest in spirit but specifically scoped to OS distros; misusing it for language stdlibs would conflict with spec intent — REJECTED). `Package.builtDate` (temporal, wrong axis — REJECTED). **(b) stackage-resolver audit**: `Package.sourceInfo` (free-text) — could technically fit but loses machine-readability that downstream filters depend on. REJECTED. | **(a) ghc-stdlib audit**: `software_softwarePurpose` enum — same shape as SPDX 2.3's `primaryPackagePurpose`; REJECTED for the same reason. **(b) stackage-resolver audit**: same shape as SPDX 2.3 audit; no fit. | **Two `mikebom:*` parity-bridges introduced**, both durable until SBOM standards evolve native vocabulary for the respective axes: (a) **`mikebom:ghc-stdlib = "true"`** (Q1 + FR-014). Emitted on every component whose lowercased name matches the hardcoded boot-library allowlist (case-insensitive). Informational — does NOT gate emission per Q1's over-emission contract. Identical shape to milestone-141's `mikebom:otp-stdlib`. (b) **`mikebom:stackage-resolver`** (FR-005). Emitted on the Stack snapshot placeholder component (`pkg:generic/stackage-<resolver>@<sha-or-unspecified>`) with the verbatim resolver identifier from `stack.yaml`'s `resolver:` field. Allows downstream tools to correlate scans against known-vulnerable Stackage snapshots without parsing PURL strings. Both annotations documented under research §R6 of `specs/143-haskell-reader/research.md`. |

## Maintenance contract

This map MUST be updated in lockstep with any change that adds a new mikebom property, new external reference, new evidence type, or new ecosystem. A CI check enforces 100% row coverage by scanning the existing CDX output for distinct property names and asserting each has a row here.

---

## See also

- [Identifiers](identifiers.md) — the four-layer identity model and per-flag
  identity behavior. Search `C47` here for the `mikebom:identifiers` row.
- [SBOM types](sbom-types.md) — CISA SBOM Type signaling and the
  `--sbom-type` flag. Section I above carries the milestone 081 audit-record.
- [Conformance harness guide](conformance-harness-guide.md) — per-format
  envelope-decode rules and the 7 inherent format-spec asymmetries.
