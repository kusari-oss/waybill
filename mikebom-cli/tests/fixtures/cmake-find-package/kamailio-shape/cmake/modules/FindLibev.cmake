# Depth-2 placeholder — NOT discovered by mikebom's current depth-1
# walker. Included for Kamailio-shape documentation only. The
# find_package_handle_standard_args below would prove FR-009 negation
# if the walker ever extends to depth-2 (future milestone).

find_package_handle_standard_args(Libev DEFAULT_MSG LIBEV_LIBRARY LIBEV_INCLUDE_DIR)
