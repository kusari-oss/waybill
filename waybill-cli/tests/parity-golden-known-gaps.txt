# Known cross-format parity gaps in the committed golden corpus.
#
# CURRENTLY EMPTY. Every gap milestone 965 surfaced has been fixed:
#
#   bazel:E1 / cmake:E1  compositions reached CDX but not SPDX      -> #1001
#   bazel:B1 / bazel:B2  dev-scoped edge emitted as runtime in SPDX -> #1000
#   npm:A6               file-tier hash appeared to reach SPDX 3
#                        only — a normalizer artifact, not a waybill
#                        defect                                     -> #1002
#
# Keep it empty. An entry here is a real cross-format defect that the
# gate is being told to tolerate, so adding one is a decision to make
# deliberately and with an issue to point at — never a way to turn a red
# lane green.
#
# THE TEST FAILS BOTH WAYS. A violation absent from this file fails as a
# regression, and a listed entry that no longer violates ALSO fails,
# naming the line to delete. That second assertion is what caught all
# three fixes above; without it the file rots into permanent cover.
#
# Format: <ecosystem>:<row-id>   # <issue> <why>
