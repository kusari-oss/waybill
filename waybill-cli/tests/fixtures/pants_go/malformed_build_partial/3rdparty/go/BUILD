go_mod(name="mod")

go_third_party_package(name="one", import_path="github.com/waybill-fixture/one")

go_third_party_package(name="two", import_path="github.com/waybill-fixture/two")

# INTENTIONALLY BROKEN — unclosed paren spans EOF.
go_third_party_package(
    name="broken",
    import_path=
