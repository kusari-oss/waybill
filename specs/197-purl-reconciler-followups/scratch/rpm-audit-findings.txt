m197 US2b (T014): rpm epoch qualifier audit — ALREADY CORRECT

## Finding

The rpm reader (`mikebom-cli/src/scan_fs/package_db/rpm.rs`) already
emits `?epoch=<N>` qualifier correctly. Evidence:

- `rpm.rs:414` — comment documents `&epoch=0` handling convention
- `rpm.rs:548` — canonical PURL shape doc-comment includes `&epoch=<epoch>`
- `rpm.rs:572` — `Some(v) if v != 0 => format!("&epoch={v}")` — emits
  when non-zero, drops when None or Some(0), matching purl-spec + m190
  opkg convention
- `rpm.rs:1124` — pre-existing test explicitly asserts `entry.purl.as_str().contains("&epoch=1")`
  against a `TAG_EPOCH: Int32Array(&[1])` synthetic rpm header. Test
  passes as of m197 baseline.
- `rpm.rs:1129` — negative assertion — `!contains("@1:")` — proves
  the epoch is NOT emitted inline. Test passes.

## Origin

Correct handling was in place from milestone 004 (rpm reader ship) +
refined through m003 / m144 rpm-purl-size-fixes. Predates the m190
opkg-side bug that motivated m197 US1/US2.

## Action taken

**No code change**. US2b's FR-002a "If the reader is already-correct,
the FR is satisfied by a non-regression fixture" branch fires. The
pre-existing `rpm.rs:1097+` test IS the non-regression fixture; no
new fixture needed.

## PR body note

m197 US2b audit finding: rpm reader confirmed already-correct via
existing test coverage at rpm.rs:1097-1134 (TAG_EPOCH set to 1 →
PURL contains `&epoch=1`, does NOT contain inline `@1:`). No code
change. Regression coverage preserved via the pre-existing test.
