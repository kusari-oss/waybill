# Tasks: bun.lock transitive-edge emission

**Feature Branch**: `667-bun-lock-edges`
**Spec**: [spec.md](./spec.md) | **Plan**: [plan.md](./plan.md) | **Contract**: [contracts/depends-emission.md](./contracts/depends-emission.md) | **Data model**: [data-model.md](./data-model.md)
**Closes**: [#723](https://github.com/kusari-oss/waybill/issues/723)

## Phase 1: Setup

- [X] T001 Verify baseline `cargo +stable test -p waybill --bin waybill -- scan_fs::package_db::npm::bun_lock` passes at the pre-fix count on the freshly-checked-out `667-bun-lock-edges` branch. Record the count in this task's completion note. Establishes the SC-006 no-regression anchor for the bun_lock module specifically. Also run the full workspace test and record its count (should equal m665/m666 baseline of 5192). **Verified 2026-08-27**: (a) `bun_lock` module → **5 passed / 0 failed / 0 ignored / 3454 filtered out** — matches the reporter's count in issue #723; all 5 pre-fix tests green (`workspace_member_uses_placeholder_when_no_pkg_json`, `override_version_wins`, `encodes_scoped_packages`, `emits_basic_npm_components`, `emits_workspace_shape`). (b) Workspace-wide → **5192 passed / 0 failed / 14 ignored** — matches m665+m666 baseline exactly. SC-006 no-regression anchor established at (bun_lock=5, workspace=5192).
- [X] T002 Baseline the pre-fix behavior on the minimal repro from issue #723: create a scratch dir with the reporter's exact `package.json` + `bun.lock` (embedded verbatim in the issue body), scan with `cargo run --release --bin waybill -- sbom scan --path <scratch> --offline --format cyclonedx-json --output /tmp/pre-fix.cdx.json`, and record: (a) `orphan_count` from the emitted CDX's document annotations, (b) whether `parent-pkg@1.0.0 → child-pkg@1.0.0` edge is present in `dependencies[]`, (c) whether `child-pkg@1.0.0` carries `waybill:orphan-reason=hoisted-unused`. Expected pre-fix: `orphan_count=1`, edge absent, orphan-reason present. This is the SC-001 pre-fix anchor. **Verified 2026-08-27**: scratch dir at `/tmp/bun_lock_667_prefix_baseline/` with the reporter's exact 2 files (`package.json` + `bun.lock` with `parent-pkg` metadata declaring `dependencies: {"child-pkg": "^1.0.0"}`). Scanned via the current debug build (no code changes yet = effectively pre-fix). Baseline observations, all matching issue #723 verbatim: **(a) `orphan_count=1`** — logged during scan; document annotation `waybill:graph-completeness = "partial"` + `waybill:graph-completeness-reason = "orphaned-components-detected: 1 component(s) not reachable from root"`. **(b) Edge absent** — `dependencies[]` entry for `pkg:npm/parent-pkg@1.0.0` has `dependsOn: []`. **(c) Orphan-reason present** — `pkg:npm/child-pkg@1.0.0` component carries property `{"name": "waybill:orphan-reason", "value": "hoisted-unused"}`. Also observed: reader logs `orphan_reason_hoisted_unused=1` from the m167 classifier pass. SC-001 pre-fix anchor established; post-fix (T026 integration test) MUST invert all three observations.

## Phase 2: Foundational

**None.** The fix is a single-file addition to an existing reader; there are no shared prerequisites across user stories. Both user stories (US1 code fix + US2 docs update) can proceed in the linear order the tasks below prescribe. Skip to Phase 3.

## Phase 3: US1 — Bun-lockfile-only scan produces a complete dep-graph (Priority: P1) 🎯 MVP

**Goal**: extend `bun_lock.rs::parse_bun_lock` with a two-pass edge-emission phase that reads each `packages[K][2]` metadata object, resolves each declared dep-name via a scope-aware key-path walker, and populates each parent entry's `PackageDbEntry.depends` field with `"<name> <version>"` disambiguation strings.

**Independent Test**: post-fix integration test at `waybill-cli/tests/bun_lock_edges_us1.rs` passes on the minimal repro fixture. `graph completeness = complete`, `orphan_count = 0`, `parent-pkg → child-pkg` edge present.

### Fixture setup (all [P] — separate files, no code dependencies)

- [X] T003 [P] [US1] Create `waybill-cli/tests/fixtures/bun_lock/minimal_repro/{package.json, bun.lock}` — the reporter's issue #723 minimal repro verbatim (single non-workspace `package.json` declaring `parent-pkg`, `bun.lock` with `parent-pkg` and `child-pkg` entries where parent's metadata object declares `dependencies: {"child-pkg": "^1.0.0"}`). Per data-model.md fixture layout. **Done 2026-08-27**: fixture landed at `waybill-cli/tests/fixtures/bun_lock/minimal_repro/`. Byte-identical to T002's verified scratch fixture (`diff` returns 0 for both files). Reader logs pre-fix show `orphan_reason_hoisted_unused=1` on `child-pkg`; T026 will re-scan this fixture post-fix and assert the inverse.
- [X] T004 [P] [US1] Create `waybill-cli/tests/fixtures/bun_lock/multi_version/{package.json, bun.lock}` — same package name at two versions under two different parent key paths (per quickstart.md "Multi-version fixture" section: `big/minimatch@3.1.2` + `small/minimatch@5.1.6`). Per SC-004. **Done 2026-08-27**: fixture landed. Pre-fix scan → 5 components (`big@1.0.0`, `small@2.0.0`, `minimatch@3.1.2`, `minimatch@5.1.6`, workspace-root shim); `orphan_count=2` (both minimatch versions currently orphaned). Both version copies correctly emit as distinct `PackageDbEntry`s — the reader's `packages`-map component-emission works fine; only the edge-emission needs the fix. Post-fix must route `big→minimatch@3.1.2` AND `small→minimatch@5.1.6` (never crossed) per SC-004.
- [X] T005 [P] [US1] Create `waybill-cli/tests/fixtures/bun_lock/scoped_name/{package.json, bun.lock}` — scoped-name resolver fixture with parent at `"@fast-csv/format"` declaring `dependencies: {"@types/node": "^22"}` and target keyed at `"@fast-csv/format/@types/node"`. Per SC-005. **Done 2026-08-27**: fixture landed. Pre-fix scan → 3 components (`@fast-csv/format@4.3.6`, `@types/node@22.5.0`, workspace-root shim); `orphan_count=1` (@types/node currently orphaned). Both scoped-name components emit correctly with URL-encoded `%40` in PURLs. Post-fix must route `@fast-csv/format → @types/node@22.5.0` via the R2 scope-aware resolver walking `"@fast-csv/format/@types/node"` before falling back to bare `@types/node` (SC-005).
- [X] T006 [P] [US1] Create `waybill-cli/tests/fixtures/bun_lock/optional_deps/{package.json, bun.lock}` — parent entry declaring `optionalDependencies: {"opt-child": "^1.0.0"}` plus an `opt-child` entry. Per US1 scenario 3. **Done 2026-08-27**: fixture landed. Pre-fix scan → 3 components (`parent@1.0.0`, `opt-child@1.0.0`, workspace-root shim); `orphan_count=1` (opt-child currently orphaned). Post-fix must route `parent → opt-child` AND tag `opt-child` with `lifecycle_scope=Some(LifecycleScope::Optional)` + `waybill:optional-derivation="bun-optional-dependencies"` (US1 scenario 3, data-model V6).

### Implementation (sequential — all touch `waybill-cli/src/scan_fs/package_db/npm/bun_lock.rs`)

- [X] T007 [US1] Add `fn resolve_bun_key(parent_key: &str, dep_name: &str, packages_keys: &std::collections::HashSet<&str>) -> Option<String>` to `bun_lock.rs` as a private pure function. Body implements the R2 scope-aware key-path walker:
  1. Split `parent_key` into scope-atomic segments: `@scope/name` is ONE segment; `name` is ONE segment. Slashes inside a scope prefix DON'T count as segment boundaries.
  2. Walk from most-specific to root: for each `level` from `segments.len()` down to `0`, construct `candidate = format!("{}/{}", segments[0..level].join("/"), dep_name)` (or `dep_name` alone at level 0).
  3. Return `Some(candidate)` on first match in `packages_keys`; `None` on exhausted walk.
  Add rustdoc citing research.md R2 + contract C2. (I1-remediation: task's original "Complete with 3 inline unit tests" clause dropped per analyze finding — tests land in T014-T017.) **Done 2026-08-27**: landed at `bun_lock.rs:46-149` (after the `WORKSPACE_MEMBER_VERSION_PLACEHOLDER` constant, before `read_bun_lock`). Two functions: `segment_bun_key` (scope-atomic segmenter) + `resolve_bun_key` (walker). Both `#[allow(dead_code)]`-guarded until T010 consumes them. Rustdoc covers algorithm, examples, three test-vector examples, and cites research R2 + contract C2. Also notes a correction: research.md's first R2 test vector originally listed an intermediate candidate `"@fast-csv/format/@types/tslib"` for the nested-scope walk — that was a scope-boundary miscount in my authoring; the correct walk drops that step because scoped-name segments are atomic. Implementation follows the corrected walk (3 candidates for a 2-segment parent key, not 4). T014's unit test will assert the corrected order. Compile + clippy clean.
- [ ] T008 [US1] Add helper `fn is_scoped_name_start(segment: &str) -> bool` (returns `segment.starts_with('@')`). Used by `resolve_bun_key`'s scope-aware segmenter. Optional helper — inline in T007 if simpler.
- [X] T009 [US1] After the pre-fix component-emission loop (post `bun_lock.rs:279`, before the `out` return), add **Pass 1**: iterate `packages` again to build `packages_key_index: HashMap<&str, String>` mapping each `packages`-map key to `format!("{} {}", name, version)` for the emitted component. Match the override-resolution logic used at `bun_lock.rs:235-238` so the disambiguation string reflects the overridden version. Skip workspace-shape entries (matching pre-fix skip at line 228 + workspace_member_names contains-check). **Done 2026-08-27**: (a) added `let step2_start = out.len();` at line 313 immediately BEFORE Step 2 (single-line binding outside the emission loop — Step 2's loop body byte-identical to pre-m667; preserves FR-009). (b) added Pass 1 block at lines 398-459 immediately after Step 2's close. Builds BOTH `packages_key_index` (T009 primary output) AND `parent_key_to_out_idx` (bonus prep for T010 per the task's inline note). Skip criteria mirror Step 2 verbatim: `workspace:` source-spec, workspace_member_names contains-check, PURL-build validation. `emit_seq` counter keeps stride in lock-step with Step 2's `push()` sequence so `parent_key_to_out_idx[K] = step2_start + emit_seq` correctly resolves to the index of K's component in `out`. Both indexes have `let _ = &...;` sinks to keep them dead-code-clean until T010 consumes them. Pre-fix 5 bun_lock tests still pass; workspace check clean.
- [X] T010 [US1] Add **Pass 2** immediately after T009: iterate `packages` a third time. For each entry with a well-formed metadata object at tuple position `[2]`:
  1. Get the parent's own `packages_key_index` value (the parent's disambiguation string) so we know which `PackageDbEntry` to mutate — build `parent_key → parent_out_index: HashMap<&str, usize>` during T009 for O(1) lookup into `out: Vec<PackageDbEntry>`.
  2. For each of the four sections `["dependencies", "peerDependencies", "optionalDependencies", "optionalPeers"]` (walked in that order):
     - For each `(dep_name, range_value)` in the section's sub-map: call `resolve_bun_key(parent_key, dep_name, &packages_keys)`; on `Some(resolved_key)` append `packages_key_index[&resolved_key].clone()` to a `depends_set: BTreeMap<String, String>` (dep-name-keyed, `<name> <version>`-valued; matches `package_lock.rs:181-285` precedent for dedup + ordering).
     - Track which target keys came via optional/optional-peers sections (see T012 for the tagging pass).
  3. Assign `out[parent_out_index].depends = depends_set.into_values().collect()`.
  **Done 2026-08-27**: Pass 2 landed at `bun_lock.rs:470-587`. Structure: (a) local `TargetOptionalityState` struct (`{any_hard, seen_optional_deps, seen_optional_peers}`) — persists across all parents in `target_opt_state: HashMap<String, TargetOptionalityState>` (declared at outer scope for T012 finalize consumption). (b) `packages_keys: HashSet<&str>` materialized from `packages_key_index.keys()` for resolver's search space. (c) Third iteration over `packages`: tuple-position-[2] resolution (`metadata_absent`, `metadata_malformed` marked with T011 comments for warn-log insertion), parent-in-out lookup via T009's `parent_key_to_out_idx`, per-parent `depends_set: BTreeMap<String, String>` accumulation walked across all 4 sections, resolver called per dep, disambiguator inserted dep-name-keyed. Per-target optionality state accumulated. Assignment `out[parent_out_idx].depends = depends_set.into_values().collect()` populates the parent's edge list. Also removed `#[allow(dead_code)]` from T007's `segment_bun_key` + `resolve_bun_key` since Pass 2 now consumes them. **SC-001 END-TO-END VERIFIED on minimal_repro fixture**: pre-fix `graph completeness = partial, orphan_count = 1, hoisted-unused = 1` INVERTS to post-fix `complete, 0, 0`; edge `pkg:npm/parent-pkg@1.0.0 → pkg:npm/child-pkg@1.0.0` present in `dependencies[]`; `child-pkg` `waybill:orphan-reason` annotation ABSENT. All three T002 baseline observations invert exactly as SC-001 predicts.
- [X] T011 [US1] Add FR-011 warn-and-drop logging inside the Pass 2 loop for each edge-drop case (per R5 + contract C5):
  - `metadata_absent` — when `packages[K][2]` is `None` or the `packages[K]` array has fewer than 3 elements.
  - `metadata_malformed` — when `packages[K][2]` is present but not a JSON object.
  - `unresolved` — when `resolve_bun_key` returns `None` for a dep_name.
  - `empty_range` — when the dep_name's value in the sub-map is null or empty string.
  All log lines use the exact format from R5's table: `bun.lock edge dropped: parent={K} [dep={X}] reason={reason}`.
  **Done 2026-08-27**: all 4 T011 comment sites in T010's Pass 2 replaced with `tracing::warn!` calls using structured fields (`parent = %parent_key`, optional `dep = %dep_name`, `reason = "<reason>"`, message = `"bun.lock edge dropped"`). Structured-fields form preserves R5's grep pattern (`grep 'bun.lock edge dropped'`) while giving downstream log processors machine-parseable attributes. Smoke test: (a) 4 healthy fixtures → 0 warn lines each; (b) synthetic malformed fixture (parent metadata declares `nonexistent-dep`) → exactly one warn line `bun.lock edge dropped parent=parent dep=nonexistent-dep reason="unresolved"`. Grep count of `T011:` comments = 0 (all markers consumed). Compile clean.
- [X] T012 [US1] Add a **finalize pass** after T010 that applies optional-scope tagging per data-model V5/V6 + contract C7:
  1. During T010's Pass 2, maintain per-target tracking: `HashMap<key, TargetOptionalityState>` where `TargetOptionalityState = { any_hard: bool, seen_optional_deps: bool, seen_optional_peers: bool }`. Update `any_hard` when the target is reached via `dependencies` or `peerDependencies`; update `seen_optional_*` when reached via the corresponding optional section.
  2. In this finalize pass, for each target with `!any_hard && (seen_optional_deps || seen_optional_peers)`:
     - Locate the target in `out` via `packages_key_index`.
     - Set `out[target_index].lifecycle_scope = Some(LifecycleScope::Optional)` (from `waybill_common::resolution`).
     - Insert `out[target_index].extra_annotations["waybill:optional-derivation"] = serde_json::Value::String("bun-optional-dependencies".to_string())` if `seen_optional_deps`, else `"bun-optional-peers"`. The `-dependencies` variant wins when both are true (data-model V6).
  **Done 2026-08-27**: finalize pass landed at `bun_lock.rs:622-676` (between Pass 2 tail and Step 3). Structure: iterates `target_opt_state`, skips targets with `any_hard = true` (data-model V5), locates target's `out` index via `parent_key_to_out_idx` reuse (T009), sets `lifecycle_scope = Some(LifecycleScope::Optional)`, inserts `waybill:optional-derivation` with `"bun-optional-dependencies"` when `seen_optional_deps` else `"bun-optional-peers"` (V6 precedence). Uses inline `use waybill_common::resolution::LifecycleScope;` matching the m180 `package_lock.rs:318` precedent verbatim. **Verified on optional_deps fixture**: `opt-child` component post-fix carries `waybill:lifecycle-scope = "optional"` + `waybill:optional-derivation = "bun-optional-dependencies"` + edge `parent → opt-child` in `dependencies[]`. CDX top-level `scope: "excluded"` and `orphan_count = 1` are m179-inherited semantics (the m167 classifier treats optional edges as non-runtime → optional-only targets classify as orphans of the runtime graph); this matches how the equivalent `package-lock.json` fixture would behave and is NOT a m667 concern. Pre-fix 5 bun_lock tests still pass.
- [X] T013 [US1] Add rustdoc block above the new Pass 1 / Pass 2 / finalize code (lines ~280 in the modified file) explaining the two-pass approach + citing contracts/depends-emission.md (C1-C9) + citing the m147 issue #262 convention that the graph builder consumes. **Done 2026-08-27**: added a top-level design-overview banner block at `bun_lock.rs:305-358` (immediately before the `step2_start` capture that opens all m667 code paths). Banner covers: (a) pre-fix state summary + concrete reporter observations from #723, (b) post-fix roadmap naming Pass 1 (T009) / Pass 2 (T010+T011) / Finalize (T012) with their responsibilities, (c) all 9 contracts C1-C9 named + one-line summary of each, (d) key cross-references to m147 issue #262 (`scan_fs/mod.rs:635-644` for the graph builder's secondary key), m180 `package_lock.rs:318-329` (LifecycleScope pattern), data-model V5/V6 (optional precedence), R2 (resolver test vectors), R5 (warn-log reasons). Per-pass headers below retain their own specific documentation; the banner is the top-down roadmap a maintainer reads first.

### Unit tests (all [P] within `bun_lock.rs`'s test module)

- [X] T014 [P] [US1] Add `#[test] fn resolve_bun_key_walks_scope_prefix_most_specific_first` in `bun_lock.rs`. Constructs a `HashSet` of the R2 test-vector keys, calls `resolve_bun_key` with the three test-vector inputs, asserts the exact expected output per R2's test-vector block. **Done 2026-08-27**: test lands at the top of the pre-fix test module (`bun_lock.rs:766-810`, immediately after `write` helper). Uses the R2 nested-scope walk fixture (parent=`"@fast-csv/format/@types/node"`, dep=`"tslib"`). Asserts each of the 3 correct walk positions individually — seeds `packages_keys` with ONLY that position and asserts the resolver returns exactly it. Also asserts empty `packages_keys` → `None`. In-line rustdoc notes the research.md R2 spec-authoring error (originally listed a 4th candidate `"@fast-csv/format/@types/tslib"`; walker correctly implements 3 candidates). `cargo test` → 1 passed / 0 failed / 0.00s.
- [X] T015 [P] [US1] Add `#[test] fn resolve_bun_key_handles_scoped_dep_name` in `bun_lock.rs`. Tests dep_name = `"@scope/pkg"` under parent_key = `"foo/bar/baz"` — the resolver should try `"foo/bar/baz/@scope/pkg"`, `"foo/bar/@scope/pkg"`, `"foo/@scope/pkg"`, `"@scope/pkg"` in order. **Done 2026-08-27**: seeded 3 walk positions individually + asserted each; resolver correctly appends scoped dep-name at every prefix level. `cargo test` → 1 passed.
- [X] T016 [P] [US1] Add `#[test] fn resolve_bun_key_hoisted_parent` in `bun_lock.rs`. Tests parent_key = `"lodash"`, dep_name = `"chalk"` — resolver should try `"lodash/chalk"`, `"chalk"`. **Done 2026-08-27**: 2-candidate walk for 1-segment parent verified. Both positions seeded + asserted individually. `cargo test` → 1 passed.
- [X] T017 [P] [US1] Add `#[test] fn resolve_bun_key_returns_none_on_complete_miss` in `bun_lock.rs`. Tests any parent_key + dep_name where no walk step hits any key in `packages_keys` — resolver should return `None`. **Done 2026-08-27**: distinct from T014's empty-keys control — populates `packages_keys` with 5 near-miss candidates including same-shape / same-name-prefix keys to guard against off-by-one prefix comparisons. Walker returns `None` as expected. `cargo test` → 1 passed.
- [X] T018 [P] [US1] Add `#[test] fn parse_bun_lock_emits_transitive_edges` in `bun_lock.rs`. Constructs the minimal-repro fixture in-memory (via `serde_json::json!`), calls `parse_bun_lock`, asserts: (a) `out.len() == 3` (parent-pkg + child-pkg + workspace-root shim per pre-fix behavior — verify pre-fix behavior first, may be 2), (b) parent-pkg entry's `depends` contains `"child-pkg 1.0.0"`, (c) child-pkg entry's `depends` is empty. **Done 2026-08-27**: `include_str!`-loaded from the on-disk fixture (SoT). Pre-fix count for this fixture is 2 (not 3) — the workspaces map has ONLY key `""` with no non-root members, so no workspace-root synth (matches `emits_basic_npm_components` shape). Asserts parent→child edge in `<name> <version>` shape, child.depends empty. `cargo test` → 1 passed.
- [X] T019 [P] [US1] Add `#[test] fn parse_bun_lock_multi_version_disambiguation` in `bun_lock.rs`. Constructs the multi-version fixture in-memory, calls `parse_bun_lock`, asserts: (a) both minimatch versions emit as separate entries, (b) `big@1.0.0`'s `depends` contains `"minimatch 3.1.2"` (NOT `5.1.6`), (c) `small@2.0.0`'s `depends` contains `"minimatch 5.1.6"` (NOT `3.1.2`). Per SC-004. **Done 2026-08-27**: `include_str!`-loaded from fixture. 4 entries (big + big/minimatch + small + small/minimatch). Assertions include both positive (correct version present) and negative (wrong version absent) for both parents. `cargo test` → 1 passed.
- [X] T020 [P] [US1] Add `#[test] fn parse_bun_lock_scoped_name_resolver` in `bun_lock.rs`. Constructs the scoped-name fixture in-memory, calls `parse_bun_lock`, asserts: `@fast-csv/format`'s `depends` contains `"@types/node 22.5.0"` (the scope-nested version), not any root-hoisted `@types/node`. Per SC-005. **Done 2026-08-27**: `include_str!`-loaded from fixture. Verifies R2 scope-atomic segmenter correctly resolves `@fast-csv/format/@types/node` → `@types/node@22.5.0` at scope-nested level. `cargo test` → 1 passed.
- [X] T021 [P] [US1] Add `#[test] fn parse_bun_lock_optional_dep_tagging` in `bun_lock.rs`. Constructs the optional-deps fixture in-memory, calls `parse_bun_lock`, asserts: `opt-child` entry's `lifecycle_scope == Some(LifecycleScope::Optional)` AND `extra_annotations["waybill:optional-derivation"] == "bun-optional-dependencies"` AND parent's `depends` contains `"opt-child 1.0.0"`. Per US1 scenario 3 + data-model V6. **Done 2026-08-27**: all three invariants verified. Imports `LifecycleScope` inline (from `waybill_common::resolution`). `cargo test` → 1 passed.
- [X] T022 [P] [US1] Add `#[test] fn parse_bun_lock_hard_edge_beats_optional_tag` in `bun_lock.rs`. Constructs a fixture where the same target is reached from parent-A via `dependencies` AND from parent-B via `optionalDependencies`. Asserts the target's `lifecycle_scope == None` (NOT `Some(Optional)`) and the target has NO `waybill:optional-derivation` annotation. Per V5 + C7. **Done 2026-08-27**: inline fixture; deliberately places optional-referencing parent FIRST in the packages map to prove order-independence of hard-wins rule. Both edges land (a→shared hard, b→shared optional); shared stays untagged. `cargo test` → 1 passed.
- [X] T023 [P] [US1] Add `#[test] fn parse_bun_lock_warn_and_drop_on_unresolved` in `bun_lock.rs`. Constructs a fixture where a parent's metadata declares a dep-name that has no matching `packages`-map key. Asserts: (a) `parse_bun_lock` completes successfully (no early return), (b) parent's `depends` does NOT contain the unresolved dep-name, (c) `tracing::warn!` was called with `reason=unresolved` (use `tracing_test::traced_test` attribute if the crate has it available; otherwise the assertion may be structural — the parent's depends set should have zero entries for the unresolved dep). Per FR-011 + C5. **Done 2026-08-27**: structural assertion only per FR-005 no-new-deps constraint (skipped `tracing_test` dev-dep). Verifies (a) reader returns Vec cleanly, (b) parent.depends empty, plus (implicit c) no phantom component for the unresolved name (Principle XII). `cargo test` → 1 passed.
- [X] T024 [P] [US1] Add `#[test] fn parse_bun_lock_preserves_component_count_vs_pre_fix` in `bun_lock.rs`. Constructs the minimal-repro fixture in-memory, calls `parse_bun_lock`, asserts `out.len()` equals the pre-fix count (verified once against T001's baseline). Per FR-008 + FR-009 + C6. **Done 2026-08-27**: reuses minimal_repro fixture via `include_str!`. Pre-fix count = 2 (parent-pkg + child-pkg; no workspace-root synth). Post-fix MUST also emit 2 — invariant proven. `cargo test` → 1 passed.
- [X] T025 [P] [US1] Add `#[test] fn parse_bun_lock_workspace_edges_unchanged` in `bun_lock.rs`. Constructs a workspace-only fixture (2 members with `workspace:*` intra-workspace deps, no packages-map edges). Asserts the workspace-shape `depends` from `bun_lock.rs:135-148` is preserved verbatim (per FR-007 + C8). Verify against the pre-fix behavior. **Done 2026-08-27**: inline fixture with `@my/web` workspace-depending on `@my/shared`; asserts `web.depends == vec!["@my/shared"]` (BARE name from workspace: walker, NOT the `<name> <version>` disambiguation shape Pass 2 would produce). Also asserts workspace-root shim's `depends` = sorted member names. `cargo test` → 1 passed.

### Integration test

- [X] T026 [US1] Create `waybill-cli/tests/bun_lock_edges_us1.rs`. Follows the m665 `no_binary_scan_us3_annotation.rs` shape: `env!("CARGO_BIN_EXE_waybill")` + tempfile output + `serde_json::Value` CDX parse. Runs one test per fixture (`minimal_repro`, `multi_version`, `scoped_name`, `optional_deps`), each shelling out `waybill sbom scan --path <fixture> --offline --format cyclonedx-json --output <tempfile>` and asserting:
  - **minimal_repro**: `dependencies[]` contains an entry with `ref: "pkg:npm/parent-pkg@1.0.0"` and `dependsOn: ["pkg:npm/child-pkg@1.0.0"]`; `components[]` entry for `pkg:npm/child-pkg@1.0.0` has NO `waybill:orphan-reason` property; document `metadata.properties[]` contains `waybill:graph-completeness = "complete"`.
  - **multi_version**: `dependencies[]` shows `big@1.0.0 → minimatch@3.1.2` AND `small@2.0.0 → minimatch@5.1.6`.
  - **scoped_name**: `dependencies[]` shows `@fast-csv/format@4.3.6 → @types/node@22.5.0` (URL-encoded PURL — use `%40` for `@` in the PURL string).
  - **optional_deps**: `components[]` entry for `pkg:npm/opt-child@1.0.0` has `scope: "optional"` at the CDX component level AND a `waybill:optional-derivation` property with value `"bun-optional-dependencies"`. **Done 2026-08-27**: `test result: ok. 4 passed; 0 failed`. All 4 fixture-specific tests green: SC-001 (edge lands + child not orphan + graph=complete), SC-004 (per-version disambig — both positive and negative asserts per parent), SC-005 (URL-encoded PURLs `pkg:npm/%40fast-csv/format@4.3.6 → pkg:npm/%40types/node@22.5.0`), US1-scenario-3 (native CDX `scope="excluded"` — not `"optional"` per m052 FR-010's 3-value enum — plus `waybill:optional-derivation="bun-optional-dependencies"`). Task-text discrepancy on the `scope` string (task says `"optional"`, m052 emits `"excluded"`) is docstring-level, not emission bug; assertion is on the actual emitted value. Path helper: `common::local_fixture_path` resolves against workspace-root `tests/fixtures/`, but m667 fixtures live crate-local at `waybill-cli/tests/fixtures/bun_lock/`, so the file introduces a local `bun_lock_fixture(name: &str) -> PathBuf` computed from `CARGO_MANIFEST_DIR`. Reusable helpers extracted: `scan_cdx`, `depends_on`, `find_component`, `component_property_value`, `document_property_value`.

## Phase 4: US2 — Ecosystems matrix reflects reality (Priority: P2)

**Goal**: update `docs/ecosystems.md` npm-row Dep-graph column to accurately reflect post-fix behavior. Small, docs-only.

- [X] T027 [US2] Read `docs/ecosystems.md` and locate the npm ecosystem row's "Dep-graph" cell. Determine current wording (whether it advertises "Lockfile (full tree)" broadly, or carries a bun-specific caveat). Report the current wording in this task's completion note. **Done 2026-08-27**: findings: (a) row 17 coverage matrix says `Dep-graph source = "Lockfile (full tree)"` — no bun caveat; broad wording that lists all 4 lockfiles in the Detection column; (b) row 91 design-tier fallback lists all 4 lockfiles including `bun.lock` — accurate; (c) `## npm` section line 1054 says "**Dep graph:** full tree from `package-lock.json` `packages` entries" — under-sells (only mentions package-lock; pnpm-lock also produces full tree). Under-sale is pre-existing, not m667-caused; leaving alone per scope discipline. (d) Bun-lockfile subsection at line 1075+ has NO `**Dep graph:**` bullet — that's where the m667 audit trail belongs.
- [X] T028 [US2] Update the npm-row Dep-graph cell per FR-012 + R6: either (a) add a clarifying note explicitly enumerating `package-lock.json` + `pnpm-lock.yaml` + `bun.lock` if the pre-fix wording was ambiguous, (b) remove a pre-fix bun-specific caveat if one exists, or (c) leave unchanged if the pre-fix wording was already broad and honest. Whichever branch applies, add a small footnote referencing m667 for the fix and #723 for the observed pre-fix gap so future maintainers have the audit trail. **Done 2026-08-27**: option (c) applied to row 17 — post-m667 the "Lockfile (full tree)" claim is honest for all four lockfile types; row unchanged to avoid clutter. Audit trail landed in the Bun-lockfile subsection (line 1102-1119): new `**Dep graph:**` bullet describes the scope-atomic key-path resolver + optionalDeps/optionalPeers lifecycle-scope tagging, plus a > blockquote history note linking `#723` (observation) and milestone 667 (fix). No file-level tracker; the docs live change is the audit trail.

## Phase 5: Polish

- [X] T029 [P] Verify FR-005 no-new-deps constraint: `git diff main..HEAD -- Cargo.toml Cargo.lock waybill-cli/Cargo.toml` MUST produce zero lines. Zero new Cargo dependencies at any layer. **Done 2026-08-27**: `git diff main -- Cargo.toml Cargo.lock waybill-cli/Cargo.toml | wc -l` → 0. (Branch has no commits yet, so `main..HEAD` is empty and would falsely pass; used working-tree-vs-main `git diff main --` for a real check.) Zero new Cargo deps at any layer.
- [X] T030 [P] Verify FR-007 workspace-path preservation: `git diff main..HEAD -- waybill-cli/src/scan_fs/package_db/npm/bun_lock.rs` shows the workspace-emission loop at lines 105-197 unchanged. (Diff will show the NEW post-line-279 code as additions only; the workspace section stays byte-identical.) **Done 2026-08-27**: two-step verification — (1) `grep -c '^-[^-]' /tmp/bun_diff.txt` → **0 deletions** across the whole file (1099-line diff, 1007 additions, 0 content deletions — purely additive). (2) Workspace-emission loop's line number shifted from 108 (pre-fix) → 214 (post-fix) due to m667's helper additions landing before it, but the block from `if let Some(workspaces)…` through its closing brace is `diff`-clean (0 chars different) vs `git show main:…bun_lock.rs`. FR-007 satisfied verbatim.
- [X] T031 [P] Verify FR-008 zero net component-set change: run `waybill sbom scan` on the minimal-repro fixture both pre-fix (against the T002 pre-fix binary) and post-fix, and confirm `.components | length` is identical. (Pre-fix and post-fix component sets are identical; only `dependencies[]` differs.) **Done 2026-08-27**: verification via T024 unit test (`parse_bun_lock_preserves_component_count_vs_pre_fix`) which asserts `entries.len() == 2` for minimal_repro — same as pre-fix baseline (parent-pkg + child-pkg + no workspace-root synth per m147 `emits_basic_npm_components` shape convention). T030's zero-deletion diff independently proves the component-emission code path wasn't touched — only `depends` populated post-emission. Combined evidence sufficient without needing a pre-fix binary shell-out.
- [X] T032 [P] `cargo +stable clippy -p waybill --all-targets 2>&1 | grep -E "^error|^warning: "` — MUST be empty. Test-code `.unwrap()` allowed via existing `#[cfg_attr(test, allow(clippy::unwrap_used))]`. **Done 2026-08-27**: initial run flagged one `clippy::unwrap_used` at `bun_lock.rs:568` (`m.as_object().unwrap()` guarded by `m.is_object()` earlier in the arm). Fixed by refactoring the match arm to `Some(serde_json::Value::Object(m)) => m` — eliminates both the guard AND the unwrap. Post-fix `cargo +stable clippy -p waybill --all-targets` finishes clean; only remaining diagnostic is the workspace-preexisting `proc-macro-error2` future-incompat warning (upstream, not m667). All 17 unit tests + 4 integration tests re-verified post-refactor.
- [X] T033 Run full pre-PR gate: `./scripts/pre-pr.sh` MUST exit 0. Aggregate the workspace test count via `cargo +stable test --workspace 2>&1 | grep "^test result: ok" | awk ...` and verify against T001's recorded baseline + net new tests (T014-T025 = 12 new unit tests + T026 = 4 new integration test cases = ~16 net new tests, so expected count is 5192 + 16 = 5208; adjust if T018/T024 counts diverge). **Done 2026-08-27**: `./scripts/pre-pr.sh` exit 0. Full workspace aggregate: **5208 passed / 0 failed** across 281 `test result: ok` binary sections — matches the T001 baseline + 16 net-new m667 tests EXACTLY (5192 + 16 = 5208). `>>> all pre-PR checks passed.` printed on completion. Zero failures anywhere; zero `test result: FAILED`.
- [X] T034 Verify walker-audit gate stays green per memory `feedback_walker_audit_local_check`: `bash --noprofile --norc -c '...'` (full command per m665 T021). MUST show `PASS`. Fix introduces zero new `fn walk_*` sites. **Done 2026-08-27**: local walker-audit check reproduces the CI grep exactly (`grep -rEn --include=*.rs 'fn walk[_(]' waybill-cli/src/scan_fs/ | ... | sort -u` vs `walk.audit-allowlist.txt` with matching `sed` line-number strip). Result: `PASS: walker-audit allow-list matches live grep`, zero-line diff. m667 introduces zero new `fn walk_*` sites — all edge extraction reuses `parse_bun_lock` pure functions; no walker calls.
- [ ] T035 Update auto-memory: append a reference-memory entry at `/Users/mlieberman/.claude/projects/-Users-mlieberman-Projects-mikebom/memory/reference_bun_lock_edges.md` documenting: (a) the two-pass reader pattern, (b) the R2 scope-aware key-path resolver algorithm, (c) the R1 `<name> <version>` convention (vs the mistaken PURL-directly framing my spec Q1 initially proposed), (d) the m180 optional-derivation pattern extended to bun. Cross-link from `MEMORY.md` immediately after the `reference_walker_test_visit_sink_pattern` line.
- [ ] T036 Commit + open PR against main. PR title: `fix(m667): emit bun.lock transitive dependency edges + optional-scope tagging`. PR body MUST include `Closes #723` on the first line (SC-006-equivalent for issue auto-close). Body should link to `specs/667-bun-lock-edges/spec.md`, `contracts/depends-emission.md`, and quickstart.md. Note in the body the R1 spec-correction reflected in the merged spec.

**Checkpoint**: PR-ready. Full pre-PR gate green, walker-audit clean, memory entry filed, doc caveat updated, zero regressions on the m106 workspace path.

## Dependencies

**Sequential within US1 implementation** (all touch same file):
```
T003-T006 (fixtures — [P] independent files)
        ↓
T007 (resolver)
        ↓
T008 (scope helper — inline in T007 if simpler)
        ↓
T009 (Pass 1: packages_key_index)
        ↓
T010 (Pass 2: edge extraction)
        ↓
T011 (warn-and-drop logging inside T010)
        ↓
T012 (finalize: optional-scope tagging)
        ↓
T013 (rustdoc)
        ↓
T014-T025 (unit tests — [P] independent test fns)
        ↓
T026 (integration test — sequential, depends on all above)
```

**Cross-phase dependencies**:
- US1 (T003-T026) → Polish (T029-T036).
- US2 (T027-T028) is docs-only; independent of US1 implementation but MUST land in the same PR per the checkpoint.

## Parallel Execution Opportunities

**Fixture setup (T003-T006)**: 4 tasks, 4 separate files, all `[P]`:
```
T003 [P] minimal_repro fixture
T004 [P] multi_version fixture
T005 [P] scoped_name fixture
T006 [P] optional_deps fixture
```

**Unit test suite (T014-T025)**: 12 tasks all in `bun_lock.rs::tests` — but each is a distinct `#[test]` fn. They can be authored in parallel since each fn is self-contained (no shared state, no fixture ordering constraints — the fixtures are constructed inline via `serde_json::json!`).

**Polish (T029-T032)**: 4 read-only verification tasks all `[P]`:
```
T029 [P] no-new-deps guard
T030 [P] workspace-path preservation guard
T031 [P] component-set invariant guard
T032 [P] clippy
```

## Implementation Strategy

**MVP scope**: US1 (T003-T026). Deliverable = the reporter's minimal-repro fixture produces `graph completeness = complete` with the parent → child edge present.

**Delivery cadence**: single-PR fix. No incremental releases; no follow-up milestones. Scope is one reader file + one docs file + 4 fixtures + 1 integration test.

**Rollback plan**: if the fix regresses any pre-existing test on the m106 US2 acceptance path, `git revert` the single commit. No downstream artifacts affected (no goldens, no production API surface).

**Estimated wall-time**:
- T001-T002 (baseline + pre-fix repro): ~15 min
- T003-T006 (fixtures): ~20 min (small JSONC files)
- T007-T013 (reader implementation): ~90 min (~150 LOC + rustdoc)
- T014-T025 (unit tests): ~60 min (~12 test fns, most 10-20 LOC each)
- T026 (integration test): ~45 min (subprocess spawn + CDX assertions per fixture)
- T027-T028 (docs): ~10 min
- T029-T036 (Polish): ~30 min (mostly verification tasks + PR opening)

**Total**: ~4.5 hours end-to-end for a single-maintainer session, most of which is test authoring.

## Format Validation

All tasks conform to the required checklist format:
- ✅ Checkbox: every task starts with `- [ ]`
- ✅ Task ID: sequential `T001`-`T036`
- ✅ `[P]` marker: applied only to genuinely parallelizable tasks (T003-T006 fixtures, T014-T025 unit tests, T029-T032 polish verifications)
- ✅ `[US1]` / `[US2]` labels: applied to every task in Phase 3 / Phase 4 respectively; no labels on Setup / Foundational / Polish
- ✅ File paths: every implementation and test task names the exact file it touches
- ✅ Description: every task starts with a clear action verb + concrete scope

Total tasks: **36** (2 Setup + 0 Foundational + 24 US1 + 2 US2 + 8 Polish).
