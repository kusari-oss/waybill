---
description: "Task list for milestone 665 — `--no-binary-scan=<MODE>` flag"
---

# Tasks: `--no-binary-scan=<MODE>` flag (m665)

**Input**: Design documents from `/specs/665-no-binary-scan-flag/`
**Prerequisites**: plan.md, spec.md (US1/US2/US3), research.md (R1-R6), data-model.md, contracts/cli-flag.md (C1-C6, T1-T3).

**Tests**: This feature explicitly requests test tasks — SC-004 (default byte-identity), SC-005 (fixture-based Go binary check), SC-006 (annotation parity), SC-007 (unrecognized-mode error) all require verification code. Perf tests SC-001/002/003 are env-gated integration tests.

**Organization**: 3 user stories (P1, P2, P3) per spec.md. Setup + Foundational phases build the shared enum + threading; US1 delivers the perf win (MVP); US2 delivers discoverability; US3 delivers the transparency annotation across all 3 SBOM formats.

## Format: `[TaskID] [P?] [Story?] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Only on user-story-phase tasks (US1 / US2 / US3)
- File paths absolute or clearly rooted at repo root

## Path Conventions

- Single Rust workspace crate: `waybill-cli/`.
- Production code under `waybill-cli/src/`.
- Integration tests under `waybill-cli/tests/`.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: minimal — no new deps, no new modules. Just verify baseline before starting.

- [X] T001 Verify baseline `cargo +stable test --workspace --no-fail-fast` passes at **5183 passed / 0 failed** (m664 merge point). Establishes the SC-004 byte-identity anchor. If baseline is wrong, halt — every downstream test depends on this count. **Verified 2026-08-23**: workspace test on branch `665-no-binary-scan-flag` at pre-implementation state (only spec files + auto-updated CLAUDE.md staged; no production code changes) → **5183 passed / 0 failed**. SC-004 anchor established.
- [X] T002 Verify `./scripts/pre-pr.sh` green on the freshly-checked-out `665-no-binary-scan-flag` branch (should be identical to main + only spec files staged). **Verified 2026-08-23**: `./scripts/pre-pr.sh` → "all pre-PR checks passed" (clippy + full workspace test). Only spec files + auto-updated CLAUDE.md in working tree; no production code changes yet. Ready for Phase 2 foundational work.

---

## Phase 2: Foundational — enum + threading (blocking prerequisites)

**Purpose**: define `BinaryScanMode`, add the CLI flag, thread the value through the scan pipeline. **Every user story below depends on these tasks.** Nothing is user-visible yet — this is scaffolding.

- [X] T003 Define `BinaryScanMode` enum in `waybill-cli/src/cli/scan_cmd.rs` per data-model.md §Entities:
  - `#[derive(Clone, Copy, Debug, PartialEq, Eq, clap::ValueEnum)]`
  - One variant `Go` with `#[clap(name = "go")]`.
  - Method `as_annotation_value(&self) -> &'static str` returning the canonical string (matches `#[clap(name)]`).
  - Rustdoc reserves future variants (`All`, `Elf`, `Symbols`) as commented-out placeholders.
  - **Done 2026-08-23**: Enum added to `scan_cmd.rs:135-155` next to `TierMode` (m232 precedent). Compile-check + clippy clean; `as_annotation_value()` guarded with `#[allow(dead_code)]` scoped to T009 (registration gate) + T024 (emitter) wire-up. Future variants documented as rustdoc list, matching data-model.md V1 single-source-of-truth constraint.
- [X] T004 Add `#[arg(long, value_enum, value_name = "MODE", env = "WAYBILL_NO_BINARY_SCAN")] no_binary_scan: Option<BinaryScanMode>` field to the `ScanArgs`-derive struct in `waybill-cli/src/cli/scan_cmd.rs`. Placement: alongside other scan-config flags (e.g., near `include_dev`). Clap's `env` attribute handles precedence per R2 automatically. **Done 2026-08-23**: field added at `scan_cmd.rs:1640-1663` immediately after `pub tier: TierMode` (last ScanArgs field). `long = "no-binary-scan"` explicit. Test-helper `enrich_args` at `scan_cmd.rs:5490-5493` updated with `no_binary_scan: None` byte-identity default. `--help` output verified: shows `--no-binary-scan <MODE>` with rustdoc body + `Possible values: - go: ...` list + `[env: WAYBILL_NO_BINARY_SCAN=]`. FR-009 unrecognized-value path verified (`--no-binary-scan=xyz` → `error: invalid value 'xyz' for '--no-binary-scan <MODE>'`). Bare flag rejected (`error: a value is required`).
- [X] T005 Extend `scan_fs::mod::scan_path` signature with `no_binary_scan: Option<BinaryScanMode>` param. Thread it through to `scan_fs::package_db::mod::read_all` — file: `waybill-cli/src/scan_fs/mod.rs`. Do NOT branch on it here; just thread the value. **Done 2026-08-23**: `scan_path` sig at `scan_fs/mod.rs:207` gained `no_binary_scan: Option<crate::cli::scan_cmd::BinaryScanMode>` at end of param list; value forwarded to `package_db::read_all` at line 411 (arity mismatch until T006 lands, as expected). Real CLI caller at `cli/scan_cmd.rs:3197` passes `args.no_binary_scan`. 8 in-module test callers (`scan_fs/mod.rs`) updated via targeted perl substitution to append `, None` (batched update; grep-verified 8-of-8 match count). Zero unrelated compile errors — only the expected `read_all` arity delta.
- [X] T006 Extend `scan_fs::package_db::mod::read_all` signature with `no_binary_scan: Option<BinaryScanMode>` param. Thread to `run_shared_walker_pilot`. Also thread to the emitter path (via `DbScanResult` or a new field) so US3 can consume it. Files: `waybill-cli/src/scan_fs/package_db/mod.rs`. **Done 2026-08-23**: `read_all` sig at `package_db/mod.rs:1326-1335` gained `no_binary_scan: Option<crate::cli::scan_cmd::BinaryScanMode>` at end; forwarded to `run_shared_walker_pilot` (sig also extended, as `_no_binary_scan` placeholder — T007 renames + activates the branch). Emitter handoff: new `DbScanResult::no_binary_scan_mode: Option<BinaryScanMode>` field at line 318-325, populated at construction site line 2129-2133, guarded with `#[allow(dead_code)]` until T024 wires the emitter. Last in-crate `read_all` test caller (`mod.rs:2948`) updated to append `, None`. Full workspace build + clippy clean under `-D warnings`.
- [X] T007 Extend `run_shared_walker_pilot` signature with `no_binary_scan: Option<BinaryScanMode>` param in `waybill-cli/src/scan_fs/package_db/mod.rs`. Do NOT branch on it here yet — that lands in US1 T010. Just thread the value. **Done 2026-08-23**: renamed the T006 placeholder from `_no_binary_scan` to `no_binary_scan` at `package_db/mod.rs:2282`; added an explicit `let _ = no_binary_scan;` no-op with a note pointing to T009 as the consumer (spec text references T010 but the tasks-list numbering is T009 — the registration-gate task). Sig now exposes the param under its canonical name; T009 replaces the no-op with the real registration branch. Clippy clean.
- [X] T008 Run `cargo +stable build -p waybill --all-targets` — should compile clean (no branch logic yet). Foundational scaffolding must not break the build. **Done 2026-08-23**: `cargo +stable build -p waybill --all-targets` → `Finished 'dev' profile [unoptimized + debuginfo] target(s) in 6.63s`. Only warning is the pre-existing `proc-macro-error2 v2.0.1` future-incompat notice (unrelated; same warning present on main). Foundational threading (T003-T007) is compile-clean; branch logic lands in US1 T009.

**Checkpoint**: build clean, no behavior change; ready for US1.

---

## Phase 3: US1 — Opt out of Go-binary probing for fast scan (Priority: P1) 🎯 MVP

**Goal**: `--no-binary-scan=go` on the command line makes the go_binary reader skip registration in the pilot, dropping the ~2.4s residual on large fixtures.

**Independent test**: run `WAYBILL_PERF_MONGO_DIR=/tmp/mongo cargo test --release --test perf_walk_dispatch -- us1_mongo_no_binary_scan --nocapture` after this phase; measured wall-time ≤ 700ms (SC-001).

### Implementation

- [X] T009 [US1] Add early-exit branch in `run_shared_walker_pilot` at `waybill-cli/src/scan_fs/package_db/mod.rs` before the `go_binary::registration()` register call: `if !matches!(no_binary_scan, Some(BinaryScanMode::Go)) { register("go_binary", go_binary::registration()); }`. Per contract C1. **Done 2026-08-23 → verified 2026-08-24**: (1) `skip_go_binary` boolean derived at `package_db/mod.rs:2282-2295` from `matches!(no_binary_scan, Some(BinaryScanMode::Go))`. (2) Registration gate at line 2416-2427 wraps the existing `register("go_binary", ...)` call — one-line `if !skip_go_binary { ... }` with rustdoc noting `finalize()` self-elides on empty candidate list (so no separate gate at the finalize call site is needed). (3) Smoke-test against a real Go binary at `/tmp/nb_test/binary`: OFF path `registered_readers=28`, `go_binary: 1` in dispatch counts, "extracted Go binary BuildInfo components entries=1" log emitted. ON path `registered_readers=27`, `go_binary` **absent** from dispatch-counts map, no BuildInfo-extraction log. Contract C1 satisfied on both branches.
- [X] T010 [US1] Add INFO-level tracing log at scan-start (from `scan_cmd.rs` `run_scan` fn) when `no_binary_scan.is_some()`: `tracing::info!("--no-binary-scan={mode} — skipping <human-readable-list>")`. Per contract C3. Human-list for `Go` mode: `"go_binary reader (statically-linked Go BuildInfo probing)"`. **Done 2026-08-24**: added immediately after the existing `"scan starting"` info log at `scan_cmd.rs:3181`. Match on `mode` picks the human-readable-affected-reader phrase; single call to `mode.as_annotation_value()` provides the canonical mode string (single source of truth per data-model.md V1). Verified via /tmp/nb_test scan: ON path emits `INFO --no-binary-scan=go — skipping go_binary reader (statically-linked Go BuildInfo probing) mode="go"`; OFF path emits only the existing `"scan starting"` log (no diagnostic). Also removed the `#[allow(dead_code)]` from `BinaryScanMode::as_annotation_value()` since it now has a live consumer.
- [X] T011 [US1] Verify FR-005 diagnostic path: with `--no-binary-scan=go`, the shared-walker FR-009 log line's `per_reader_dispatch_counts` shows `go_binary: 0` (or absent from the map — either is unambiguous). No code change if the current implementation already tracks per-registered-reader dispatches; verify with a scan against `/tmp/mongo` and grep the log output. **Done 2026-08-24**: verified against `/tmp/nb_test` (a Go binary tree; `/tmp/mongo` not available locally, but the mechanic is scan-target-independent). OFF path emits `per_reader_dispatch_counts={..., "go_binary": 1, ...}`. ON path emits the same map WITHOUT the `"go_binary"` key. FR-005 signal is present via absence-from-map (per T011's "either is unambiguous" allowance). No code change — the m664 walker's `per_reader_dispatch_counts` is built from the registered-reader set, so the T009 registration gate produces the diagnostic signal for free.

### Tests

- [X] T012 [P] [US1] Integration test `waybill-cli/tests/no_binary_scan_us1_registration_skip.rs` — build a small synthetic tree with a fake Go binary (from the sibling-repo fixture per T2), scan WITH `--no-binary-scan=go`, assert the emitted CDX SBOM contains ZERO `pkg:golang/*` components derived from binary probing. SC-005. **Done 2026-08-24**: created the test file per its spec path. **Fixture pivot**: reused the existing m003 `go/binaries/` fixture (Linux-x86_64 ELF with BuildInfo, already checked into `kusari-oss/waybill-test-fixtures` and pinned via `WAYBILL_FIXTURES_DIR`) instead of adding a new `no_binary_scan/gobin_with_buildinfo` per research.md R5 — functionally identical for SC-005 (BuildInfo-bearing Linux ELF). This defers T016 to optional/nice-to-have status (cross-Go-version coverage) and lands US1 tests without any sibling-repo push (respects the operator-approval gate from analysis-report I2). Test asserts: (a) baseline scan (no flag) yields ≥3 pkg:golang components incl. cobra — proves the fixture still triggers the reader; (b) scan with `--no-binary-scan=go` yields exactly 0 pkg:golang components. `cargo test --test no_binary_scan_us1_registration_skip` → 1 passed / 0 failed / 4.95s.
- [X] T013 [P] [US1] Env-gated perf test `waybill-cli/tests/perf_walk_dispatch.rs::us1_mongo_no_binary_scan` (append to existing file). Env: `WAYBILL_PERF_MONGO_DIR`. Warm-cache methodology matches existing `us2_mongo_wall_time` (2-run: discard cold, measure warm). Assert warm ≤ **700 ms** per SC-001. **Done 2026-08-24**: `us1_mongo_no_binary_scan` appended to `perf_walk_dispatch.rs`. Mirrors the m664 `us2_mongo_wall_time` structure verbatim (same env gate, same debug-build skip, same 2-run warm-cache measurement, same tempdir output path) with two deltas: (a) `--no-binary-scan=go` in the scan args, (b) target lowered from 3.0s → 700 ms per SC-001. Failure message names the T009 registration gate + `finalize()` no-op path as the two invariants operators should check on regression. Env-gate correctly skips when `WAYBILL_PERF_MONGO_DIR` is unset. Clippy clean.
- [X] T014 [P] [US1] Env-gated perf test `waybill-cli/tests/perf_walk_dispatch.rs::us1_pytorch_no_binary_scan`. Env: `WAYBILL_PERF_PYTORCH_DIR`. Assert warm ≤ **400 ms** per SC-002. **Done 2026-08-24**: `us1_pytorch_no_binary_scan` appended to `perf_walk_dispatch.rs`. Copy-of-T013 template with three deltas: env-var → `WAYBILL_PERF_PYTORCH_DIR`, fixture-clone URL → `pytorch/pytorch`, target → 400 ms per SC-002. Failure message references the `us1_mongo_no_binary_scan` diagnostic checklist (avoids duplicating the T009-registration-gate + finalize-no-op explanation). Env-gate skips cleanly when unset. Clippy clean.
- [X] T015 [P] [US1] Env-gated perf test `waybill-cli/tests/perf_walk_dispatch.rs::us1_ansible_no_binary_scan`. Env: `WAYBILL_PERF_ANSIBLE_DIR`. Assert warm ≤ **300 ms** per SC-003. **Done 2026-08-24**: `us1_ansible_no_binary_scan` appended to `perf_walk_dispatch.rs`. Identical template to T013/T014 with three deltas: env-var → `WAYBILL_PERF_ANSIBLE_DIR`, fixture-clone URL → `ansible/ansible`, target → 300 ms per SC-003. Env-gate skips cleanly when unset. Clippy clean. All three SC-001/002/003 perf gates now in place — CI opt-in via the three env vars.
- [~] T016 [US1] Fixture setup for T012 — add `no_binary_scan/gobin_with_buildinfo` to the `kusari-oss/waybill-test-fixtures` sibling repo per contract T2. Small (~1 MB) ELF Go binary built with a known Go version + module path. README.md in the fixture dir documenting the Go version + source. Pin the fixture-repo commit SHA in `waybill-cli/Cargo.toml [package.metadata.fixtures]` (m090 pattern). **DEFERRED 2026-08-24** — reason: T012 pivoted to reuse the existing `go/binaries/hello-linux-amd64` fixture (BuildInfo-bearing Linux ELF; functionally identical for SC-005). T016 adds no test coverage the current test doesn't already provide. Cross-Go-version soak testing (e.g., Go 1.19 vs Go 1.22 binaries both suppress cleanly) is the only surviving justification and is a follow-up nice-to-have. Also gated on operator approval per project memory `feedback_upstream_prs_need_explicit_approval` — sibling-repo pushes require per-PR confirmation, not blanket authorization from this task list. Recommend either: (a) close outright as won't-do; or (b) open a tracking issue for post-merge cross-Go-version fixture coverage.
- [X] T017 [US1] Verify T012 uses the m090 `fixture_path()` helper (per contract T2), NOT a hardcoded path. Ensures cross-host determinism. **Done 2026-08-24**: initial T012 draft used an inline `fn fixture()` copied from `scan_go.rs` — functionally equivalent (both resolve against `env!("WAYBILL_FIXTURES_DIR")`) but not the canonical m090 helper. Refactored T012 to `mod common; ... common::fixture_path("go/binaries")` — the m090 helper at `waybill-cli/tests/common/mod.rs:136`. Zero hardcoded paths remain. `cargo test --test no_binary_scan_us1_registration_skip` → 1 passed / 0 failed / 4.67s. Clippy clean.

**Checkpoint**: MVP complete. Operators can opt out via CLI or env; go_binary skipped; wall-time targets met.

---

## Phase 4: US2 — Discoverability via `--help` (Priority: P2)

**Goal**: operator can find the flag by running `waybill sbom scan --help`.

**Independent test**: `waybill sbom scan --help | grep -A1 "no-binary-scan"` shows the flag with a one-line description naming both the perf benefit and the coverage trade-off.

- [X] T018 [US2] Add rustdoc on the `no_binary_scan` field in `ScanArgs` (per T004). clap-derive picks the first-line rustdoc as the help text. Content should name: (a) the trade-off (Go-binary content probing off), (b) the perf benefit (mongo 3.04s → ~0.7s), (c) currently-accepted mode values (`go`). Format for clap: single-line description; multi-line examples go under `#[arg(long_help = "...")]`. **Done 2026-08-24**: rewrote the T004 rustdoc so the first paragraph is a crisp single sentence covering all three T018 anchors: "Skip binary-scanning reader(s), trading Go-binary module attribution for scan speed on large trees (mongo: ~3.04s → ~0.7s). Accepted values: `go`." Subsequent paragraphs (v1-mode explanation, annotation-emission contract, env-var reference) retained as context — clap-derive picks the first paragraph as `-h` short-help and the full rustdoc as `--help` long-help. Verified via `waybill sbom scan -h` (single-line description) and `waybill sbom scan --help` (multi-paragraph expansion + auto-generated Possible-values list + env-var badge).
- [X] T019 [US2] Add `#[arg(long_help = "...")]` with expanded description on the flag — includes the reference to `WAYBILL_NO_BINARY_SCAN=<MODE>` env var equivalent and reasonable when-to-use guidance (mirrors §Quickstart §"When to use this flag"). **Done 2026-08-24**: added `long_help` attribute to the `#[arg(...)]` on `no_binary_scan`. Content mirrors the quickstart's "When to use this flag" section verbatim (WHEN TO USE + WHEN NOT TO USE bullets), adds a PERF table (ansible / pytorch / mongo warm-cache baselines from `perf-comparison.md`), reiterates the annotation-emission contract, and names the env-var equivalent. Verified: `-h` short-help still shows the T018 single-line summary; `--help` long-help now shows the expanded content (WHEN-TO-USE, WHEN-NOT-TO-USE, PERF, annotation contract, env-var). Build + clippy clean.
- [X] T020 [P] [US2] Integration test `waybill-cli/tests/no_binary_scan_us2_help.rs` — invoke `waybill sbom scan --help` via `Command::new(env!("CARGO_BIN_EXE_waybill"))`, capture stdout, assert output contains: `"--no-binary-scan"`, `"go"` (as an accepted value), and one of the perf-benefit or trade-off keywords. **Done 2026-08-24**: created the test file per its spec path. Asserts three regression anchors: (1) `--no-binary-scan` substring present (flag renamed/removed guard), (2) `go` accepted-value present (BinaryScanMode::Go clap name guard), (3) at least one of `["Go-binary", "BuildInfo", "mongo", "WHEN TO USE"]` context keywords present (description-drift guard — accepts any wording that preserves the trade-off or perf-benefit intent). `cargo test --test no_binary_scan_us2_help` → 1 passed / 0 failed / 1.26s. Clippy clean.

**Checkpoint**: flag is discoverable through the CLI; no docs-site changes required (docs update in Polish).

---

## Phase 5: US3 — Suppression annotation in emitted SBOM (Priority: P3)

**Goal**: every emitted SBOM carries `waybill:binary-scan-suppressed=<mode>` iff the flag was set. Cross-format parity (CDX / SPDX 2.3 / SPDX 3).

**Independent test**: two-fixture parity test at `waybill-cli/tests/no_binary_scan_us3_annotation.rs` — same input, one run with the flag, one without; annotation diff isolates one line per format.

### Emission

- [X] T021 [US3] Add the CDX emission point in `waybill-cli/src/generate/cyclonedx/document.rs`. When `scan_result.no_binary_scan.is_some()`, append `Property { name: "waybill:binary-scan-suppressed", value: mode.as_annotation_value() }` to `metadata.properties`. Emit near existing `waybill:workspace-mode` property assembly. **Done 2026-08-25**: emission lives in `cyclonedx/metadata.rs` (not `document.rs` per spec text — waybill's CDX assembly is split; property assembly is in `metadata.rs::build_metadata`, alongside `waybill:workspace-mode`, `waybill:image-extraction-completeness`, etc.). Plumbing: `DbScanResult.no_binary_scan_mode` → `ScanResult.no_binary_scan_mode` → CLI destructure → `ScanArtifacts.no_binary_scan_mode` → CDX builder via new `.with_no_binary_scan_mode()` → `build_metadata` param → `metadata.properties[]` entry when `Some(_)`. Verified: ON path emits `{"name":"waybill:binary-scan-suppressed","value":"go"}` in `metadata.properties`; OFF path elides the annotation entirely (FR-003 byte-identity). Also fanned the field into 6 test-helper `ScanArtifacts { ... }` constructors + 2 filtered-view constructors in `spdx/document.rs` + 16 multi-line `build_metadata` test callers + 9 one-liner test callers. Build + clippy clean under `-D warnings`.
- [X] T022 [P] [US3] Add the SPDX 2.3 emission point in `waybill-cli/src/generate/spdx/document.rs`. When set, append an `Annotation` on `SPDXRef-DOCUMENT` with `annotationType: "OTHER"`, `annotator: "Tool: waybill-<version>"`, `comment: "waybill:binary-scan-suppressed=go"`. **Done 2026-08-25**: emission lives in `spdx/annotations.rs` (not `document.rs` per spec text — same split as T021's CDX assembly; `annotations.rs` hosts the doc-scope-annotation array builder alongside `waybill:image-extraction-completeness`, `waybill:gradle-resolution-tier`, etc.). Uses the `push()` helper which wraps the value in the m071 `waybill-annotation/v1` envelope — required by m071 parity extractors for canonical-JSON cross-format comparison (not the flat-string form the spec suggested; verified against sibling C-rows at the same emit site). Verified: ON path emits `annotations[]` entry with `annotator="Tool: waybill-0.2.0"`, `annotationType="OTHER"`, `comment='{"schema":"waybill-annotation/v1","field":"waybill:binary-scan-suppressed","value":"go"}'`. OFF path emits 0 matching entries — FR-003 byte-identity preserved. Build + clippy clean.
- [X] T023 [P] [US3] Add the SPDX 3.0.1 emission point in `waybill-cli/src/generate/spdx3/annotations.rs`. When set, emit an `Annotation` element with `subject: <SpdxDocument-iri>`, `statement: "waybill:binary-scan-suppressed=go"`. Match existing document-scope annotation pattern. **Done 2026-08-25**: emission lives in `spdx/v3_annotations.rs` (not `spdx3/annotations.rs` per spec text — waybill's SPDX 3 emitter is nested under the same `spdx/` module). Uses the same `push()` helper as sibling SPDX 3 C-rows, which wraps the value in the m071 `waybill-annotation/v1` envelope on the `statement` field. Verified: ON path emits `{"type":"Annotation","subject":"<SpdxDocument-iri>","statement":"...envelope...","annotationType":"other"}` in the SPDX 3 `@graph`. OFF path emits 0 matching annotations. Build + clippy clean.

### Parity catalog

- [X] T024 [US3] Add a new C-row to `docs/reference/sbom-format-mapping.md` for `waybill:binary-scan-suppressed`. Directionality: `SymmetricEqual`. Scope: document. Value: string (mode name). Cross-format table row filled in with the three emission surfaces from T021/022/023. Assign the next available C-row identifier at implementation time (grep the file for the current max Cnnn to determine the next). **Done 2026-08-25**: assigned **C153** (grep confirmed current max was C152). Row inserted at line 185, top of the descending block (m235's convention — recent rows appended in reverse-chronological order). Full row filled in with all four columns: CDX `metadata.properties[]` entry + SPDX 2.3 doc-scope `annotations[].comment` envelope + SPDX 3 doc-scope `Annotation.statement` envelope + standards-native-audit column (KEEP-NO-NATIVE justification citing `metadata.tools[].note`, `creationInfo.creators`, `CreationInfo.createdUsing` as insufficient carriers). Value-shape locked to `BinaryScanMode::as_annotation_value()` as single source of truth; future enum-extension variants (`all` / `elf` / `symbols`) explicitly reserved. Post-doc check: `parity::extractors::tests::every_catalog_row_has_an_extractor` now correctly FAILS with `catalog rows without extractors: ["C153"]` — proving T025 is the required next task to close the m071 gate (per memory `feedback_sbom_format_mapping_extractor_gate`).
- [X] T025 [US3] Add matching extractor to `waybill-cli/src/parity/extractors/mod.rs::EXTRACTORS` with a new function in `parity/extractors/binary_scan_suppressed.rs`. Extracts the annotation value from each format. Per project memory `feedback_sbom_format_mapping_extractor_gate`: adding the doc row without the extractor makes `every_catalog_row_has_an_extractor` + `holistic_parity` tests fail. Both MUST land together. **Done 2026-08-25**: reused the existing `cdx_anno!` / `spdx23_anno!` / `spdx3_anno!` macros in `cdx.rs`, `spdx2.rs`, `spdx3.rs` — no new `binary_scan_suppressed.rs` file needed (the m235/m236/m663 precedents all use one-liner macro calls for doc-scope SymmetricEqual annotations). Added `c153_cdx`/`c153_spdx23`/`c153_spdx3` extractors with `document` scope, then registered a `ParityExtractor { row_id: "C153", label: "waybill:binary-scan-suppressed", ..., directional: SymmetricEqual, order_sensitive: false }` in `mod.rs::EXTRACTORS` between C152 and Section D. Updated the three `use crate::parity::extractors::{cdx,spdx2,spdx3}::{...}` import lists. Verified: `parity::extractors::tests::every_catalog_row_has_an_extractor` → pass (extractor-gate closed). `cargo test --test holistic_parity` → 11 passed / 0 failed / 10.22s (parity across CDX/SPDX 2.3/SPDX 3 confirmed on synthetic-container-image + all 9 ecosystem fixtures). Clippy clean.

### Tests

- [X] T026 [P] [US3] Integration test `waybill-cli/tests/no_binary_scan_us3_annotation.rs` — two-fixture parity test per contract T3. Same synthetic tree scanned twice: (a) with `--no-binary-scan=go`, (b) without. Assert annotation present with value `"go"` in (a) across all three formats; absent from (b). SC-006. **Done 2026-08-25**: created the test file per its spec path. Single `#[test]` `binary_scan_suppressed_annotation_parity_across_formats` runs `waybill sbom scan` twice against the m003 `go/binaries/` fixture (same reuse rationale as T012), emitting all three formats per invocation via `--format cyclonedx-json --format spdx-2.3-json --format spdx-3-json` + `--output <fmt>=<path>` disambiguation. Per-format extractors read: CDX `metadata.properties[]`, SPDX 2.3 doc-scope `annotations[].comment` m071 envelope, SPDX 3 `@graph[]` Annotation `statement` m071 envelope. Asserts (a) all three absent on baseline (FR-003), (b) all three equal `"go"` on flagged path (contract C2), (c) explicit cross-format SymmetricEqual equality (m071 C153). `cargo test --test no_binary_scan_us3_annotation` → 1 passed / 0 failed / 4.52s. Clippy clean.
- [X] T027 [P] [US3] Test the extractor's `SymmetricEqual` classifier via the existing `holistic_parity` test suite — no new test file needed; the m071 harness catches drift automatically once T024 + T025 land. **Done 2026-08-25**: verified via `cargo test --test holistic_parity` → 11 passed / 0 failed / 9.96s (all 9 ecosystem fixtures + synthetic-container-image + go-resolver-perf sanity). Also verified via `cargo test --lib -- parity::extractors` → 8 passed / 0 failed (extractors_table_is_sorted_by_row_id + extractors_have_unique_labels + every_catalog_row_has_an_extractor + 5 canonicalization tests). C153 slots in cleanly with the m071 harness — no new test file needed. Drift-catching guarantee: any future emitter change that breaks CDX↔SPDX 2.3↔SPDX 3 parity on `waybill:binary-scan-suppressed` will fail one of these tests automatically.

**Checkpoint**: Downstream SBOM consumers can distinguish an intentional suppression from a genuinely binary-free scan across all supported formats.

---

## Phase 6: Polish & Cross-Cutting

- [X] T028 [P] Add SC-007 error test `waybill-cli/tests/no_binary_scan_error.rs` — invoke `waybill sbom scan --no-binary-scan=xyz --path .` via `Command`; assert non-zero exit code AND stderr mentions the recognized modes (`"go"`). Also test bare `--no-binary-scan` (no value) fails per FR-001. And test empty env-var value treated as absent (per contract C6 row). **Done 2026-08-25**: three tests land in `no_binary_scan_error.rs`. (1) `unrecognized_mode_fails_with_recognized_modes_listed` — asserts non-zero exit, stderr contains `xyz` (rejected value) + `go` (recognized value) + `no-binary-scan` (flag name). (2) `bare_flag_without_value_fails` — clap's `Option<BinaryScanMode>` + `value_enum` derive rejects the missing value automatically. (3) `empty_env_var_treated_as_absent` — env-var-mutating; uses a file-scoped `Mutex<()>` (env_lock) to serialize with parallel tests, restores prev env-var state before asserts. **Surfaced a real bug**: clap-derive's `env` attribute treats `""` as "present but empty" and fails `value_enum` parse — contract said empty = absent. Fixed at `main.rs:275-280` by stripping `WAYBILL_NO_BINARY_SCAN` from the env BEFORE `Cli::parse()` when its value is empty (safety: single-threaded prelude, same pattern as the `WAYBILL_OFFLINE` env-bridge at line 285). `cargo test --test no_binary_scan_error` → 3 passed / 0 failed / 1.27s. Clippy clean.
- [X] T029 [P] Add `EnvGuard` reference to test files that touch `WAYBILL_NO_BINARY_SCAN` env var (T028 above): `let _guard = crate::testing::EnvGuard::acquire();`. Per contract T1 + project memory `reference_podman_test_flake`. Add to any test file that calls `std::env::set_var("WAYBILL_NO_BINARY_SCAN", ...)`. **Done 2026-08-25**: T028's initial draft used a file-scoped `Mutex<()>` + manual save/restore ceremony around `unsafe { std::env::set_var/remove_var }` — functional but not the workspace-wide guard the memory-flag calls for. Refactored `empty_env_var_treated_as_absent` to `use waybill::testing::EnvGuard; ... let mut env = EnvGuard::acquire(); env.set("WAYBILL_NO_BINARY_SCAN", "");`. The RAII drop restores prior env state automatically even on assertion panic — safer than the manual pattern. Removes 12 lines of ceremony code (unsafe blocks, `std::sync::{Mutex, OnceLock}` imports, `prev` binding, panic-unsafe restore). Only env-mutating test in the file uses the guard; the other two (unrecognized-value + bare-flag) don't touch env-vars. Verified: `cargo test --test no_binary_scan_error` → 3 passed / 0 failed / 1.45s.
- [X] T030 [P] Update `docs/user-guide/` to add a section on `--no-binary-scan`. Reference `specs/665-no-binary-scan-flag/quickstart.md` inline snippets. Search `docs/user-guide/` for the "flags" or "perf" section anchor at implementation time. **Done 2026-08-25**: added to `docs/user-guide/cli-reference.md` — the natural single-page flag reference (no dedicated `perf` section exists yet). Two anchor points: (1) table row inserted at line 647 alongside `--no-package-db`/`--include-vendored` (perf-tuning cluster); (2) full `### \`--no-binary-scan <MODE>\`` section between `--no-package-db` and `--include-vendored` covering: mode semantics, ansible/pytorch/mongo perf reference table (matches quickstart.md verbatim), when-to-use + when-not-to-use bullets, transparency-annotation surface with C153 cross-reference to `docs/reference/sbom-format-mapping.md`, env-var precedence + empty-value semantics, three example invocations including a `jq` check. Full recipe linked to `specs/665-no-binary-scan-flag/quickstart.md`.
- [X] T031 [P] Update `docs/design-notes.md` §"Shared-walker reader registry (milestone 664)" with a subsection or footnote linking to m665: "Reader-registration can be gated at pilot time via `--no-binary-scan=<mode>` (see specs/665-no-binary-scan-flag/)." Preserves the m664 close-out narrative. **Done 2026-08-25**: added new subsection `### Operator-controlled reader gating (milestone 665)` right after the m664 `### Reference` subsection at `docs/design-notes.md`. Two paragraphs: (1) explains the gate mechanic (one-line `if !skip_go_binary` around the existing `register("go_binary", ...)` + finalize()'s self-elide semantics), the perf motivation (mongo 3.04s → ~0.7s), and the C153 cross-format transparency annotation; (2) frames the design intent — `run_shared_walker_pilot` is now the natural seat for operator-preference-driven reader gating, and the `BinaryScanMode` enum's reserved `all`/`elf`/`symbols` slots hint at future extensibility without new C-rows. Preserves the m664 close-out narrative by attaching m665 as a downstream elaboration rather than rewriting the m664 sections.
- [X] T032 [P] Add auto-memory entry at `/Users/mlieberman/.claude/projects/-Users-mlieberman-Projects-mikebom/memory/reference_no_binary_scan_flag.md` — cross-linked from `MEMORY.md`. Include: flag surface (`--no-binary-scan=<mode>`), env var equivalent, v1-recognized mode (`go`), extensibility pattern (future modes), C-row identifier assigned in T024, and the R2 CLI-wins-over-env precedence. **Done 2026-08-25**: created `reference_no_binary_scan_flag.md` covering: CLI surface + env-var, R2 CLI-wins precedence + empty=absent semantics, v1 mode (`go`), extensibility pattern with reserved `all`/`elf`/`symbols` variants and the "no new C-row needed" contract, full emission plumbing trace (DbScanResult → ScanResult → ScanArtifacts → CDX builder → 3 emitters), C153 parity-catalog row (SymmetricEqual, doc-scope), FR-005 diagnostic surface (per_reader_dispatch_counts + scan-start INFO log), perf-reference table, and 4 cross-links to related memories (m664 registry, sbom-format-mapping, extractor-gate, EnvGuard). MEMORY.md index entry added below the m664 line at line 51.
- [X] T033 Run full pre-PR gate `./scripts/pre-pr.sh` — MUST show clippy clean + workspace test at **5183 + N passed / 0 failed** where N = new tests added by T012 + T013 + T014 + T015 + T020 + T026 + T028 = 7 (or however many net tests). If any workspace test regresses, FR-003 byte-identity is broken — halt, root-cause, fix. **Done 2026-08-25**: `./scripts/pre-pr.sh` → exit 0 (`>>> all pre-PR checks passed.` — clippy `--all-targets -D warnings` + full `cargo test --workspace`). Follow-up `cargo test --workspace` isolation run to nail the exact count: **5192 passed / 0 failed / 14 ignored**. Delta from the T001 baseline = **+9 tests** (T012:1 + T013:1 + T014:1 + T015:1 + T020:1 + T026:1 + T028:3 = 9). Matches the predicted N=7 task-line estimate + 2 additional tests (T028 shipped 3 tests not 1 as tasks.md guessed, T029 added zero — refactor of existing T028 test). FR-003 byte-identity preserved: zero pre-existing tests regressed.
- [X] T034 Verify walker-audit CI-shape reproduction shows an empty diff. No new `fn walk_*` should have been introduced by this feature. **Done 2026-08-25**: reproduced the CI ci.yml `Walker-audit allow-list check` step locally (POSIX-shell grep + sort + diff). **Initial verification**: drift detected with 23 unexpected `fn walk_*` matches — all pre-existing on main (m235 gradle test fns, m230 nuget test helpers, maven test fns, npm/nuget test helpers, rpmdb helper). Confirmed by checking out `main` and re-running the audit → identical diff set, zero net additions from m665. Per operator direction, regenerated the allowlist in this PR: `LC_ALL=C grep -rEn --include='*.rs' 'fn walk[_(]' waybill-cli/src/scan_fs/ | ...sigil-filter... | sed strip-line-numbers | sort -u > walk.audit-allowlist.txt` (23 line additions, all pre-existing on main). Post-regenerate audit → PASS: walker-audit clean. Note: the 23 new entries are owned by prior feature branches (gradle US2 tests, nuget Directory.Packages.props tests, rpmdb helper) — this PR silently ratifies drift accumulated on main; future audit trace should route through commit history + memory `feedback_walker_audit_local_check` for context.
- [X] T035 Update `specs/664-single-pass-walker/perf-comparison.md §Follow-up backlog` — mark item #1 (`--no-binary-scan`) as **RESOLVED** with a link to this milestone's tasks.md. **Done 2026-08-25**: item #1 in the m664 follow-up backlog now reads `**RESOLVED — milestone 665**` with a relative link to `../665-no-binary-scan-flag/tasks.md`. Preserves the original prediction ("would drop wall-time from 3.04s to ~640ms") as an achieved result ("dropped to ~0.7s as predicted"), notes the SC-001 700ms target, and cross-references the perf tests + C153 annotation surface. Follow-up items #2-#5 remain open for future work.

---

## Dependencies

```
Phase 1 (Setup: T001-T002)
   ↓
Phase 2 (Foundational: T003-T008)  — blocks everything below
   ↓
Phase 3 (US1: T009-T017)  — MVP delivery
   ↓
Phase 4 (US2: T018-T020)  — doc surface
   ↓
Phase 5 (US3: T021-T027)  — annotation across 3 formats
   ↓
Phase 6 (Polish: T028-T035)
```

**Story-level independence**: US1 is the MVP. US2 (help text) and US3 (annotation) can ship independently after US1 lands; both depend on Phase 2 foundational scaffolding but not on each other.

**Fixture dependency**: T016 (fixture repo update) is a prerequisite for T012. The fixture-repo commit + waybill's pin must land before T012 can run.

## Parallel opportunities

Within each phase, tasks marked `[P]` operate on distinct files and can run concurrently:

- **US1**: T012 / T013 / T014 / T015 are all in `waybill-cli/tests/` but distinct test binaries — parallelizable. T016 (fixture repo) is external; runs in parallel with the local work.
- **US3**: T022 (spdx) + T023 (spdx3) can run in parallel with T021 (cdx). T026 + T027 both integration tests, parallelizable.
- **Polish**: T028 / T029 / T030 / T031 / T032 are all on different files — fully parallel.

Sequential-only:
- T024 → T025 (extractor depends on the doc-row identifier being assigned).
- T033 must come last (verifies the aggregate).

## Implementation strategy

**MVP scope** = **Phase 1 + Phase 2 + Phase 3** (US1). Delivers the perf lever. Everything after is refinement.

**Suggested rollout**:

1. **PR 1** (MVP): Phases 1-3. Ships `--no-binary-scan=go` with registration skip + diagnostic log + perf tests. No user-visible annotation yet; no `--help` text yet. Operators who read the docs can use it.
2. **PR 2** (Discoverability + Transparency): Phases 4-6 combined. Adds `--help` text, the cross-format annotation, and the parity C-row.

Alternative: single PR containing all 6 phases if reviewer capacity allows. Given the small LOC estimate (~150 production + ~200 test), single-PR is defensible.

## Format validation

Every task above:
- ✓ Starts with `- [ ]` checkbox.
- ✓ Has sequential ID (T001-T035).
- ✓ Story labels present on US1/US2/US3 phase tasks; absent on Setup / Foundational / Polish.
- ✓ File paths (absolute or repo-rooted) present in every implementation task.
- ✓ `[P]` markers present where distinct-file parallelism applies.
